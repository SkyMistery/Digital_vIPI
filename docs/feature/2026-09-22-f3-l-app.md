# F3 — L'app: aprire il sector, vederlo sulla mappa, modificarlo e salvarlo senza danni (22 settembre 2026)

> **Stato: 🟡 IN CORSO — slice 0-8 fatte** (23 settembre 2026), CI verde. ▶ Prossima: slice 9, il **salvataggio** —
> la prima che scrive sul disco.
> §9 accettato dal committente, con **una modifica**: la consegna
> passa da una **release GitHub** con il link nella vIPI, visibile solo ad admin ed editor (§9.2, §9.7, slice 11).
> Terza fase di Aurora Sector Lab ([carta madre](2026-09-18-aurora-sector-lab.md), §1, §7, §8). Usa il motore di
> F2 ([carta F2](2026-09-22-f2-motore-del-sector.md)) così com'è. Metodo: [FEATURE-PROCESS](../FEATURE-PROCESS.md).
> 🔴 **Nessun dato di vIPI si tocca**, niente migrazioni. Il sito cambia solo per **una scheda di download**
> (slice 11); l'app è un eseguibile nuovo, fuori dal pacchetto.

## §0 — La domanda

La carta madre (§7) chiede a F3: **guscio, sfoglia, mappa di anteprima con contesto, diff, backup, scrittura
atomica**; finita quando **una modifica = un diff di 3 righe e la forma sulla mappa**.

Con F2 la metà difficile c'è già: il motore legge tutto l'albero, capisce ogni riga, valida e riscrive **solo il
campo cambiato** (una modifica per record = una riga, sull'albero intero). Quello che manca è il giro dell'AOD:
**apro → vedo → cambio → guardo che cosa cambierà → salvo senza poter fare danni**. F3 è quel giro, e nient'altro:
niente git (F4), niente Aurora (F5), niente PDF (F6).

Il criterio, detto in modo misurabile: sposto un fix → il diff mostra **una riga tolta e una aggiunta** (più le righe
di contesto), la mappa mostra il punto nel posto nuovo, il file sul disco dopo il salvataggio è **quello del diff e
nient'altro**, e l'originale sta in un backup.

## §1 — Le misure (22 settembre)

**Il motore sull'albero intero** (master `7e761aa`, 749 file; prova in `scratchpad\f3-misure`, fuori repo):

| cosa | misura |
|---|---|
| file letti (col lettore) | 701 |
| record | 118 315 |
| righe | 260 673 |
| **apertura** (lettura + basi di ogni record, F2 §9.5) | **1,4 s a freddo**, 0,8-0,9 s dopo |
| memoria trattenuta con tutto l'albero aperto | **102 MB** |
| validatore dell'albero (i cinque `.isc`) | 1,7-2,0 s, 483 problemi (130 errori, 353 avvisi) |

Conseguenza: **l'app apre l'albero intero** all'avvio, non un file per volta. Due secondi e cento mega sono il
prezzo di poter disegnare il contesto, risolvere i punti per nome e validare senza chiedere niente all'AOD.

**Il guscio e la mappa** (F0, carta madre §3-§4): circuito Blazor interattivo in WebView2 a **567 ms**; Leaflet canvas
col sector intero (8 531 oggetti, 128 709 punti) **55 ms** al primo disegno, gesti ~39 ms. Quattro trappole già pagate
(§7).

**Che cosa c'è nel repo, e che cosa no:**
- Il **Bridge** (`src/Vipi.AuroraBridge`) è **Avalonia**, `net8.0`, non WebView2: nessun guscio da riusare. La forma
  però sì — guscio sottile, logica in un `Core` provato senza finestre (`Vipi.AuroraBridge.Core`).
- **Leaflet** sta in `src/Vipi.Ui/wwwroot/vendor/leaflet`. `vipi-aor.js` (788 righe) è la mappa del **sito**
  (settori da `data-poly`, tessere Esri, navigazione enhanced): non serve al Lab, che ha un altro mestiere.
- **Il convertitore di F1** (`CoordinateParser` + `AipGeometryReader`, `Vipi.Application/Coordinates`) legge DMS,
  decimali, KML e il testo AIP con gli archi. `Vipi.Application` pesa poco (Domain + due astrazioni): l'app lo usa
  per il campo «incolla coordinate» (decisione 3 della carta madre: **un motore solo** fra sito e app).
- **Nel motore mancano tre cose per F3**: il **catalogo dei punti** per nome (quello che carica un `.isc`; il
  validatore dell'albero lo ricostruisce per conto suo, `ValidaLAlbero`), la **geometria** di un record per la mappa
  (con i punti per nome risolti, i `DUMMY` e le righe vuote che **spezzano**), e il **controllo che il file sul disco
  non sia cambiato** dall'apertura. `FileSaverOrchestrator.Save` (atomica, `.tmp` + `File.Replace`) c'è già.
- SDK della soluzione: `10.0.401` (`global.json`).

## §2 — L'app

### 2.1 Tre progetti e un progetto di test

| progetto | TFM | che cosa | perché così |
|---|---|---|---|
| `src/Vipi.SectorLab.Core` | `net10.0` | la **sessione** (l'albero aperto), catalogo dei punti, geometria per la mappa, **modifiche in sospeso** e il loro diff, **salvataggio** (conflitto → backup → atomica → rilettura), validazione | tutta la logica, senza finestre: si prova in CI (Ubuntu) come il `Core` del Bridge |
| `src/Vipi.SectorLab.Ui` | `net10.0`, Razor | le pagine e i componenti Blazor, `sectorlab-mappa.js`, il foglio | i componenti si provano con **bUnit** in CI; se stessero nell'eseguibile Windows, il progetto di test dovrebbe essere Windows anche lui e non girerebbe su Ubuntu |
| `src/Vipi.SectorLab` | `net10.0-windows`, `WinExe` | il **guscio**: finestra WinForms + WebView2, server Kestrel su `127.0.0.1`, il cancello | il solo pezzo Windows; compila in CI con `EnableWindowsTargeting` (carta madre §8.4) |
| `tests/Vipi.SectorLab.Tests` | `net10.0` | Core + Ui (bUnit), campioni veri presi da `tests/Vipi.Sectorfile.Tests/Campioni` | un progetto, un numero in `conteggi-attesi.txt` |

Dipendenze: `Core` → `Vipi.Sectorfile` + `Vipi.Application`; `Ui` → `Core`; guscio → `Ui`. **Nessuno** di questi
referenzia `Vipi.Infrastructure`, EF o l'host del sito, e nessun progetto del sito referenzia loro. Del sito cambia
solo la scheda di download (§2.5).

Solo `net10.0` (non il multitarget delle librerie): è un eseguibile **autonomo** (self-contained), il runtime lo porta
con sé. Il motore resta multitarget com'è.

### 2.2 Il giro dell'AOD, schermata per schermata

1. **Apri la cartella** del clone del sector (quella che contiene `SectorFiles/`). L'app controlla che ci siano gli
   `.isc` e `Include/IT`, e si ricorda l'ultima. Legge **tutto** (§1), fotografa per ogni file l'**impronta** (SHA-256
   dei byte) e mostra la barra di stato: file, record, errori e avvisi del validatore.
2. **Sfoglia** (colonna sinistra): l'albero delle cartelle di `Include/IT` e una **ricerca** per nome (fix, VOR,
   procedura, settore, area). Un file aperto elenca i suoi record; un record scelto va nell'**ispettore**.
3. **L'ispettore**: i campi del record, modificabili, e sotto le **righe grezze** del file (quelle vere, coi numeri di
   riga). Per le forme, l'elenco dei vertici.
4. **La mappa** (a destra, Leaflet canvas, **nessuno sfondo a tessere**): le coste e i confini di `GEO/itgeo.geo`
   sempre accesi; il record scelto evidenziato; **il contesto** = gli altri record dello stesso file e, a richiesta, gli
   strati per tipo (settori, aree P/R/D, fix, VOR/NDB, procedure dello stesso ICAO). Clic su una forma = la sceglie.
   Il record in modifica si vede **com'è sul disco e com'è ora**, tratteggiato sopra.
5. **Modifica** (§2.3): cambiare un campo aggiorna subito ispettore e mappa; nel sector non si scrive niente.
6. **Modifiche in sospeso**: il pannello «N modifiche in M file», con il **diff unificato** di ogni file (righe tolte e
   aggiunte, tre di contesto) — è esattamente ciò che uscirà, prodotto dallo scrittore vero e non da una simulazione.
   Si annulla una modifica o tutte.
7. **Salva** (§2.4).
8. **Problemi**: il pannello del validatore (130/353 oggi), filtrabile per file o ICAO; clic = file, riga, record,
   mappa. Dopo una modifica il pannello dice che cosa è **nuovo**: «la tua modifica introduce 1 errore».

### 2.3 Che cosa si modifica in F3

- **I campi di un record**: nome, coordinate, frequenza, pista, tipo… per ogni formato che il motore capisce.
  Una coordinata si scrive in qualunque forma: il campo la legge col convertitore di F1 e il motore la **scrive nella
  forma del file** (puntata, compatta o decimale, F2 slice 2).
- **I vertici di una forma** (`.tfl`, `.pol`, `.geo`, aree P/R/D, `.artcc`/`.hartcc`/`.lartcc`, `.mva`, tracciato
  delle `.sid`, `.vrt`): cambiare, **aggiungere**, **togliere** un vertice dall'elenco; e **«incolla da testo»**: si
  incolla il testo dell'AIP (anche con gli archi) o l'uscita del convertitore del sito, e diventa l'elenco dei vertici.
- **Aggiungere e togliere un record** (un fix nuovo, un'area che sparisce): il record nuovo prende la forma dei vicini.
- Non in F3: **trascinare** i vertici sulla mappa (F9), la geometria fine (F8), i tag `//@` (§9.4).

### 2.4 Il salvataggio

Per ogni file con modifiche, in quest'ordine, e al primo intoppo ci si ferma **prima di scrivere**:
1. **Confini** (carta madre §1): solo `SectorFiles/Include/IT/**`, `SectorFiles/update.ini` e `changelog.md` nella radice del clone (dove stanno davvero: misurato nella slice 2). Il controllo sta nel
   `Core`, non nella pagina: nessun percorso fuori da lì si scrive, qualunque cosa chieda l'interfaccia.
2. **Conflitto**: l'impronta del file sul disco è ancora quella dell'apertura? Se no (un `git pull`, un altro editor,
   un collega sulla stessa cartella) **non si scrive**: si dice quale file, e si offre di ricaricarlo (perdendo le
   modifiche di quel file, che restano leggibili nel diff finché la finestra è aperta).
3. **Backup**: i byte originali in `%LOCALAPPDATA%\VipiSectorLab\backup\<aaaammgg-hhmmss>\<percorso relativo>`,
   **fuori dal clone** (git non li vede, e non finiscono in una PR). Si tengono 30 giorni.
4. **Scrittura atomica** (`FileSaverOrchestrator`: `.tmp` + flush + `File.Replace`), con la codifica e i fine riga
   del file (il motore li ricorda: CRLF sul disco, UTF-8 senza BOM, di solito senza a capo finale).
5. **Rilettura**: i byte sul disco devono essere **quelli del diff**; il file si rilegge, si rivalida, la sua
   impronta nuova diventa quella di riferimento.

Se una modifica introduce **errori nuovi** del validatore, il salvataggio lo dice e chiede conferma; gli errori che
c'erano già non fermano niente (§9.6).

### 2.5 La consegna: release GitHub, link nella vIPI (committente, 22 settembre)

- **Chi costruisce**: un workflow di GitHub Actions su un runner **Windows**, lanciato da un tag `sectorlab-v<versione>`
  (es. `sectorlab-v0.1.0`): pubblica win-x64 autonomo, zippa la cartella, crea la **release** col tag e ci allega
  `VipiSectorLab-<versione>-win-x64.zip` e il suo SHA-256. Niente costruito sul PC di nessuno: lo zip viene dal commit
  del tag. Il prefisso tiene le release del Lab separate da qualunque altra release futura del repo (il Bridge, per
  dire). Oggi il repo di release non ne ha nessuna.
- **Dove si trova**: una scheda «Aurora Sector Lab» nell'hub `/services` della vIPI, **visibile solo da Editor in su**
  (`VipiRole.Editor` e `Admin`), con versione, data, link allo zip e due righe su come si installa. La versione la
  legge il server dall'API pubblica di GitHub (le release del repo, la più recente col prefisso `sectorlab-v`), con
  una cache di qualche ora; se GitHub non risponde, la scheda mostra il link alla pagina delle release invece di
  sparire.
- ⚠️ **Il limite, detto al committente**: il repo `SkyMistery/Digital_vIPI` è **pubblico**, quindi la release e il suo
  zip sono scaricabili da chiunque abbia il link, e compaiono nella pagina «Releases» del repo. Il ruolo nella vIPI
  nasconde **il link**, non **il file**. Va bene perché il codice è già pubblico e l'app non contiene segreti (non
  parla con vIPI, non ha chiavi). Se un giorno l'app ne avesse, la consegna dovrà passare dal server della vIPI.
  ✅ **Confermato dal committente (22 settembre)**, dopo aver visto le alternative: release in un repo privato scaricata
  attraverso la vIPI (token sul server), zip caricato via FTP sul server, o **vIPI privata** — quest'ultima scartata per
  la CI (843 corse in 30 giorni, ~17 minuti fatturati a corsa ≈ 14 000 minuti al mese contro i 2 000-3 000 inclusi nei
  piani privati) e per il submodule di Ivao.It (`docs/guide/integrazione-ivao-it-da-fare.md` §1.1). «Va bene così per
  il tipo di progetto che è»: **tutto resta pubblico**.
- ⚠️ **Gli AOD** vedono la scheda solo se sono Editor o Admin. Il permesso ortogonale «Sectorfile (AOD)» (carta
  madre, decisione 8) non esiste ancora: quando nasce, apre anche questa scheda.
- **Aggiornamenti**: all'avvio l'app fa **una** richiesta anonima all'API pubblica di GitHub (nessun dato mandato,
  nessun token) e, se c'è una versione più nuova, lo dice nella barra con il link alla scheda della vIPI. Senza rete
  tace. L'aggiornamento resta a mano (scarica, sostituisci la cartella): quello **automatico** si decide dopo, se
  serve.

## §3 — La sicurezza del guscio

- Kestrel **solo su `127.0.0.1`**, porta a caso; un **segreto** nell'indirizzo che la WebView2 apre diventa un cookie
  `HttpOnly`/`SameSite=Strict`; senza, **403** (F0). Un altro programma sulla macchina non può parlare con l'app.
- **Istanza unica** (un mutex per cartella del sector): due finestre sulla stessa cartella si scriverebbero addosso.
- La geometria per la mappa **non passa dal circuito SignalR**: la mappa la chiede con una `fetch` a un endpoint locale
  (dietro lo stesso cookie), uno strato alla volta. Il circuito porta solo i comandi e le scelte. Così il limite dei
  32 KB (F0, trappola 3) non si incontra affatto, e il circuito resta leggero.
- Nessuna rete verso fuori in F3, tranne **una** richiesta anonima a GitHub per la versione (§2.5): né vIPI, né
  tessere, né altro.

## §4 — Cosa F3 NON fa

- **Git**: niente rami, commit, PR (F4). L'app scrive i file nel clone; l'AOD fa commit col suo strumento, come oggi.
- **Aurora**: niente ricariche a lotti (F5); l'app non avvia Aurora e non lo aspetta.
- **PDF AIP, checklist AIRAC, `update.ini`/`changelog.md`** (F6): i due file sono nei confini, ma F3 non li scrive.
- **Tag `//@` e initial climb** (F7), **geometria fine** (F8), **trascinare** (F9).
- **Login e permesso «Sectorfile (AOD)»** (carta madre, decisione 8): l'app non parla col sito, quindi non c'è niente
  da proteggere lì. Il permesso entra quando l'app chiederà qualcosa a vIPI.
- Non **corregge** gli errori del sector: li mostra, la correzione è dell'AOD (carta F2 §10).
- Non interpreta `.txt`, `.cpr`, `.datis`, `.clr`… (li mostra come testo, sola lettura).

## §5 — Le slice

| # | cosa | prova |
|---|---|---|
| 0 | I quattro progetti vuoti nella soluzione; il guscio compila su Ubuntu con `EnableWindowsTargeting` | build Release verde, CI verde, conteggi |
| 1 | **Guscio**: finestra, WebView2, Kestrel su `127.0.0.1`, cancello col segreto, le trappole del §7 dal primo giorno | test del cancello (403/200) in CI; a mano: avvio misurato, lanciato **da un collegamento** |
| 2 | **Sessione**: apri la cartella, leggi l'albero, impronte, confini | test sui campioni; tempi sull'albero intero scritti qui |
| 3 | **Catalogo dei punti** per `.isc` e **geometria** dei record (punti per nome risolti, interruzioni) | test: `libb_es_ctr.tfl` (vertici per nome), un `.mva` coi `DUMMY`, `lied.sid` (tratti spezzati) |
| 4 | **Mappa**: strati, `itgeo.geo`, scelta, evidenza | a schermo sull'albero intero; tempi del primo disegno |
| 5 | **Sfoglia** e **ispettore** in lettura, **ricerca** | bUnit; a schermo |
| 6 | **Modifica dei campi** e pannello delle **modifiche in sospeso** col diff; annulla | «sposto un fix» = 1 riga tolta + 1 aggiunta; bUnit del pannello |
| 7 | **Vertici**: cambia, aggiungi, togli, **incolla da testo** (convertitore di F1) | un'area da testo AIP con arco; il diff tocca solo i vertici cambiati |
| 8 | **Aggiungi e togli un record** | il record nuovo esce nella forma dei vicini |
| 9 | **Salvataggio**: conflitto, backup, atomica, rilettura, problemi nuovi | test di ognuno dei cinque passi, compreso il file cambiato sotto i piedi |
| 10 | Pannello dei **problemi** del validatore | i 130/353 dell'albero, con clic che porta a riga e mappa |
| 11 | **Consegna** (§2.5): workflow su tag `sectorlab-v*` (runner Windows, zip + SHA-256 nella release), versione nel titolo, avviso di versione nuova; **scheda nella vIPI** (Editor e Admin) + Guida breve | release di prova scaricata ed avviata da una cartella **fuori** da quella di build; bUnit della scheda (nascosta a DivisionStaff, visibile a Editor); la scheda va nel **prossimo pacchetto** del sito |
| 12 | Chiusura: carta, lavori aperti, memorie | |

Un commit per slice; `dotnet build Vipi.slnx -c Release --no-incremental` verde; test nuovi → `conta-test.sh --scrivi`
nello **stesso** commit; ogni regola con la sua controprova; `gh run list` dopo ogni push. La prova finale del §0 si
fa **sull'albero vero** in una copia (`git archive`), mai sul clone di lavoro di un AOD.

## §6 — Pre-flight, le quattro domande

1. **Modello**: nessun concetto nuovo sul sector — record, righe grezze, basi e punti sono quelli di F2. L'app aggiunge
   **la sessione** (l'albero aperto con le impronte) e **la modifica in sospeso** (record toccato + il suo diff), tutti
   e due nel `Core`.
2. **Dispatch**: lettore e scrittore restano scelti in un posto solo (`Formati`, F2). La geometria per la mappa è un
   **visitatore** dello stesso tipo (`IUsoDelFormato`), non un secondo `switch` sulle estensioni.
3. **Ingressi + verifica**: l'AOD arriva dalla cartella e dalla ricerca; si verifica con bUnit e i test del `Core` in
   CI, e a schermo sull'albero intero a ogni slice che mostra qualcosa (le misure si scrivono qui).
4. **Propagazione**: niente si rimuove né si rinomina. Si **aggiungono** quattro progetti; il motore di F2 cambia solo
   per **aggiunte** (catalogo dei punti, geometria), con i suoi test.

## §7 — Trappole note

- 🔴 Le quattro di F0 (carta madre §3), dal primo giorno: `UseAntiforgery()` (senza, **500 muto**);
  `ContentRootPath = AppContext.BaseDirectory` e `wwwroot` copiato in uscita (lanciato da un collegamento, **404** su
  tutti gli statici); `MaximumReceiveMessageSize` (qui aggirato alla radice, §3); Leaflet servito da noi.
- 🔴 **La cartella dei dati di WebView2**: di base sta accanto all'eseguibile; se l'app è in una cartella senza
  permessi di scrittura, la WebView2 non parte. Va in `%LOCALAPPDATA%\VipiSectorLab\WebView2`.
- ⚠️ **WebView2 Runtime**: c'è su Windows 10/11 aggiornati; se manca, un messaggio chiaro col collegamento, non un crash.
- ⚠️ `File.Replace` può fallire se **Aurora** o un antivirus tiene il file aperto in quel momento.
  `FileSaverOrchestrator` oggi cancella il `.tmp` e rilancia l'eccezione, **senza riprovare**: la slice 9 aggiunge
  pochi tentativi e poi dice quale file. Il `.tmp` nasce **accanto al file, dentro il clone**: se l'app muore a metà
  resta lì e git lo vede come file nuovo — all'apertura l'app cerca i `*.tmp` orfani nei confini e li segnala.
- ⚠️ Le operazioni lunghe (apertura, validatore) **fuori** dal filo del circuito, con lo stato a schermo.
- 🔴 `dotnet test` esce 0 anche rotto: si contano i progetti. Sorgenti **solo con l'editor**, mai heredoc.

## §8 — Definition of done

- [ ] Slice 0-11, un commit ciascuna, build Release verde, suite verde contando i progetti.
- [x] Il criterio del §0 sull'albero vero: sposto un fix → diff di una riga, punto spostato sulla mappa, file sul disco
      = diff, originale nel backup (slice 9).
- [x] Un file cambiato sul disco dopo l'apertura **non** si sovrascrive (slice 9, anche sull'albero vero).
- [x] Nessun percorso fuori dai confini si scrive (test di `Confini`, slice 2; il salvataggio passa da lì, slice 9).
- [ ] L'app parte da una cartella qualunque, lanciata da un collegamento, su un PC col solo WebView2 Runtime.
- [ ] Del sito cambia solo la scheda di download; nessun dato di vIPI toccato, nessuna migrazione.
- [ ] Una release `sectorlab-v*` scaricata dalla scheda della vIPI parte su un PC col solo WebView2 Runtime.

**Slice 0 — i quattro progetti** (22 settembre). `Vipi.SectorLab.Core` (→ `Vipi.Sectorfile`, `Vipi.Application`),
`Vipi.SectorLab.Ui` (Razor, → Core), `Vipi.SectorLab` (`net10.0-windows`, `WinExe`, WinForms + WebView2, → Ui; un
`Main` vuoto), `tests/Vipi.SectorLab.Tests` (xunit + bUnit). Build Release verde con zero avvisi; 9 test.
- 🔴 **Il pacchetto WebView2 aggiunge sempre la sua DLL per WPF** (`build/Common.targets`, per ogni app .NET 5+): con
  WinForms senza WPF, avviso MSB3277 su `WindowsBase`. Tolta con un target prima di `ResolveAssemblyReferences` (un
  `Remove` statico nel csproj arriverebbe prima dell'`Include` del pacchetto).
- `NessunaDipendenzaDelSitoTests` guarda la **cartella dei test**, non `GetReferencedAssemblies`: con progetti ancora
  senza codice quello è vuoto e il test passerebbe a vuoto. Controprova: `Vipi.Infrastructure` collegato per un momento
  al Core → 2 rossi (Infrastructure ed EF).
- 🔴 **Il commit `74154183` della slice 0 conteneva SOLO questa carta**: il `git add` dei progetti era stato lanciato con
  `-n` (prova a secco) e nessuno ha guardato che cosa entrava nel commit. La CI verde di quel commit non provava niente
  sul guscio, e al committente era stato detto il contrario. I progetti della slice 0 entrano col commit della slice 1.
  Da qui: prima di ogni push, `git show --stat HEAD`.

**Slice 1 — il guscio** (22 settembre). `Vipi.SectorLab.Ui/Server`: `ServerDelLab` (Kestrel su `127.0.0.1:0`, radice dei
contenuti = cartella dell'eseguibile, `UseStaticWebAssets`, `UseAntiforgery`, `MapStaticAssets`, tetto SignalR a 4 MB per
il testo AIP incollato) e `Cancello` (segreto di 32 byte per avvio; `?k=` → cookie `HttpOnly`/`SameSite=Strict` e
**rimando** all'indirizzo senza segreto; col cookie si passa ovunque, **anche `/_blazor`** — il prototipo di F0 lo
lasciava aperto; confronto a tempo costante). Pagina iniziale che, viva, lo dice alla finestra (`sectorlab.pronto`).
Guscio: `Finestra` (WebView2, dati in `%LOCALAPPDATA%\VipiSectorLab\WebView2`, strumenti di sviluppo solo in Debug),
controllo del **WebView2 Runtime** con messaggio e indirizzo se manca, **diario d'avvio**
(`%LOCALAPPDATA%\VipiSectorLab\avvio.txt`: tempi, errori JS della pagina, ogni risposta ≥ 400), versione del Lab
`0.1.0` nel titolo, e **`--autoprova`**: parte, aspetta il circuito vivo, esce con 0 (o 1 dopo 60 s, 2 se la WebView2
non parte, 3 se manca il runtime).

Due trappole nuove, tutte e due «la pagina arriva a 200 e il circuito non parte mai, senza errori a schermo» — le ha
trovate l'autoprova, e ora il diario scrive i 404:
- 🔴 **Da .NET 10 `blazor.web.js` entra in un progetto web solo se il progetto ha dei `.razor` suoi**
  (`RequiresAspNetWebAssets`, `Microsoft.NET.Sdk.Web.ProjectSystem.targets`): i nostri stanno in `Vipi.SectorLab.Ui`.
- 🔴 **…e solo se `OutputType` è `Exe`** (target `_AddBlazorFrameworkStaticWebAssets` del pacchetto
  `Microsoft.AspNetCore.App.Internal.Assets`): un'app a finestra è `WinExe`. Il csproj del guscio fa vedere `Exe` a quel
  target soltanto, mettendosi in **testa** alla catena `ResolveStaticWebAssetsInputsDependsOn` — con `BeforeTargets` sul
  target non funziona, perché MSBuild ne valuta la condizione **prima** dei `BeforeTargets` (provato). Misurato poi che
  compilatore e apphost leggono il tipo prima, e l'eseguibile resta a finestra anche senza rimettere `WinExe`.
- `namespace Vipi.SectorLab` + `Vipi.Application` referenziato: `Application.Run` si risolve nel namespace, non in
  WinForms (e un alias globale non basta). Alias `WinForms` dentro il namespace.
- Il pacchetto WebView2 e la sua DLL WPF: già nella slice 0.

Misure, `--autoprova` lanciata da `C:\Users\cgran` (non dalla cartella dell'eseguibile): server in ascolto 0,2-0,3 s,
WebView2 pronta 0,4-0,7 s, **circuito vivo 0,7-1,0 s**, nessun 404. Lo stesso dalla versione **pubblicata** (win-x64
autonoma, 151 MB in 392 file, cartella fuori dal repo): 0,95 s.

Test (28, in CI): `ServerDelLabTests` con un Kestrel vero (403 senza segreto anche su asset e `/_blazor/negotiate`,
segreto sbagliato, cookie e rimando, pagina 200, asset 200, radice dei contenuti, tetto SignalR) e `GuscioTests`, che
legge il guscio compilato (il progetto di test lo fa compilare senza referenziarlo): `blazor.web.js` nel manifesto, e
sottosistema PE a finestra. Il progetto di test è ora **SDK web** (MapStaticAssets vuole il manifesto dell'assieme
d'ingresso). Controprove: tolto `UseAntiforgery` → 1 rosso (la pagina 500); tolta la testa della catena → 2 rossi;
`Exe` a tutto il progetto senza inferenza WinForms → 1 rosso (console).

**Slice 2 — la sessione** (22 settembre). `Vipi.SectorLab.Core/Sessione`:
- `CartellaDelSector.Riconosci`: dalla cartella scelta **risale** fino a quella che contiene `SectorFiles/` (va bene
  la radice, ma anche `SectorFiles`, `IT` o una sua sottocartella), e se non è un sector dice perché (cartella
  inesistente, niente `SectorFiles`, niente `Include/IT`, niente `.isc`). Relativi alla radice, barre dritte.
- `Confini.Scrivibile`/`Pretendi`: `SectorFiles/Include/IT/**`, `SectorFiles/update.ini`, `changelog.md`. 🔴 La carta
  madre diceva `update.ini` senza cartella: sul master (`edccf2b`, 22 settembre) sta in `SectorFiles/`, mentre
  `changelog.md` sta nella radice. Il percorso si **normalizza prima** di confrontarlo (`IT/../../../Aurora.exe`), e
  la cartella si confronta col separatore in coda (`IT-vecchio` non è `IT`).
- `SessioneAperta.Apri`/`ApriAsync`: tutti i file di `Include/IT` letti dal motore con le basi fissate
  (`FileLetto<T>`, col suo scrittore), quelli che non interpreta tenuti come testo (`FileNonInterpretato`), più gli
  `.isc`, `update.ini` e `changelog.md` come testo; ognuno con l'**impronta** SHA-256, presa **prima** e ricontrollata
  **dopo** la lettura (il lettore apre il file per conto suo: un `git pull` a metà darebbe un'impronta e dei record
  diversi — si riprova tre volte). Lettura **in parallelo**, una raccolta d'avvisi per file (`RaccoltaDiAvvisi`: quella
  di A dipendeva da `ILogger` e non era stata portata). Chiavi indifferenti alle maiuscole. `CambiatiSulDisco()`
  (cambiati o spariti) e i `*.tmp` rimasti nei confini (`TmpOrfani`: si segnalano, non si aprono e non si toccano).

Misure sull'albero di `7e761aa` (scratchpad `f3-sessione`): 754 file (701 letti), 118 315 record, **1,3 s alla
prima apertura** (JIT compreso), **0,35-0,48 s dalla seconda**, 102 MB; `CambiatiSulDisco` sull'albero intero 28 ms.
Righe: 271 392 contro 271 388 sul disco — i 4 file **vuoti** (`TERM.fix`, `FRA.fix`, `ENR.fix`, `ACC/test.artcc`) il
modello di A li tiene come una riga vuota; si sa, non cambia niente.

Test: 38 nuovi (66), su un **albero di prova** fatto coi campioni veri del motore più `.isc`, `update.ini`,
`changelog.md` e un `Aurora.exe` finto (`AlberoDiProva`). Controprove: senza normalizzazione → 4 rossi; senza il
separatore in coda → 1; coi `.tmp` letti come dati → 1. 🔴 La prima controprova ha trovato un buco **nel test**: i
casi con `../` erano scritti con `/` e su Windows restavano fuori anche senza normalizzare, perché `/` e `\` non
combaciavano. Aggiunto il caso coi separatori del sistema, che senza normalizzazione cade.

**Slice 3a — il catalogo dei punti** (22 settembre). Un vertice scritto per nome (`AMSOR;AMSOR;`) la mappa lo disegna
solo se sa dov'è, e **Aurora lo cerca nei cataloghi che quel master carica**: `ITALY.isc` e `LIBB.isc` rispondono in
modo diverso. Due conoscenze servivano, e **stavano tutt'e due dentro il validatore**: si sono estratte nel motore,
non copiate (una seconda risposta alla stessa domanda si separa al primo cambiamento).
- Nel motore: `IO/CarichiDegliIsc.cs` (`CaricoDiUnIsc`: il master, la cartella dei dati, i file caricati e i citati
  assenti) — le tre vie per cui un file entra: `F;`, il codice di uno scalo di `[AIRPORT]`, un file nominato da un
  `.frq` caricato. Gli ICAO degli scali li chiede a chi lo chiama (`Func<string, IEnumerable<string>>`), così il
  motore non decide qui come si legge un file: il validatore passa il suo lettore, l'app i record che ha già in mano.
  E `Models/Catalog/Cataloghi.cs` (`Dichiarato`): quali record sono un punto chiamabile per nome (fix, VOR, NDB,
  scalo, VRP col nome e col codice). `ValidaLAlbero` ora usa tutt'e due.
- Nell'app: `CatalogoDeiPunti.PerOgniIsc(sessione)` — un catalogo per master, costruito sui record **già letti**
  (l'albero non si rilegge), nomi indifferenti alle maiuscole come in Aurora.

Prova che l'estrazione non cambia niente: 419 test del motore verdi, e lo strumento sull'albero dà gli **stessi
numeri** (701/701, opache 93, validatore **130 errori, 353 avvisi**, concordanza 0 discordi). Catalogo sull'albero
vero: 5 master in **39 ms**, `ITALY.isc` 4 465 punti da 707 file, `LIBB.isc` 3 844 da 244; non risolve `KPT`,
`MAFRE`, `ALPHA SUOTH` — gli stessi che il validatore segnala come errori veri del sector.

Test: +3 nel motore (`CarichiDegliIscTests`: le tre vie, i citati assenti con la loro riga), +7 nell'app; l'albero di
prova ha ora `.isc` veri (intestazione di sei righe, `F;`), uno grande e uno che carica solo `APT.fix`. Controprove:
catalogo costruito su tutti i file invece che sui caricati → 2 rossi; catalogo che guarda le maiuscole → 1 rosso.

**Slice 3b — la geometria** (22 settembre). `Core/Mappa`: `FormaDellaMappa` (file, indice del record, punto/linea/area,
etichetta, **tratti** di coordinate, nomi non risolti) e `Geometria.DelFile(file, catalogo)`, un tipo di record per
volta. I punti per nome li risolve il motore (`Punto.TryRisolvi`, che con due nomi diversi prende la latitudine dal
primo e la longitudine dal secondo, come Aurora): il catalogo è un `IFixResolver`. I segmenti dei `.geo` e delle aree
P/R/D si **cuciono** in polilinee finché la fine di uno è l'inizio del successivo e il nome dell'area non cambia; i
`DUMMY` dei bordi e le righe vuote dei tracciati SID **spezzano** il tratto.

Sull'albero vero: **21 667 forme, 244 227 punti, in 42 ms** (7 747 punti, 11 395 linee, 2 525 aree) — `.geo` 99 875
punti, `.pol` 38 815, `.str` 35 759, `.tfl` 24 480. Nomi citati e non risolti: **36 citazioni, 16 nomi** — i 14 del
validatore (`MAFRE`, `PZN`, `HITAC35/36/06`, `ALPHA SUOTH`, `BV-PAJOT`, `BV-IRDAG`, `MG763`, `EDOXI`, `KPT`,
`PL-BRAVO`, `MC904`, `MC734`) più le due coordinate rotte di `lipp.hartcc`, lette come nomi.

Due difetti, tutt'e due trovati dalla misura sull'albero e non dai test:
- 🔴 **Una forma che non si disegna spariva in silenzio, e con lei il motivo.** `LIMN;35;IAF HITAC35` di `limn.str`
  cita un fix che non esiste: la forma restava senza punti e veniva scartata, così il nome mancante non lo sapeva
  nessuno. Ora un record che non si disegna **per un nome irrisolto** resta, con zero tratti e il nome. (Prima della
  correzione l'albero dava 13 citazioni irrisolte, dopo 36.) Stessa cosa per un'attesa senza il suo fix: prima
  finiva a `0,0`, nel golfo di Guinea.
- 🔴 **Nel motore: i `.rw` dei quattro FIR scrivono `///////PISTE`**, e il lettore di A guardava solo `//PISTE`. Le
  **187 righe di pista** di `libb.rw`, `limm.rw`, `lipp.rw` e `lirr.rw` restavano righe grezze: round-trip perfetto,
  piste invisibili al modello, **nessun avviso** — la trappola del §7 della carta F2 in un posto che le misure di F2
  non toccavano (le righe grezze senza avviso non contano fra le «opache»). Corretto in `RwParser`; sull'albero
  «una modifica per record» sale da 115 382 a **115 569**, esattamente quelle righe, e il resto non cambia
  (701/701, opache 93, tutto toccato 0, validatore 130/353).

Test: +11 (`GeometriaTests`, 83 nell'app) e +2 nel motore (424). Controprove: forme vuote scartate → 1 rosso; senza
spezzare i tratti → 1 rosso; `RwParser` com'era → 2 rossi. 🔴 La seconda controprova ha trovato un altro **test
debole**: il caso della SID guardava il campione vero e sarebbe passato anche senza la regola, perché lì il tratto si
spezza pure per altri motivi. Riscritto con un tracciato costruito apposta (tre punti, riga vuota, due punti).

**Slice 4 — la mappa** (22 settembre). Leaflet in canvas, servito da noi (`wwwroot/vendor/leaflet`, 1.9.4, la stessa
copia del sito), **nessuno sfondo a tessere**: il mare è il fondo della pagina, le coste le disegna `GEO/itgeo.geo`.
- `Core/Mappa/StratiDellaMappa`: **13 strati**, e in quale finisce ogni file. La regola sta nel `Core` e non nella
  pagina perché l'elenco delle caselle e l'endpoint devono dare la **stessa** risposta, e perché è conoscenza sul
  sector: `GEO/itgeo.geo` è lo sfondo di tutti, gli altri `.geo` sono disegni di un aeroporto; un `.restrict` è
  un'area P/R/D anche se il lettore è lo stesso del `.geo`. Un file senza strato (`.frq`, `.atis`, gli `.isc`: 65
  sull'albero) non è un buco — su una mappa non ci sta.
- `Ui/Servizi/SessioneDelLab`: la cartella aperta, **singleton** (l'albero costa 102 MB: un Ctrl+F5 non lo rilegge),
  con gli strati accesi, il record scelto, l'ultima cartella ricordata in `%LOCALAPPDATA%\VipiSectorLab`, e il
  **master** con cui si risolvono i nomi (cambiandolo la geometria si rifà: cambiano i punti risolti).
- `Ui/Server/MappaDelLab`: `/mappa/strati` (i numeri, senza coordinate) e `/mappa/strato/<id>`, dietro lo stesso
  cancello — **403** senza cookie, come tutto il resto. JSON corto: chiavi di una lettera, coordinate a **cinque**
  decimali (≈ 1 m), i punti di un tratto in fila a coppie. Sessione chiusa = elenco vuoto, non un errore.
- La pagina: barra coi numeri, caselle degli strati col loro colore, riquadro «Scelto» col file, il record, i punti e
  — se ce ne sono — **i nomi che il master non conosce**. Clic su una forma = la sceglie; la scelta che viene dalla
  mappa **non** riquadra (chi ha appena cliccato non vuole la mappa che salta sotto il dito).

Misure sull'albero vero (`7e761aa`): strati costruiti in **48 ms** (21 854 forme, 244 601 punti — 187 forme più della
slice 3b: sono le piste che `RwParser` ora vede), JSON **5,8 MB** in tutto, il più grosso `geo` 2,2 MB. A schermo,
lanciando `VipiSectorLab.exe --autoprova --cartella <albero> --tutti-gli-strati`: **13 strati, 21 854 forme disegnate
in 436 ms**, app pronta in 2,7 s (apertura della cartella compresa, 876 ms). Il singolo strato: `geo` 8 832 forme in
113 ms, `procedure` 1 514 in 66 ms, `terra` 4 502 in 53 ms, `sfondo` 129 in 45 ms.
- 🔴 **Un componente senza parametri Blazor non lo ridisegna** quando il padre cambia: la mappa restava ferma su quel
  che aveva al primo giro, e accendere una casella non arrivava al JavaScript. `Mappa.razor` si iscrive **da sé** a
  `SessioneDelLab.Cambiata` e sincronizza fuori dal ciclo di render. L'hanno trovato tre test bUnit rossi, non lo
  schermo.
- **Nuovi argomenti del guscio**: `--cartella <percorso>` apre subito quella cartella (un collegamento per l'AOD, e la
  misura qui sopra), `--tutti-gli-strati` li accende tutti; con `--autoprova` la prova aspetta la riga «mappa
  disegnata» invece di «pronto». Il tempo di ogni strato finisce nel **diario d'avvio**: è la prima domanda da fare a
  un AOD che dirà «la mappa è lenta».

Test: +37 (120 nell'app): `StratiDellaMappaTests` (la classificazione, e che nessuna forma si perda per strada),
`MappaDelLabTests` (Kestrel vero: 403 senza cookie, 404 per uno strato inventato, il formato, i cinque decimali, il
nome irrisolto che viaggia con la forma), `PaginaConLaMappaTests` (bUnit: apertura, caselle, scelta, «cambia
cartella»). Controprove: tolto l'arrotondamento → 1 rosso; tolta la regola di `itgeo.geo` → 2 rossi; senza
l'iscrizione a `Cambiata` → 3 rossi (è così che è saltato fuori il difetto qui sopra).

**Slice 5 — sfoglia, cerca, ispeziona** (22 settembre). Tutto in **lettura**: i campi si modificano nella slice 6.
- `Core/Sessione/AlberoDaSfogliare`: l'albero delle cartelle di `Include/IT` costruito **dai file già aperti**, non
  rileggendo il disco — quel che si sfoglia è quel che si ha in mano. Gli `.isc`, `update.ini` e `changelog.md` non
  sono dati: stanno in `FuoriDaiDati`.
- `Core/Sessione/Ricerca`: cerca fra le **forme della mappa**, che hanno già l'etichetta che si vede a schermo e
  l'aggancio (file + record). Ordine: chi si chiama **esattamente** così, poi chi comincia così, poi chi lo contiene
  — chi cerca `LIRF` vuole LIRF. ⚠️ In questa slice i record **senza geometria** (i `.frq`, gli ATIS) non si trovano
  per nome: ci si arriva sfogliando. Quando serviranno, l'indice si allarga lì, non in una seconda ricerca.
- `Core/Ispezione/Ispettore`: i campi si leggono per **riflessione** sul modello del motore, non da una tabella per
  tipo: i tipi di record sono venticinque e cambiano con le slice: una tabella a mano sarebbe una seconda verità che
  invecchia in silenzio. Quel che si scrive a mano è la traduzione dei **valori** (coordinate in DMS puntato, punti
  per nome, elenchi contati). Le **righe grezze** vengono dai `Chunks`, contate dall'inizio del file: il numero è
  quello **vero del disco**, lo stesso che cita il validatore, con tre righe di contesto sopra e sotto.
- La pagina è a tre colonne più la mappa: sfoglia (ricerca, albero, record del file aperto, con filtro e tetto di
  200), ispettore (campi e righe), strati. Scegliere una forma sulla mappa **apre il suo file** nell'albero.

Misure sull'albero vero: albero da sfogliare in **6 ms** (15 cartelle in cima, 749 file), ricerca **0-11 ms** (`LIRF`
60 risultati, `AMSOR` 1, `R47` 1), etichette del file più grosso (`itgeo.geo`, 13 560 record) in **6 ms**, scheda di
un record in 2-4 ms.
- 🔴 **Due etichette sbagliate, trovate dalla misura e non dai test**: gli elenchi dicevano «AtcPosition» 201 volte
  (i `.frq`: si chiamano col loro `Code`, `LIMM_FSS`) e «Line» 13 560 volte (i segmenti dei `.geo`: senza nome resta
  il **colore**, `COAST`). Aggiunti `Code` e `Color` in fondo ai nomi che fanno da etichetta. Dove il colore è vuoto
  — lecito (F2 slice 5) — resta il nome del tipo accanto al numero del record: non si inventa un nome.
- 🔴 **`bUnit` 1.40 con AngleSharp 1.6** (la versione messa per la mXSS): l'indicizzatore `FindAll(...)[0]` lancia
  `MissingMethodException`. Si usa `.First()`.

Test: +28 (148): `SfogliaERicercaTests`, `IspettoreTests`, `SfogliaEIspettoreTests` (bUnit). Controprove: senza
l'ordine della ricerca → 1 rosso; righe grezze non contate nella numerazione → 1 rosso; la scelta che non apre il
file → 1 rosso. 🔴 La prima controprova ha trovato **una prova che non distingueva**: i nomi del test (`AAA`,
`AAABIS`, `PRIMAAAA`) erano in ordine alfabetico *e* in ordine di punteggio, così passava anche senza la regola.
Riscritta con `AMMM`, `MMMX`, `MMM`, dove i due ordini sono diversi.

**Slice 6 — modifica dei campi e modifiche in sospeso** (22 settembre). La prima slice che cambia qualcosa: **in
memoria**. Sul disco non si scrive niente fino alla slice 9.
- `Core/Modifiche/ModificheInSospeso`: cambia un campo di un record (per riflessione, col tipo giusto: testo,
  numeri, `sì`/`no`, enum, **coordinate** — che si scrivono in due pezzi e in qualunque forma, e il file le
  riscriverà nella **sua**, F2 slice 2). Una modifica rifiutata dice **perché** e non tocca niente.
- 🔴 Il «prima» di una modifica è sempre **quello dell'apertura**, anche dopo dieci modifiche di fila sullo stesso
  campo: annullare deve riportare a un valore che sul disco è esistito davvero. E rimettere a mano il valore di
  partenza **non è una modifica**: sparisce dal pannello, come se non fosse successo niente.
- `Core/Modifiche/Diff`: l'algoritmo di **Myers**, non un confronto riga per riga: una modifica deve uscire come una
  riga tolta e una aggiunta, non come «da qui in giù è tutto diverso». Costa O((N+M)·D) e D qui è due: su un file di
  13 560 righe è lavoro da niente. Oltre 2 000 differenze si ferma e lo dice.
- Le righe le produce lo **scrittore vero** (`FileSaverOrchestrator.Righe`, aggiunto al motore): il diff che si vede
  è quello che uscirà, non una simulazione che potrebbe differire.
- Nell'ispettore i campi semplici diventano caselle (gli elenchi, cioè i vertici, sono la slice 7); il pannello dice
  «N modifiche in M file», il diff di ciascuno, e si annulla una per una o tutte. Dopo una modifica la **geometria
  del solo file toccato** si rifà, e la mappa riprende quello strato: il punto si vede dov'è adesso.

Misura sull'albero vero, **una modifica per file**: in ognuno dei 348 file che hanno un campo coordinata scrivibile
si sposta un record → **348 file su 348 con il diff di UNA riga tolta e UNA aggiunta**, zero fuori misura, zero in
blocco; il diff costa **0,8 ms per file** (265 ms per tutti e 348). Poi «annulla tutto»: 11 ms, e i 754 file tornano
**identici** all'originale, riga per riga.
- ℹ️ Una controprova non è diventata rossa, e dice una cosa vera: dando per toccati **tutti** i record di un file il
  diff non cambia di una riga. È la fusione di F2 (§9.5) che tiene — «tutto toccato = 0 righe cambiate» era già la
  sua misura. L'insieme dei record toccati serve all'intenzione e al costo, non ai byte.

Test: +18 (166): `ModificheInSospesoTests` (il criterio del §0: sposto un fix → una riga tolta e una aggiunta) e
`ModificaDeiCampiTests` (bUnit: casella, pannello, diff a schermo, annulla, mappa che riprende). Controprove: il
«prima» preso dall'ultimo valore invece che dall'apertura → 4 rossi; senza ripulire i record tornati puliti → 2
rossi; senza rifare la geometria dopo la modifica → 1 rosso.

**Slice 7 — i vertici** (22 settembre). Cambia, aggiungi, togli, e **«incolla da testo»** col convertitore di F1.
- Un vertice si scrive per coordinate, o **per nome** dove il file lo sa riscrivere (i `.tfl`: `AMSOR;AMSOR;`). Dove
  l'elenco è di sole coordinate (`.pol`, `.lairway`) un nome si rifiuta **prima** di toccare niente: scriverlo lì
  vorrebbe dire perderlo al salvataggio.
- L'ultimo vertice non si toglie: una forma senza punti non si disegna. Togliere il **record** è la slice 8.
- «Incolla da testo»: il testo dell'AIP (archi compresi), l'uscita del convertitore del sito, o delle coordinate una
  per riga — lo legge `CoordinateParser` di F1, lo stesso della vIPI. Un testo con **più di un'area** si rifiuta
  dicendo quante sono: quale sarebbe questa forma?
- 🔴 Annullare i vertici non rifà i gesti al contrario: rimette l'**elenco dell'apertura**, fotografato al primo
  gesto. Dieci gesti sono una modifica sola nel pannello, e si annullano insieme.

Misura sull'albero vero: **154 file su 154** con un elenco piatto di vertici danno il diff di **una riga tolta e una
aggiunta** spostando il primo vertice, 0,6 ms per file; «annulla tutto» rimette i 754 file **identici**. L'esempio
AIP del committente (arco da 17 NM, carta F1 §0) incollato su `lsas.tfl`: 1 023 → **89 vertici in 15 ms**, diff
−1 023 +89 in un pezzo solo.
- 🔴 **Un difetto vero, trovato dalla misura e non dai test**: `Punto` ha una conversione **implicita** da
  `Coordinate`, e nel ternario che sceglieva il tipo del vertice il tipo comune diventava `Punto` — anche il ramo
  «questo elenco tiene Coordinate» usciva come `Punto`, e ogni `.pol` e ogni `.lairway` cadeva con un'eccezione a
  tempo d'esecuzione. I test non l'avevano visto perché toccavano i `.tfl`, che i `Punto` li tengono davvero. Due
  `(object)` e un test sul `.pol`.
- ⚠️ **Limite dichiarato**: si toccano gli elenchi **piatti** di vertici (`.tfl`, `.pol`, `.mva`, `.vrt`,
  `.lairway`/`.hairway`, `.str` a punti). I tracciati a più tratti — le zone dei `.str` con i segmenti, il
  `Track` delle `.sid` — tengono i punti **dentro** i loro segmenti: là dentro la slice 7 non entra. Sono 547 file
  dei 701 letti (la gran parte sono `.geo`, che sono segmenti indipendenti e si modificano come record). ▶ Da
  decidere col committente se farne una slice a parte prima della 8.

Test: +19 (185): `VerticiTests` (i gesti, il nome rifiutato dove non si scrive, l'arco AIP, le due aree) e
`VerticiAschermoTests` (bUnit: elenco, + e −, incolla, rifiuto). Controprove: fotografia dell'elenco presa a ogni
gesto invece che una volta → 3 rossi; due aree accettate → 1 rosso; nomi accettati ovunque → 1 rosso.

**Slice 7-bis — i tracciati a più tratti** (23 settembre, chiesta dal committente subito dopo la 7). Quel che la
slice 7 non toccava: il `Track` delle `.sid` e le zone dei `.str`.
- `Core/Modifiche/ElenchiDiVertici`: «i punti di questa forma» nel sector è scritto in **tre** modi — un elenco di
  `Coordinate` (`.pol`, `.lairway`), un elenco di `Punto` che ammette anche i nomi (`.tfl`, `.mva`, `.vrt`), e un
  elenco di **involucri** che portano il punto più altro: `PuntoDelTracciato` delle `.sid` (etichetta e «nuovo
  tratto») e i **segmenti** delle zone `.str`, che sono elenchi dentro un elenco. Adesso i gesti non sanno quale
  dei tre: leggono e scrivono un punto, e **quel che l'involucro porta in più resta**.
- Una zona a più tratti ha un elenco **per tratto** (`Segments[2].Points`, a schermo «Tratto 3»): si modifica un
  tratto per volta, e gli altri non si muovono. Due tratti toccati sono **due** modifiche nel pannello.
- 🔴 Spostando un punto di una SID l'**etichetta** e il segno di «nuovo tratto» **non si perdono**: se si
  perdessero, il diff toccherebbe righe che nessuno ha chiesto di cambiare.

Misura sull'albero vero: **4 468 record** hanno vertici, per **5 892 elenchi** (134 record a più tratti, 1 304
tracciati di SID), trovati in 87 ms. «Un vertice spostato per file» passa da 154 a **244 file, tutti col diff di una
riga tolta e una aggiunta** (2,2 ms per file), e «annulla tutto» rimette i 754 file identici.
- 🔴 Un difetto preso di nuovo **dalla misura**: nel ciclo interno mancava la guardia sugli **indicizzatori** che il
  ciclo esterno aveva — un `this[int]` chiesto senza argomenti fa «Parameter count mismatch», e l'albero vero ne ha.

Test: +11 (196): `TracciatiATrattiTests` (SID e zone `.str`) e due bUnit. Controprove: involucro non conservato → 1
rosso; elenchi annidati non cercati → 8 rossi.

**Slice 8 — aggiungere e togliere un record** (23 settembre). Cambia **quanti** record ha un file, non il contenuto
di uno: nel motore `IO/RecordNuovo` (aggiungi, togli) e nell'app la voce «struttura» del pannello.
- Il record nuovo **nasce copiando il vicino** (`CopiaDelRecord`): ha già i campi di struttura che in quel file
  valgono per tutti (l'ICAO, la pista, il tipo di settore, il colore) e le sue righe escono **nella forma dei
  vicini** — puntata, compatta o decimale (F2 slice 2), perché passano dalla stessa strada di una riga aggiunta a un
  record toccato. Poi l'AOD cambia quel che deve: è più corto che chiedergli dieci campi vuoti, e non si sbaglia su
  ciò che non tocca. 🔴 L'elenco dei vertici del nuovo è **un altro oggetto**: se i due se lo dividessero, spostare
  un punto del nuovo sposterebbe anche il vicino.
- 🔴 **Togliere un record lascia il commento che ha sopra.** Nel sector un commento prima di un record è quasi
  sempre l'intestazione di una **sezione** (`//////COAST`, `//fence`), non la descrizione di quella riga: portarselo
  via togliendo il primo record del blocco cancellerebbe l'intestazione di tutti gli altri. L'ha mostrato la misura
  — in `itgeo.geo` un taglio da una riga ne faceva sparire tre. Un commento davvero legato al record lo toglie
  l'AOD a mano, e si vede nel diff.
- L'**ultimo** record di un file non si toglie: un file senza record non è più quel file. Si cancella il file.
- 🔴 I numeri dei record **scorrono**: le modifiche in sospeso sugli altri record vengono rinumerate, o punterebbero
  al record sbagliato; quelle del record tolto spariscono con lui. La struttura è **una voce sola per file** e si
  annulla per ultima — prima tornano i valori dei campi, poi i record.
- Il «prima» del diff viene dalla **fotografia della struttura**: con un record in più, il file di adesso
  confrontato con sé stesso direbbe che non è cambiato niente.

Misura sull'albero vero: un record aggiunto in ognuno dei **695** file interpretati → **695 su 695** col diff giusto
(zero righe tolte, tante aggiunte quante ne cresce il file), di cui **422 con una riga sola**; un record tolto in
ognuno dei **637** file con più di un record → **637 su 637**. «Annulla tutto» rimette i 754 file identici, in tutti
e due i giri.

Test: +17 (213): `RecordAggiuntiEToltiTests` e `RecordAschermoTests` (bUnit: i due tasti, il pannello, il record
nuovo modificato subito). Controprove: commenti portati via col record → 1 rosso; indici non rinumerati dopo un
taglio → 1 rosso; elenchi non copiati nel record nuovo → 1 rosso.

**Slice 9 — il salvataggio** (23 settembre). La prima cosa del Lab che scrive nel clone: `Core/Disco/Salvataggio`,
i cinque passi del §2.4 in quell'ordine, e nel pannello delle modifiche il tasto **Salva** con l'esito.
- **Passi 1 e 2 su TUTTI i file prima di scriverne uno**: un conflitto nel quinto file non lascia i primi quattro
  salvati. I byte letti per il confronto dell'impronta sono gli stessi che vanno nel backup (niente seconda lettura
  fra controllo e copia). Il controllo si **rifà** al clic su «Salva lo stesso»: fra le due cose può esserci un
  `git pull`.
- **Conflitto**: non si scrive niente; il pannello dice quale file e offre **«Ricarica dal disco»** — il file si
  rilegge, le sue modifiche si lasciano andare (`ModificheInSospeso.Dimentica`, che NON rimette i valori: i record
  sono oggetti nuovi) e il loro **diff resta leggibile** sotto, finché la finestra è aperta.
- **Backup** in `%LOCALAPPDATA%\VipiSectorLab\backup\<aaaammgg-hhmmss>\<percorso relativo>`, una cartella per
  salvataggio (due nello stesso secondo: `-2`). I vecchi di 30 giorni si tolgono **dal nome** della cartella, non
  dalla data del disco; una cartella dal nome che non è di un backup non si tocca.
- **Scrittura atomica** dal motore (`FileSaverOrchestrator.ScriviAtomico`, `.tmp` + flush + `File.Replace`) coi byte
  di `FileSaverOrchestrator.Byte` — gli stessi che producono il diff, aggiunto al motore perché i byte si validano
  PRIMA e si confrontano col disco DOPO. **Tre tentativi** a 250 ms (Aurora, un antivirus); poi si dice QUALE file, e
  che sul disco è rimasto quello di prima. Un guasto a metà lascia salvati i file già scritti, gli altri in sospeso.
- **Rilettura**: i byte sul disco devono essere esattamente quelli scritti; il file si rilegge
  (`SessioneAperta.Rileggi`), la sua impronta diventa quella di riferimento, si rivalida. Poi cataloghi, strati e
  albero si rifanno: un fix spostato deve spostare anche le forme che lo citano per nome.
- **Errori nuovi (§9.6)**: il file coi byte nuovi si valida da una copia temporanea, e si confronta col file di prima
  per **regola e testo della riga**, non per numero (un record aggiunto sopra fa scorrere tutti gli errori vecchi),
  **contandoli** (un record copiato da un vicino che ha un errore porta un errore in più). Gli errori chiedono
  conferma, gli avvisi si dicono e basta.
- 🔴 **Un difetto della slice 8 trovato dalla misura di questa**: un record aggiunto in ognuno dei 695 file, salvato e
  riletto da capo → solo **632** tornavano col record in più. Nei `.mva`, `.lartcc`, `.hartcc` un record è un
  **blocco chiuso dalla riga vuota**: la copia accostata al vicino, riletta, era un pezzo di lui (il diff era giusto, il
  file no). Ora `AggiungiComeIlVicino` fa una **rilettura di prova** e, se il file non ha mai due record accostati,
  mette fra i due la riga vuota che separa anche gli altri (`RecordNuovo.Aggiungi(…, separatore)`). Dove i record si
  accostano e li separa la **chiave** (`.vrt` per numero, aerovie per nome: 13 file) la copia resta com'è — l'AOD cambia
  il numero — e finché non lo fa il salvataggio lo dice (**record che si fondono**) e chiede conferma. Dopo: **682/695**,
  i 13 restanti sono esattamente quelli segnalati.
- 🔴 **Un mio errore preso da una prova che all'inizio non distingueva**: la copia di prova con il solo NOME del file
  perdeva `/ENRMVA/`, e il motore leggeva un `.mva` di rotta col lettore degli aeroporti (etichetta dal campo 2 invece
  che dal 5) — errori «nuovi» falsi. `RiletturaDiProva` copia il percorso relativo intero. La prima prova (problemi e
  numero di record) passava anche rotta; quella buona guarda l'**etichetta** letta.

Misura sull'albero vero, in una copia: **§0** sposto un fix e salvo → 116 ms, 838 righe prima e dopo, **una**
diversa, impronta in sessione = disco. **Un record aggiunto in ognuno dei 695 file**, salvati insieme → controllo
9,6 s, salvataggio 20 s (rifà il controllo), 695 salvati, 0 errori nuovi, zero `.tmp` rimasti; il backup rimesso al
suo posto ridà **754/754 file identici** all'originale. **Conflitto** (`itvor.vor` cambiato dopo l'apertura) → fermo,
file del collega intatto. La misura della slice 8 dopo la correzione: sempre **695/695** e **637/637**, annulla → 0
file diversi; aggiungere un record in tutti costa 2,7 s (la rilettura di prova si fa solo nei file senza record
accostati: 10,6 s se la si fa sempre).

Test: +25 (Lab 236, motore +2): `SalvataggioTests` (i cinque passi, backup e pulizia, conflitto, errori nuovi,
tentativi, guasto a metà, rilettura diversa, record che si fondono, `.mva` di rotta) e `SalvataggioAschermoTests`
(bUnit: tasto, esito col backup, conferma, «Ricarica dal disco» col diff perso, riaprire azzera). Controprove: chiave
dei problemi col numero di riga → 1 rosso; copia di prova col solo nome → 1 rosso.

**Slice 10 — il pannello dei problemi** (23 settembre). Il validatore del motore sull'albero intero, in una linguetta
«Problemi» accanto a Sfoglia; i conti anche nella barra.
- **Fuori dal circuito**: la validazione dell'albero (1,5 s) parte all'apertura, che non la aspetta, e dopo ogni
  salvataggio; se due giri si accavallano vale l'ultimo. Il pannello dice «validazione…» invece di mostrare zero.
- 🔴 **Due modi di scrivere lo stesso file**: il validatore dà i percorsi relativi a `SectorFiles` e con le barre del
  sistema (`Include\IT\NAVAIDS\itvor.vor`), la sessione relativi alla radice e con le barre dritte. Si traducono in un
  posto solo (`Core/Problemi/ProblemiDelLab`).
- **Clic**: il record che ha quella riga (`IFileConRecord.RecordDellaRiga`: le righe di DATI, non i commenti in testa)
  si sceglie — ispettore, mappa, albero — e la riga si segna fra le righe grezze. Una riga fuori da un record (il
  lettore non l'ha capita, un `F;` di un `.isc`) non ha scheda: l'ispettore mostra le righe **del disco** intorno.
- **Filtro** per testo (file, ICAO, regola, dettaglio, riga) ed errori/avvisi; se ne disegnano i primi 200.
- **«La tua modifica introduce 1 errore»**: dopo ogni gesto il controllo della slice 9 si rifà, diviso in due. La parte
  che legge i record IN MEMORIA (che l'AOD cambia sul posto) resta sul filo del gesto: 27 ms su `itgeo.geo`, 0 su
  `APT.fix`. La validazione delle copie va fuori: 232 e 34 ms. In cima al pannello, e un «!» sulla linguetta.
- La misura ha corretto il messaggio della slice 9: un file che riletto non ridà i suoi record non è solo la copia che
  ha il nome del vicino — un «;» scritto dentro un nome spezza la riga, e riletto quel record non c'è più. Il
  controllo lo diceva già; il testo a schermo dava una causa sola.

Misura sull'albero vero: **483 problemi (130 errori, 353 avvisi) in 1,5 s**, gli stessi di F2; **483/483** file
ritrovati nella sessione; **379** agganciati a un record, tutti con la riga e il testo giusti; 98 righe fuori da un
record, 6 problemi di un file intero.

Test: +12 (Lab 248): `ProblemiDelLabTests` e `ProblemiAschermoTests` (bUnit: conti, filtro, clic con e senza record,
problemi nuovi che compaiono e spariscono annullando). 🔴 **Il lavoro in background ridisegna a metà di un test**:
alla prima corsa della suite intera tre bUnit vecchi (campi, vertici, salvataggio) sono caduti con «There is no event
handler with ID …» — la validazione finiva fra il `Find` e il `Click`. L'app non ne soffre (Blazor Server tiene gli id
vecchi finché il browser non conferma il disegno), bUnit sì: i test a schermo aprono con `ApriEValidaAsync`, e il
controllo delle modifiche ridisegna **solo se l'esito cambia** (quasi mai: la maggior parte dei gesti non porta
problemi). 🔴 Un attributo `bool` vero Blazor lo scrive **vuoto**
(`data-x=""`): la prova col valore `True` non trovava niente. Controprove: percorsi non tradotti → 6 rossi; commenti in
testa contati come righe del record → 1 rosso (la prima versione della prova passava anche così: le righe in testa ad
`APT.fix` sono un chunk grezzo, non commenti di un record).

**Prove a mano del committente** (23 settembre, dopo la slice 10). Su un fork del sector
(`SkyMistery/it-aurora-sector-test`, clonato fuori dal repo in `D:\Programmazione\IVAO_Test\it-aurora-sector-test`),
con l'eseguibile pubblicato autonomo e la lista `SectorLab-prova\PROVE.md`. Prima di passargli la mano: autoprova
dell'eseguibile sul clone (756 file, 13 strati, 21 855 forme in 0,4 s) e un salvataggio vero seguito da `git diff`
(**2 file, −1 +1 ciascuno**, CRLF intatti; clone rimesso con `git checkout`). Validatore sul fork: 125 errori, 353
avvisi (5 errori corretti dagli AOD dopo l'albero delle misure).
- ✅ **Aurora carica il sector salvato dal Lab** (prove 1-5: fix spostato, vertici, incolla da testo AIP): «tutto è
  dov'è». È la prova che i test non possono fare.
- 🔴 **I `dummy` minuscoli**: la mappa tirava 53 linee a N0 E0 (i cerchietti dei COP di `FRA-gates.artcc`, e
  `FRA.artcc`). Il motore riconosceva il separatore solo come `DUMMY`; il sector scrive anche `T;dummy;…` (100 righe in
  4 file) e Aurora li tratta allo stesso modo. Corretti i tre lettori (`.artcc`, `.hartcc`/`.lartcc`, `.mva`); la regola
  «maiuscolo soltanto» veniva dalla specifica di A e due test la difendevano: rovesciati. Il lettore della vIPI era già
  indifferente alle maiuscole: il sito non cambia. Dopo: 4 forme vicino a 0,0, tutte righe `MAPS` dei `.rw` di FIR (non
  sono piste: un punto, nessuna linea).
- **La mappa segue la scelta**: scegliere un record dall'elenco, dalla ricerca o dai problemi **accende il suo strato**
  (i punti sono spenti di base, e il fix scelto non si vedeva); un secondo clic sullo stesso record **reinquadra**; dopo
  una modifica del record scelto lo strato si ridisegna e la mappa **lo segue dov'è ora**.
- **Il registro** (chiesto dal committente): `%LOCALAPPDATA%\VipiSectorLab\log\lab-aaaammgg.txt`, un file al giorno per
  14 giorni — apertura, scelte, ogni modifica col suo esito o rifiuto, annulla, salvataggi (stato, file, conflitti,
  problemi nuovi, backup), ricariche, validazioni coi tempi, e **ogni errore con lo stack**: anche quelli del framework
  (un ponte da `ILogger`, avvisi ed errori) e quelli che nessuno raccoglie (WinForms, AppDomain, task non osservati).
  Tasto «Registro» nella barra: apre la cartella.
- `git diff` non è un tasto del Lab: il Lab non tocca git (§4, è F4). Era un comando da lanciare nel clone.

Test: +11 (Lab 254, motore 427): `RegistroTests`, due bUnit della mappa, tre del `dummy`. Controprove: strato non
acceso → 1 rosso; mappa che non segue → 1 rosso.

**Seconda tornata di prove** (23 settembre): ✅ **tutte e dieci** (la 2 e la 3 rifatte con le correzioni, 6-9, 11).
Il `git diff` delle prove ha mostrato due cose dell'«incolla da testo» che cambiavano il file più del necessario, sul
settore `LIRN_TWR` di `twrs.tfl`:
- **Gli archi fitti come quelli del file**: il convertitore di F1 mette un punto per grado, chi ha disegnato il file
  uno ogni 4-8° — un arco da 11 punti ne diventava 95, il file cresceva di 240 righe. Ora `DensitaDegliArchi` stima il
  passo dagli archi che il record (o il suo file) ha già: ai vertici «da arco» (lati quasi uguali, svolta di 0,5-30°)
  l'angolo di svolta è il passo, e se ne prende la mediana. Nella scheda, accanto a «Incolla da testo», **«un punto
  ogni N°»** porta la stima, e l'AOD la cambia. Albero vero: `LIRN_TWR` 4,3°; 192 file con archi misurabili, mediana
  9,3°, i file già fatti col convertitore 1°.
- **La chiusura**: il vecchio elenco ripeteva il primo punto in fondo (la forma dei `.tfl`), quello incollato no. Ora
  se l'elenco di prima era chiuso, lo è anche il nuovo; un elenco aperto resta aperto.

Test: +8 (Lab 262): `DensitaEChiusuraTests` (passo di un arco sintetico da 8°, 4° e 2°, nessuna stima per i lati
dritti, `LIRN_TWR` vero, chiusura conservata e non inventata, densità rispettata). Controprova: chiusura tolta → 1
rosso.

## §9 — Decise col committente prima della slice 0 (✅ 22 settembre)

Tutte accettate. Una modificata: la **2** (consegna) — non uno zip passato a mano, ma una **release GitHub** col link
nella vIPI per editor e admin (§2.5); di conseguenza la **7** ammette una richiesta anonima a GitHub per la versione.

1. **Tre progetti** (`Core`, `Ui`, guscio) + un progetto di test che gira in CI su Ubuntu, come il Bridge.
2. **`net10.0` autonomo**, consegnato come **cartella zippata** (non file unico: la WebView2 e gli statici di Blazor
   vogliono stare sul disco), **non firmato** (carta madre, decisione 11: al primo avvio Windows SmartScreen chiede
   conferma). ✅ **Modificata**: la cartella zippata sta in una **release GitHub** (tag `sectorlab-v*`, costruita
   dalla CI) e il link è nella vIPI, per Editor e Admin (§2.5).
3. **Che cosa si modifica in F3**: campi, vertici (anche incollando testo AIP), aggiungi/togli record. Il
   **trascinamento** resta in F9.
4. **Tag `//@`**: F3 **non** li scrive, nemmeno sui `.sid`/`.str` che tocca. La conversione graduale (carta madre §8.2)
   parte con F7, quando il Lab saprà anche **proporre** i valori che vIPI conosce già (initial climb, fix intero) — per
   farlo deve parlare con vIPI, e F3 non ci parla. Così in F3 un diff è sempre e solo la modifica fatta.
5. **Backup fuori dal clone**, in `%LOCALAPPDATA%`, per 30 giorni.
6. **Errori nuovi al salvataggio**: avviso con conferma, non blocco. Gli errori vecchi non fermano niente.
7. **Nessuna rete** in F3 (niente vIPI, niente login, niente sfondo a tessere) — ✅ tranne la richiesta anonima a
   GitHub per sapere se c'è una versione nuova (§2.5).
8. **Sfoglia per cartella + ricerca**. La vista «per aeroporto» di B (un ICAO → tutti i suoi file) è comoda ma chiede
   regole di attribuzione (B §2: `bd_ad_gnd.pol` → LIBD, `BC404` → LIBC): la propongo come aggiunta dopo, se serve.

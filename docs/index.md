# Indice della documentazione

Mappa di tutti i documenti del progetto, con scopo e stato. Entry point in root: **`../README.md`** (cos'è + build) e **`../HANDOFF.md`** (stato corrente + come riprendere).

**Stato:** 🟢 Autorevole (corrente) · 🔵 Reference (config/integrazione) · 🟣 Design · ⚪ Storico.

## Ordine di lettura consigliato (nuova chat)
0. **[lavori-aperti.md](lavori-aperti.md)** — 📋 elenco unico di **cosa manca da fare**, con il blocco di
   ciascuna voce. Se l'obiettivo è lavorare e non capire, si parte da qui.
1. `../README.md` — cos'è il progetto, architettura, build/run.
2. `../HANDOFF.md` — stato corrente e come riprendere il lavoro.
3. `history/rounds.md` — cosa è cambiato round per round.
4. `spec/modello-dati.md` + `spec/logica-aor.md` — modello dati e logica AoR (cuore testato).
5. `spec/mappa-pagine.md` — rotte del sito.
6. `adr/` — decisioni architetturali (in ordine 0001 → 0006).

## Processo (anti-vibecoding) 🟢
| File | Scopo |
|---|---|
| [FEATURE-PROCESS.md](FEATURE-PROCESS.md) | Runbook per feature nuove: pre-flight 4 domande (modello / dispatch «Regola del 2» / ingressi+verifica / **propagazione**) + DoD. |
| [refactor/REFACTOR-PROCESS.md](refactor/REFACTOR-PROCESS.md) | Runbook per refactor: ciclo Fase 0→4, gate «carta prima di codice». |

## Specifiche tecniche — `spec/` 🟢
| File | Scopo |
|---|---|
| [spec/modello-dati.md](spec/modello-dati.md) | Schema dati / entità EF Core. La **§9** è autorevole; §3–§5 sono storiche (pre-Round 5/13). |
| [spec/logica-aor.md](spec/logica-aor.md) | Logica di visibilità AoR + scenari di test S1–S10 (implementata e testata). |
| [spec/mappa-pagine.md](spec/mappa-pagine.md) | Gerarchia rotte dei **tre** servizi (`/services/vsop`, `/services/profile-swapper`, `/services/stats`) + tabella rotte→file→accesso. |
| [spec/pagine-disabilitate.md](spec/pagine-disabilitate.md) | Pagine spente (rotta rimossa, codice intatto) + come riattivarle. |

## Guide operative — `guide/` 🔵
| File | Scopo |
|---|---|
| [guide/integration.md](guide/integration.md) | Come agganciare il modulo (RCL) a un sito host ASP.NET Core. |
| [guide/integrazione-ivao-it-da-fare.md](guide/integrazione-ivao-it-da-fare.md) | **Lavoro aperto** per far girare il modulo dentro Ivao.It: bloccanti, verifiche mai eseguite, decisioni loro. |
| [guide/config.md](guide/config.md) | Reference completa della configurazione runtime (Division/DataSource/Ivao/Auth/segreti/policy import). |
| [guide/dev-bootstrap.md](guide/dev-bootstrap.md) | Checklist «da DB vuoto a sito popolato» in sviluppo (sequenza import ACC→settori→aeroporti→SID→gerarchia→documenti). |
| [guide/preparare-un-pacchetto.md](guide/preparare-un-pacchetto.md) | 🟢 **La regola, ogni volta che si consegna.** L'ordine dei passi (verde → ruota → publish → scelta dei file → zip → prova sul PACCHETTO → foglio → si scrive dove si è arrivati) e le trappole che nessuno script può decidere: `main` non è ciò che gira, il diff per impronta **gonfia** (gli assiemi ricompilati differiscono per l'MVID: comanda `git diff -- src`), `wwwroot` e l'indice `staticwebassets` viaggiano **insieme**, i `.md` non stanno coi file da caricare, **i segreti non entrano in nessun pacchetto**. Lo script dei passi meccanici è `tools/prepara-pacchetto.ps1`, con le due reti: lo zip si costruisce dall'elenco **dichiarato** e i file di testo si guardano **dentro**. |
| [guide/aurora-bridge.md](guide/aurora-bridge.md) | **Guida per il controllore** al tool desktop Aurora: prerequisiti, scrittura del livello, scorciatoia, limiti, dove guardare quando non va. |

## Reference — `reference/` 🔵
| File | Scopo |
|---|---|
| [reference/sector-map.md](reference/sector-map.md) | Mappa settori della ACC pilota Roma (seed di test/fixture). |
| [reference/api-aurora-bridge.md](reference/api-aurora-bridge.md) | Contratto di `POST /vsop/api/v1/transfers/resolve` (bridge Aurora): richiesta, risposta, configurazione `AuroraBridge`, tetti. |

## Design — `design/` 🟣
| File | Scopo |
|---|---|
| [design/piano-vipi-tool.md](design/piano-vipi-tool.md) | Documento di design strategico di base (requisiti, roadmap). Parti superate dai round successivi. |
| [design/piano-editor-appn.md](design/piano-editor-appn.md) | Design editor/viewer APP non remotizzati (storage su Document dopo refactor 08). |
| [design/piano-aurora-bridge.md](design/piano-aurora-bridge.md) | **Tool desktop Aurora ↔ vIPI**: propone il livello di trasferimento al prossimo ente e lo scrive nell'etichetta quota del tag. Endpoint `/vsop/api/v1/transfers/resolve` + app Avalonia. **Codice non iniziato**: prima lo spike F0 sul protocollo Aurora. |
| [design/piano-supporto-mysql.md](design/piano-supporto-mysql.md) | **Supporto MySQL per l'embedding in Ivao.It** (solo TFM net8: Pomelo non ha build EF Core 10). Slice, rischi, stime. **Esecuzione non avviata**: attende la versione del loro server MySQL, che decide la collation. |
| [design/piano-ux-hardening.md](design/piano-ux-hardening.md) | UX hardening (audit 2026-07-22): U1 conferma delete (`InlineConfirm`), U2 icone SVG (`Icon`), U3 zoom a11y, U5–U12 refactor tema (token colori/font, dedup CSS, `.choice`/`.pill.neutral`, `LoadingState`/`EmptyState`, `.live-badge.off`, touch target). **U4 i18n IT+EN COMPLETO (2026-07-23):** chrome app tutta localizzata (nav+viewer+admin 12/12+editor 12/12), 1071 chiavi `SharedResource.resx`/`.en.resx`, switch runtime `?culture=en`. Contenuto editoriale dal DB resta IT. |
| [design/regole-lingua.md](design/regole-lingua.md) | **Regole della lingua** (28 ago 2026, **R8** il 1 set): che cosa cambia quando si cambia lingua e che cosa no. Il **marchio** e il **titolo di un documento** sono nomi e non si traducono mai; la **briciola di pane** è sempre in inglese in tutte e due le versioni del sito; l'**avviso di simulazione** («ONLY FOR SIMULATION…») è un cartello e sta fuori dai `.resx` (**R8**); gli indirizzi non si localizzano. In coda le trappole già pagate: il circuito Blazor che resta nella lingua di prima con la navigazione «enhanced», i due selettori che dicevano cose diverse, il titolo di catalogo che scavalcava la traduzione. |
| [design/regole-ui-pagine-admin.md](design/regole-ui-pagine-admin.md) | **Regole di densità e uso delle pagine admin** (19 ago 2026): testata in una riga, prosa nei «?», altezza misurata (e le due unità dello zoom), tabelle lunghe, scritture che si accumulano, comandi che non saltano. Nate dai tre giri su accordi / struttura / ACC. In coda la **ricognizione misurata di tutte le pagine di lavoro** (chi aderisce, chi no e cosa le manca) in ordine di guadagno. |
| [design/regole-perimetro-servizi.md](design/regole-perimetro-servizi.md) | **Il perimetro dei servizi** (1 set 2026): che cosa entra in questo sito e che cosa no. Le cinque regole (**P1** il dato è già qui · **P2** non si duplica uno strumento che IVAO ha già · **P3** si documenta, non si valutano le persone · **P4** niente strumenti generici · **P5** un servizio è figlio diretto di `/services`) e le **otto idee del 1 settembre** con il loro esito: due accettate (coerenza sectorfile, segnalazioni), una **assorbita** nella vista live (il briefing di apertura posizione), cinque respinte **col motivo scritto** — presa visione/quiz, copertura eventi, cassetta degli attrezzi, mappa live nazionale, libretto del controllore. Serve a non rifare la stessa proposta fra sei mesi. |
| [design/piano-coerenza-sectorfile.md](design/piano-coerenza-sectorfile.md) | ✅ **ESEGUITO e FUSO IN `main`** (1 set 2026). **Coerenza vIPI ↔ sectorfile**: due sorgenti indipendenti dicono le stesse cose — API IVAO e sectorfile Aurora — e nessuno le confrontava. `itfreq.frq` (201 posizioni), `itap.ap` (130 aeroporti, **la TA**), `itrw.rw` (241 righe di cui **96 pseudo-piste `MAPS`**) contro i cataloghi. **Nessuna entità, nessuna migrazione.** ⚠️ Le tre decisioni che reggono tutto: l'health check **ignora** l'area nuova (contarla vorrebbe dire `/vsop/health` giallo per sempre), il confronto **non gira dentro la richiesta** (fa I/O di rete su un endpoint anonimo), e la prima slice è stata **misurare** — che ha ucciso il controllo sul QFU e aggiunto quattro filtri. |
| [design/piano-segnalazioni.md](design/piano-segnalazioni.md) | **Le segnalazioni dal campo** (carta, **non eseguita**). Chi legge i documenti in frequenza non ha una porta se non la posta: un canale in-app ancorato a **documento · sezione · rilascio letto**, con triage e **risposta**. Entità nuova (`FieldReport`) ma **nessuna terza lista** — entra nel read-model `WorkItem` con una `WorkOrigin` in più. ⚠️ `SectionKey` e non `SectionId` (le sezioni sono per-versione), `ReleaseNumber` o il triage guarda un testo diverso, e la migrazione deve passare il presidio della **finestra cieca**. |
| [design/piano-import-tabelle.md](design/piano-import-tabelle.md) | **Importare una tabella** (carta del 2 settembre 2026, **in esecuzione**): incolla, CSV, XLSX, Markdown, HTML e larghezza fissa in **una** pipeline a quattro stadi (acquisizione → mappatura → proposta → applica), con anteprima riga per riga prima di scrivere. `ClausePaste` diventa una spec del registry invece di restare un secondo meccanismo. ⚠️ La distanza degli alternati passa a `decimal` **nel BodyJson** — nessuna entità, nessuna migrazione. |

## Feature — `feature/` 🔵
Una scheda per feature/fix non banale: stato di partenza rilevato, design, passi, **verifica live** ed esito.
Le lezioni riusabili finiscono anche nelle memorie; qui resta il perché delle scelte.
| File | Scopo |
|---|---|
| [feature/2026-07-29-toc-editor.md](feature/2026-07-29-toc-editor.md) | TOC laterale sezioni negli editor (menu di navigazione sticky, rail azioni, UX lock). |
| [feature/2026-07-30-stampa-documenti.md](feature/2026-07-30-stampa-documenti.md) | **Stampa dei documenti** (`@media print`): foglio `vipi-print.css`, `PrintMeta`, tasto Stampa, apertura dei `<details>`, scala tipografica da carta, mappe AoR ridimensionate, dati live esclusi. Include il fix delle larghezze di colonna dei coordinamenti (schermo **e** stampa). |
| [feature/2026-07-30-pill-stato-dopo-publish.md](feature/2026-07-30-pill-stato-dopo-publish.md) | «Bozza vN» dopo «Pubblica ora»: callback `Published` di `ReleasePanel` + etichetta «rilascio #N»; in coda il fix della **chiave di release ACC** che ignorava la radice dell'albero. |
| [feature/2026-08-03-aree-regolamentate-hardening.md](feature/2026-08-03-aree-regolamentate-hardening.md) | **Aree regolamentate**: categoria di import `SpecialAreas` (escludere = congelare, non «manuale»), dettaglio shape saltato quando è già in archivio, rilievo «Area regolamentata dangling» + marcatura nell'editor; poi picker scopribile e **appartenenza multi-ACC** (`SpecialAreaCenter`: la R49 «Zita» è di LIRR *e* del militare, prima vinceva l'ultimo ACC alfabetico) e **aree estere solo su richiesta** per ACC (763 legami su 993 liberati). In coda: il default dei flag bool nuovi, che nasceva `false` anche sul reconciler Postgres. |
| [feature/2026-07-31-aor3d-leggibilita.md](feature/2026-07-31-aor3d-leggibilita.md) | **AoR 3D leggibile**: selettore «Altezza» ×0.25→×2 (default ×0.5), etichette da sprite a overlay HTML con declutter, chip settore condivise col 2D. Il link «Apri pagina» è rimosso in attesa di rilavorare la pagina dedicata (rotta viva). |
| [feature/2026-08-12-editor-trasferimenti-ux.md](feature/2026-08-12-editor-trasferimenti-ux.md) | ✅ **CHIUSO, verifica live eseguita.** **Editor trasferimenti**: l'editing esce dalla riga ed entra nel **pannello a destra** (`.xfe-layout`, il pattern che Permessi/Incarichi/Struttura già usavano e questa pagina no). Nove bottoni-icona per riga → tre; tre colonne di condizione → una; tre bottoni «Gruppo» → Duplica/Modifica/Elimina; **51 stili inline → 0** (più il `<style>` nel markup: la CSP può passare in enforcement). Dieci QoL: trascinamento, duplica gruppo, modifica in blocco del ricevente, filtro «senza ricevente», avvisi di modifiche non salvate e lock in scadenza, anteprima sempre accesa, copia righe da un altro gruppo, ordinamento della vista, tasti. |
| [feature/2026-08-12-editor-trasferimenti-rifiniture.md](feature/2026-08-12-editor-trasferimenti-rifiniture.md) | ✅ **CHIUSO, verifica live eseguita.** **Il costo per gesto, la tastiera, l'annulla, il blocco.** Secondo giro sull'editor a tre colonne, con gli attriti di nuovo **contati nel sorgente**. Il più caro era un debito contratto dal giro precedente: ogni salvataggio costava **8 query** (due caricavano tutti i flussi dell'ACC), e da quando si scrive in cella si paga a ogni Invio — il contesto delle frasi ora si rifa solo sulle scritture di **gruppo**, e salvare una casella costa **1 query**. Il picker a digitazione era scritto **sei volte** e nessuna copia prendeva i tasti: estratto in `TypeaheadPicker` con frecce, Invio ed Esc. **Annulla** dopo un'eliminazione — e il punto non è il tasto ma cosa torna: ricostruire un gruppo con `AddPointAsync` appiattirebbe l'outline in silenzio, quindi il ripristino ha una via propria che porta gruppo, profondità e ordine, con gli invarianti rivalidati. **Blocco esteso** a livello (stessa sintassi della cella), condizione ed eliminazione — tre metodi distinti perché il ricevente si propaga al gruppo e gli altri no. Più ordinamento per intestazione, «+ riga» dall'elenco e pannello che si ritira. ⚠️ Tre difetti visti **solo** a schermo, tutti della stessa famiglia: `Text="form.NextText"` senza `@` passava la **stringa letterale** (la trappola del runbook, §7), la tendina di ordinamento si mostrava **vuota** con una preferenza da-solo-elenco, ed etichette e suggerimenti si toccavano. |
| [feature/2026-08-12-editor-trasferimenti-tre-colonne.md](feature/2026-08-12-editor-trasferimenti-tre-colonne.md) | ✅ **CHIUSO, verifica live eseguita.** **L'editor trasferimenti a tre colonne.** Aperto da un giudizio d'uso («ancora scomoda e poco fluida»), con gli attriti **contati nel sorgente** prima di proporre: la pagina rendeva *tutti* i gruppi dell'ACC uno sotto l'altro dietro tre livelli di collasso richiusi a ogni cambio ACC — tre clic per arrivare a una riga, ogni volta. Ora **navigatore · riquadro di lavoro · pannello**, ognuna col proprio scorrimento; il gruppo è una **foglia** dell'albero, non un livello da aprire. Vista **Albero ⇄ Elenco** (l'elenco mostra tutte le righe dell'ACC con settore/aeroporto/tipo come colonne: è il modo di *rivedere*) con **una** tabella sola per entrambe. **CoP, livello e ricevente si scrivono in cella** — Invio scende, Tab passa, Esc annulla — e il livello si rilegge con `LevelFormatting.Parse`, il cui contratto è un round-trip provato su 102 casi generati. Stato in **URL** (un gruppo si linka, un F5 non azzera), preferenze di vista in `localStorage`. ⚠️ Chiuso un difetto che c'era da prima: il filtro «senza ricevente» si accendeva, contava giusto e **non filtrava nulla**. Tre difetti visti solo a schermo: la pagina scorreva di 148 px perché l'altezza «schermo meno chrome» in CSS non è esprimibile (ora la misura `vipiFitViewport`); il navigatore si srotolava per 2174 px a schermo stretto; aprire una cella spostava le colonne di ~100 px (larghezza **intrinseca** dell'`<input>`). |
| [feature/2026-08-12-trasferimenti-gruppi-e-salva.md](feature/2026-08-12-trasferimenti-gruppi-e-salva.md) | ✅ **CHIUSO, verifica live eseguita.** Due difetti riportati guardando la pagina rifatta. **1.** In tabella non si vedeva quale condizione fosse eccezione o alternativa di quale: le righe di un gruppo si distinguevano solo per una velatura al 14% uguale per ogni gruppo. Ora il blocco si disegna (guida verticale a tinte alternate, bordi che aprono e chiudono, pill «⑂ n varianti» sulla capofila) e la colonna Condizione dice il **ruolo**: «se X», pill «negli altri casi», «↳ eccezione di: X» col rientro spostato dalla cella del CoP a quella della condizione, dov'è nella vista pubblica. Il padre lo legge `CoordinationDerivation.ParentOf`, estratto da `ConditionChain`: una risalita sola. **2.** Il Salva del pannello non compariva in **nessuna** posizione di scorrimento (1426 px di contenuto in 904 disponibili): pannello diviso in testata · corpo · piede, scorre solo il corpo. Sotto i 1080 px `vipiRevealPanel` porta il pannello in vista se il suo piede non lo è. **In coda**: la scheda intera non ci stava lo stesso (885 px di campi in 501 su un portatile) — le sezioni «Trasferimento» e «Condizione» ora partono **richiuse quando sono vuote**, con il riassunto di cosa contengono accanto al titolo, e la scheda passa a **502 px**: zero fuori dalla vista. **Seconda coda**: su una riga **esistente** il corpo arriva a 1324 px e le sei azioni sulla riga restavano fuori schermo (823 px nascosti su un portatile) — scorrere la pagina non le raggiunge, perché a scorrere è il corpo; ora sono un **menù del piede**, che non scorre. |
| [feature/2026-08-12-varianti-a-livelli.md](feature/2026-08-12-varianti-a-livelli.md) | ✅ **CHIUSO, verifica live eseguita.** **Varianti a livelli**: il gruppo di varianti diventa un **outline** — le alternative di primo livello sono **pari-grado** (pista 07 · pista 25, nessuna è lo standard dell'altra), ognuna può avere eccezioni **annidate a profondità libera** («area attiva» e, dentro, «area attiva e di notte»), più le righe che **scavalcano** le alternative («di notte, qualunque pista»). `IsOtherwise` rimosso prima del merge, quindi a dati zero. In tabella si legge il delta, nella frase la catena cumulata. ⚠️ Chi sposta una riga deve spostarne il **sottoalbero**: lasciare indietro le eccezioni le riassegna senza alcun errore. |
| [feature/2026-08-11-trasferimenti-acc-app.md](feature/2026-08-11-trasferimenti-acc-app.md) | ✅ **CHIUSO, verifica live eseguita** (la parte «varianti» è superata dalla scheda del 12 ago). **Trasferimenti ACC↔APP**: livello **autorizzato** e livello **al trasferimento** separati (oggi il modello ha un evento solo), punto di trasferimento (confine AoR / fix / libero) distinto dalle comunicazioni, restrizione di velocità, **gruppo di varianti** con riga «negli altri casi» (le alternative per condizione oggi sono righe scollegate), e la sezione estesa che mostra **tutto** ciò che entra o esce da un ente (il passo 2 della derivazione filtrava solo arrivi da CTR). |
| [feature/2026-08-22-servizi-atc-e-profile-swapper.md](feature/2026-08-22-servizi-atc-e-profile-swapper.md) | ✅ **CHIUSO, verifica live eseguita** (confronto dei byte: la sezione copiata identica al sorgente, il resto dei file identico byte per byte). **Servizi ATC**: il sito diventa il contenitore degli strumenti. Hub «/services», la documentazione operativa scende a «/services/services/vsop», le dieci rotte rimaste in italiano passano all'inglese («guida→guide», «permessi→permissions», …) e i reindirizzamenti diventano una **tabella** con un test che pretende **un salto solo**. Gli endpoint macchina (health, API del bridge, SSE) **restano** dove sono: li conoscono Render, la CI e binari già distribuiti. Primo strumento integrato: l'**Aurora Profile Swapper** (motore copiato verbatim dal repo esterno, 26 fixture reali; da WebAssembly a Blazor Server, quindi i profili passano dal server — in memoria, mai su disco). |
| [feature/2026-08-22-topbar-misurata.md](feature/2026-08-22-topbar-misurata.md) | ✅ **CHIUSO, verifica live eseguita** (256 combinazioni: 8 larghezze × 4 zoom × 4 famiglie di pagina × 2 lingue, `scrollWidth == clientWidth` su tutte). **La topbar si misura da sola**: le tre media query del giro precedente (1500/1300/900) diventano quattro classi cumulative `tb-1…tb-4` messe da `vipiFitTopbar`. ⚠️ Il difetto non era la soglia ma il metodo — **una media query misura la finestra, mentre il problema è la larghezza della barra**, che dipende da login, stringa staff, numero di ACC, lingua, **zoom di pagina** e stato della ricerca: tarata su una configurazione era giusta solo lì, e il committente la vedeva rotta a 1940 dove la taratura diceva 1385. Chiude la voce aperta «la topbar sfonda fra 1301 e ~1410» **senza** il compromesso che dava per obbligato: a 1366 e 1440 la ricerca resta aperta, perché il gradino da 500px è diventato due. Nello stesso giro il **marchio** dice «Servizi ATC» e porta all’hub invece che a uno solo degli strumenti. |
| [feature/2026-08-22-catalogo-punti-suggerimenti.md](feature/2026-08-22-catalogo-punti-suggerimenti.md) | ✅ **CHIUSO, verifica live eseguita** (editor LIBD e LIRF, accordi LIBB, guidati con Edge). **Catalogo dei punti**: i nomi di fix/VOR/NDB che il parser SID gia' scaricava escono da Infrastructure e diventano una porta (`INavaidSource` + `NavaidCatalog`), cosi' i campi punto **suggeriscono** mentre si scrive e **segnano** i nomi che non esistono. Misurato prima di scrivere: **1 typo vero su 52** token CoP (`BESIV`, a una lettera da `BEKIV`) e **zero** falsi positivi su 442 transition. Sul tabellone delle SID importate entra il solo suggerimento: l'avviso c'e' gia' e le due condizioni coincidono **332 su 332**. ⚠️ Due difetti trovati solo a schermo: le **righe di commento** dei file navaid erano nel catalogo da sempre, e la **freccia dell'elenco nativo** si mangia ~11px — cioe' l'ultima lettera — tagliando anche RWY e TYPE, che erano **gia' rotte in `main`**. Nello stesso giro gli **alias dei fix** smettono di essere invisibili: si vedevano solo nel database (`ListAsync`/`DeleteAsync` esistevano e non li chiamava nessuno), e un alias e' **autoritativo** — se il suo bersaglio sparisce dal sectorfile l'import continua a scrivere quel nome senza che niente protesti. Ora si vedono e si tolgono da `/services/vsop/admin/sources`, e ogni ciclo d'import li elenca nei log. |
| [feature/2026-08-22-coordinamenti-lettura.md](feature/2026-08-22-coordinamenti-lettura.md) | ✅ **CHIUSO, verifica live eseguita** (A/B contro `main` sulla stessa bozza LIBB: 53 righe e 53 frasi prima e dopo, cambiano solo etichette e ordine). **I coordinamenti si leggono**: quattro attriti riferiti guardando i documenti veri. La **prosa nasce chiusa** in un blocco per tabella, e l'invito ad aprirla sta sulla **stessa riga del titolo** («Arrivi · Testo esteso (2 frasi)») invece di prendersene una — la stampa lo apre da sé e nasconde l'invito, non il titolo. La **densità** delle decine di tabelle di un documento ACC scende a misure proprie (celle `10px 12px`→`6px 10px`, corpi e margini): **misurato**, 10345→8423 px sul blocco Aerovia di LIBB, −18,6%. La **FIR porta il suo ICAO** (`Greece-LGGG`), coi nomi della sorgente IVAO. E dentro un settore l'ordine è la **distanza da chi legge** — casa, italiani, esteri — non l'alfabeto, che metteva la propria ACC in mezzo agli stranieri. ⚠️ I due cambi al **derivato** compaiono sui documenti già pubblicati alla **prossima release**: le sezioni pubbliche leggono lo snapshot congelato. ⚠️ Lo scaglione «casa» non è riproducibile sui dati di sviluppo di oggi: coperto da test, non dallo schermo. |
| [feature/2026-08-22-sorgenti-giro-automatico-ta-piste.md](feature/2026-08-22-sorgenti-giro-automatico-ta-piste.md) | **TA e piste smettono di aspettare un bottone**: delle sei righe di Sorgenti quattro giravano da sole ogni giorno e due no — una TA cambiata in AIRAC poteva restare vecchia a tempo indefinito, e la pill «su richiesta» non diceva **quanto**. Misurato prima di decidere sul `vipi.db` reale (92 aeroporti, 21 senza TA): un giro costa 1 chiamata per la TA più una per aeroporto per le piste, cioè **+15/20%** su un giro che esiste già. `AirportDataImportUseCase` passa per lo **stesso** merge del bottone (nessun secondo percorso che possa divergere sulla policy) e una **chiave di stato sola** per due categorie, che regge perché il gate sta nel merge. Nello stesso giro le due cose che la pagina non elencava: l'**anagrafica aeroporti** (resta su richiesta: crea entità) e le **shape TWR da GitHub** dentro il giro Settori. ⚠️ L'anagrafica non si riconosce più dal `null` della categoria, e i tre `switch` della pagina diventano uno. **In coda, deciso dal committente**: anche l'**anagrafica aeroporti** diventa un giro di 24h — la carta l'aveva messa fra le cose da non automatizzare — e da lì **nessuna** delle sette righe resta «su richiesta». ⚠️ Resta l'unico giro che **crea**: additivo, non rimuove mai. |
| [feature/2026-08-22-minime-di-vettoramento.md](feature/2026-08-22-minime-di-vettoramento.md) | ✅ **CHIUSO, verifica live eseguita** (copia del `vipi.db` reale, sorgente sectorfile accesa: i numeri a schermo combaciano con quelli misurati sui file — LIBB 7 tracciati/10 etichette/190 vertici, LIMM 43/51/1348). **Minime di vettoramento**: ultima voce del piano. Non la **tabella** `area → quota`, che il formato `.mva` non permette di ricostruire — l'etichetta è un testo indipendente dai poligoni (in `liph.mva` le dieci `L;` stanno tutte in cima al file), il legame va indovinato e su 345 etichette **70 cadono in più aree annidate e 13 in nessuna**, il testo non è un numero (`TRL`, `NO MINIMA`, `80/TRL`) e **92 tracciati su 315 sono aperti** — ma la **carta**, una per file `.mva`, disegnata verbatim su fondo a **rilievo senza strade** (due tile Esri impilate: terreno + ombreggiatura), con tracciati a *casing* bianco/rosso, etichette a pastiglia e i **settori AoR della stessa parte di documento accendibili** sopra, per vedere come le minime si rapportano ai confini — dal pannello dei livelli, che nasce **aperto** perché chiuso dietro l'iconcina quella funzione per chi guarda non esisteva. Larghezza piena come la mappa AoR (misurato: 729px entrambe), si adatta la sola altezza; nessun tooltip sui tracciati. Senza didascalia: il nome dell'aeroporto diceva una cosa più stretta del vero. ⚠️ La decisione del 9 agosto che scartava le MVA resta giusta **contro la tabella**: quello che il formato dichiara è il proprietario del file (`ENRMVA/{acc}.mva`, `{icao}.mva`), e a quella granularità l'attribuzione si legge invece di indovinarla — misurato, i 24 file per-aeroporto corrispondono tutti a un APP esistente, zero orfani. Sezione `minima` da `Editorial` a **`Derived`**: riprende il toggle Live/Congelata e si congela nello snapshot; tabelle `VectoringMinima*` **droppate**. Il rilievo sotto non è decorazione — sulla carta di Milano si legge 195/180 sulle Alpi, 25/30 in pianura, 90/110 sull'Appennino. ✅ I **25 APP su 49 senza file** (LIRF, LIMC, LIML…) sono **procedurali** e una carta di minime non ce l'hanno (committente, 23 agosto 2026): il file che manca è la risposta giusta, non una lacuna — nessuna richiesta all'AOD. |
| [feature/2026-08-23-quattro-difetti-e-le-proprieta.md](feature/2026-08-23-quattro-difetti-e-le-proprieta.md) | ✅ **CHIUSO, verifica live eseguita** (144 combinazioni: 6 pagine × 6 larghezze × 4 zoom, più la lingua guidata con `Accept-Language: en-US`). **Quattro difetti chiusi, e le proprietà dell'AoR.** ⚠️ Il filo che li lega non era previsto: **tre su quattro erano stati diagnosticati male**, e la diagnosi scritta costava meno di quella misurata. **(1) Admin = tutto lo staff di divisione** (decisione del committente): il default diventa il jolly `^IT-[A-Z0-9]+$`, perché il problema non erano i quattro codici scoperti ma che **ogni ruolo nuovo nasceva escluso**, in silenzio. **(2) Le tabelle del viewer sforavano a zoom alto** e il colpevole non era `.sid-table` (sta già in un div che scorre) ma **`.rwy-table`**, che non dichiara nessun minimo: 570 unità in una `.cb-body` da 497. ⚠️ La regola generale: **lo zoom qui è `zoom` sull'`<html>` e le `@media` non lo vedono** — valutano la finestra (1280) mentre il layout ne ha 914 — quindi la soglia **non si sposta, si toglie**: contenitore diretto di una tabella sempre scorrevole, `minmax(0,1fr)` **più** `min-width:0` sui figli (il primo azzera la traccia, non l'elemento), `overflow-wrap` dei titoli fuori dalla media query. Da 48px di sforo a **0 fino a zoom 1.8**, e a zoom 1 pagina identica. **(3) La lingua non arrivava al circuito**: in Blazor Server le richieste sono **due** e `/_blazor` non porta `?culture=it`, quindi ricadeva su `Accept-Language`; cookie scritto **solo** su richiesta esplicita, o congelerebbe per un anno una scelta mai fatta. **(4) Il test ballerino del bridge era un difetto vero**: `SendAsync` si connetteva prima di prendere il turno → due invii = **due socket**, e `stream` e canale letti in due istruzioni separate → si scriveva su un socket e si aspettava sull'altro canale. Visto fallire: **200 giri su 200** (3 min 10 s) contro **133 ms** col client nuovo. ⚠️ Il test lo vede **solo** se i due invii partono davvero insieme: in sequenza il difetto sparisce. Più i **test property-based sull'AoR** (E5, CsCheck): sei proprietà, e hanno subito trovato un commento che diceva `[lat,lon]` dove il formato IVAO mette la **longitudine prima** — una fixture ricavata da lì sarebbe stata ruotata di 90°, e la proiezione non protesta: disegna. |
| [feature/2026-08-23-live-coordinamenti-a-colonne.md](feature/2026-08-23-live-coordinamenti-a-colonne.md) | ✅ **CHIUSO, verifica live eseguita** (6 larghezze, 29 pagine guidate). **I coordinamenti della vista live entrano in una schermata**: una colonna per tipo di traffico, una riga per punto — 2835px → 1080 a 1920×1080, e zero scorrimento anche a 1440×900 e 1280×800. La masonry per mittente non poteva funzionare (il mittente è sempre uno). Filtro per aeroporto, «Compatta» che finalmente vale il 26%, `vipiCapInner`. In coda: `vipi-screens.js` **cancellava i coordinamenti veri**, e la potatura di 249 selettori che non potevano applicarsi a niente — col perché la passata sul solo TESTO sbaglia. |
| [feature/2026-08-24-coordinamenti-lato-ricevente.md](feature/2026-08-24-coordinamenti-lato-ricevente.md) | ✅ **CHIUSO, verifica live eseguita — fuso in `main` il 24 agosto (merge `84f741b`, ramo cancellato).** **I coordinamenti si leggono anche dal lato di chi riceve.** Un accordo si scrive una volta sola, dal lato di chi cede, e il documento del ricevente mostrava quelle stesse parole — «Zagreb Radar trasferisce a Brindisi Radar CS0…» dentro la vIPI di Brindisi — con la colonna della controparte intestata «Prossimo» mentre porta chi **consegna**. ⚠️ **Il fatto che decide il disegno: le tabelle sono MISTE.** `BuildAccTree` raggruppa per settore → ACC → aeroporto/tipo e **la direzione non è una chiave di raggruppamento**: il nodo `ES › Zagreb-LDZO › Sorvoli` porta **8 righe entranti e 6 uscenti**. Quindi il verso sta sulla **riga** (`AppCoordRow.IsIncoming`, additivo), non sulla tabella — ogni disegno che lo deduca dal tipo di flusso è sbagliato e sembra giusto (la scorciatoia «arrivi ⇒ entrante» in `AppCoordinationView` azzeccava solo fra APP e ACC). La frase cambia **template, non slot**: `{owner}` resta chi cede anche quando si scrive «{target} riceve da {owner}» — scambiare gli argomenti avrebbe cambiato in silenzio la regola asimmetrica dei codici di posizione (`OmitTargetCode`). Un nodo misto si **spezza in due tabelle** dentro `CoordTable`, non nell'albero: lì costerebbe un `<details>` su tutti i nodi, qui una riga di `<summary>` sui soli nodi misti, perché il titolo sta già nel cartiglio della prosa. In coda, su richiesta del committente, anche la frase **uscente** con faccetta: `TemplateCleared` girava il verbo principale («autorizza … e lo trasferisce a») mentre la forma breve dice «trasferisce a», e nella stessa tabella due righe dello stesso accordo si aprivano in due modi diversi. Ora le quattro forme (× direzione, × faccetta) hanno la stessa testa e la stessa coda. ⚠️ **I documenti già pubblicati non cambiano finché non si ripubblica**, misurato fianco a fianco: viewer 33 tabelle tutte «PROSSIMO», editor 39 con 13 «DA» e 4 nodi tagliati. |
| [feature/2026-08-24-servizio-statistiche-atc.md](feature/2026-08-24-servizio-statistiche-atc.md) | ✅ **FUSO in `main`** (il ramo `statistiche-atc` non esiste più) (verifica live eseguita due volte: il motore il 24 agosto contro IVAO vero, la veste il 25 su una copia del `vipi.db` con una sessione seminata). **Il terzo servizio: le statistiche da ATC** — ore, turni, traffico gestito, copertura della divisione. ⚠️ **IVAO dà le connessioni, non il traffico**: chi hai gestito lo costruiamo noi campionando l'AoR a ogni giro di poll, e l'AoR è **orizzontale E verticale** (un traffico a FL260 sopra uno spazio che finisce a FL195 non è suo). Un aereo va a **una sessione sola**; DEL e GND non si distinguono con la geometria — non hanno poligono, misurato 0 su 5 e 0 su 20 — ma con la **fase del volo**. I numeri sono **due**: movimenti (tratte che si sono mosse) e presenze (tutte), perché i volumi ACC partono da terra e contengono ogni aereo posteggiato della FIR. I **turni** non sono le connessioni: 501 su 1316 (38%) sono spezzoni di una caduta di linea. ⚠️ Difetto grosso trovato per strada: `LIRR_TS_CTR` **non attribuiva traffico a nessuno, mai** — la shape IVAO conteneva l'anello due volte, e ora un settore in quello stato lo dice da solo nella diagnostica. **§13 (25 agosto), la veste**: le **targhette** per volo dicono quel che si è VISTO — chi esce dall'area ancora in volo **non è «atterrato»** neanche se veniva da noi, è «uscito in volo» o «consegnato a LIRR_NE1_CTR»; la **striscia del turno** con la punta di traffico dichiarata *stimata* (la barra è la finestra, non la presenza); periodo e filtro **nell'indirizzo** per tenere le pagine SSR statiche; un **turno** per riga invece di una connessione; tabelle → schede sotto i 700px **di contenitore** (`@container`: lo zoom è `zoom`, le media query non lo vedono). ⚠️ Suite verde e Release pulita **prima** di guardare le pagine, e la verifica live ha poi trovato **sette difetti**, due dei quali erano conti sbagliati (la ciambella sulle prime venti postazioni invece che su tutte; un riquadro che contava le righe della classifica tagliata e le chiamava «controllori»). **§16 (25 agosto, tarda sera), le otto richieste del committente**: la sparkline che ora ha un **asse** (non si capiva di che cosa fosse la forma); «quando controlli» che diventa **due grafici** invece della griglia 7×24 (la griglia resta alla divisione, dove l'incrocio serve); **via l'esportazione CSV**, tasto e meccanismo; il tasto per la divisione **sulla riga del titolo**; la **ricerca per VID** per lo staff; e la funzione nuova: **Aeroporti — traffico e copertura**, quanto traffico c'è stato su ogni campo e quanto ha trovato un controllore acceso, per ACC. ⚠️ Ha scoperto che **§3 diceva una cosa falsa**: `/airports/{icao}/stats` non dà conteggi giornalieri, è una fotografia al minuto con `limit` sotto 100 — quel che serve lo dà `/traffics`, che regge **trenta giorni in una chiamata** e porta gli **istanti** che buttavamo (zero endpoint nuovi). Provato con dati veri: **3 525 giorni-aeroporto** misurati, **16 374 movimenti e il 20% con ATC**. Dentro anche la **potatura** del dettaglio (decisa da luglio, mai scritta) e il **capitolo di Guida** che mancava — ⚠️ la voce nella ricerca globale c'era già e puntava a un'ancora **inesistente**, cioè un collegamento morto che nessuno denuncia. Altri **quattro difetti visti solo a schermo**, tutti nei grafici nuovi. Cosa resta: **§12**. |
| [feature/2026-08-25-vid-porta-sul-profilo-ivao.md](feature/2026-08-25-vid-porta-sul-profilo-ivao.md) | ✅ **CHIUSO, verifica live eseguita** (nove pagine guidate; la risalita del clic provata **con controprova**, i due temi, la stampa). **Il VID è una porta sul profilo IVAO** — chiesto dal committente: cliccando un VID, in qualsiasi pagina, si apre `ivao.aero/Member.aspx?Id=<VID>`. **Un componente solo** (`VidLink.razor`) per quindici punti in dieci file, ed è l'unico posto dove quell'indirizzo è scritto — ⚠️ `ivao.aero` e non `api.ivao.aero`, che è l'IdP del login e si somiglia abbastanza da sbagliarsi. Tre forme (numero nudo · «VID 123456» · «Nome (VID 123456)») scelte da un parametro. ⚠️ **La regola: il link sta dove il VID si VEDE già**, non dove lo si potrebbe ricavare — dove a schermo c'è il nome (Registro, `ReleasePanel`, Incarichi) resta il nome, perché la colonna `.c-who` è larga quanto un nome e «(VID …)» su 500 righe la taglia. ⚠️ Tre trappole chiuse nel componente: `@onclick:stopPropagation` (in Permessi il VID sta dentro una riga che è essa stessa un comando), **niente render mode** (metà dei punti sono SSR statici, dove un `@onclick` non farebbe nulla in silenzio) e `.vid-link` nominato a parte nel foglio di **stampa**, perché due classi battono `.vipi-root a`. Cade `Stats_Subtitle`: una frase già formattata non lascia spezzare il `{1}` in markup. ⚠️ **La verifica live ha trovato un buco vero**: nel Registro **nove VID a schermo e zero link**, perché lì il VID sta dentro le frasi del narratore — non è un campo, è una **parola** — e nessuna prova guardava quella colonna. Chiuso con un secondo componente, **`VidText`**, che taglia la frase già composta sulla forma «VID 1234567» ed emette il resto come **testo** (niente `MarkupString`: quelle frasi portano titoli e note scritti da persone), e che aggancia anche il «Deciso da …» di Sorgenti e l'«Assegnato da …» di Incarichi, dati per irrisolvibili. ⚠️ La forma tagliata dipende da una risorsa tradotta, quindi una prova legge i `.resx` dal disco: ritradurre `Audit_VidN` spegnerebbe la feature in silenzio. Resta aperta solo la **Guida**, che il gesto non lo nomina. |
| [feature/2026-08-25-vipi-aeroporto-legata-allo-scalo.md](feature/2026-08-25-vipi-aeroporto-legata-allo-scalo.md) | ✅ **CHIUSO, verifica live eseguita** (LIBG generato, aperto e **pubblicato**: «Release published now», un documento solo invece di quattro). **La vIPI d'aeroporto è dell'AEROPORTO, non di un suo settore.** Il documento si agganciava ai settori dello scalo: regge finché c'è una torre, e **LIBG (Taranto Grottaglie) su IVAO ha solo `LIBG_APP` non remotizzato** — che ha un documento suo e per regola non può portare quello dell'aeroporto. Risultato misurato: documento legato a nessuno, il ramo «esistente» che lo cercava *attraverso gli stessi settori* non lo ritrovava, **quattro bozze orfane in un minuto**, e la pubblicazione che rispondeva «crea prima il documento» a chi l'aveva appena creato quattro volte. ⓘ Il segnale era già a schermo: il badge **«no TWR»**, che il chip conta come 11 aeroporti. Ora `Airport.DocumentId` uno-a-uno con **indice UNICO** (due scali sullo stesso documento sarebbero un difetto visibile mesi dopo, come un aeroporto che mostra le piste di un altro); APP e vLOA restano legati ai **settori**, che è ciò che descrivono. I settori d'aeroporto restano legati allo stesso documento come **proiezione**, e il rebuild riallinea anche quelli comparsi DOPO la prima generazione. I documenti già scritti ci arrivano con `LinkAirportDocumentsAsync` all'avvio, idempotente: ⚠️ deve escludere l'APP non remotizzato o lo scalo mostrerebbe la documentazione del proprio avvicinamento — misurato, 5 aeroporti collegati e LIBA andato al proprio documento (18) invece che ad «Amendola Approach» (3). ⚠️ `TryDescribe` lavora su un Document già caricato: senza `.Include(d => d.Airport)` l'ICAO esce vuoto **senza dare errore**. |
| [feature/2026-08-25-aeroporti-militari.md](feature/2026-08-25-aeroporti-militari.md) | ✅ **CHIUSO, verifica live eseguita — dal 25 agosto sul ramo `statistiche-atc` insieme alle statistiche (merge `8b76352`), NON ancora in `main`** (bottone vero dell'anagrafica premuto con le credenziali IVAO reali su una copia del `vipi.db`: **34 aeroporti** passati a presenza militare, 55 con IATA, 93 con la quota). **Quali aeroporti hanno una base militare — il dato c'era già.** `/v2/airports` porta un campo `military` che scartavamo: **34 su 221** in Italia, tutti già in archivio. ⚠️ **Non vuol dire «aeroporto militare»**: è vero anche per **Linate, Pisa, Ciampino, Catania, Elmas, Lamezia e Rimini** — scali civili con sedime militare, sette dei trentaquattro. Da qui **due campi e non uno**: `HasMilitaryPresence` è della sorgente e si riscrive a ogni giro, `IsMilitaryOnly` è un giudizio e lo dà un amministratore (l'import non lo tocca mai, salvo azzerarlo quando cade la presenza che lo rende possibile). ⚠️ **La trappola**: `AutoAssignAirportsAsync` è additiva — salta gli ICAO già presenti — quindi le cinque colonne nuove sarebbero nate al loro default su tutti e 93 gli aeroporti esistenti **e nessun giro sarebbe mai passato a riempirle**; ora lo stesso giro chiama `SyncAirportSourceFieldsAsync` su tutto l'elenco e conta a parte gli aggiornati. Nello stesso colpo IATA (⚠️ la sorgente manda stringa **vuota**, non null, per 73 aeroporti), quota e variazione magnetica (`double`: la sorgente manda int e float nella stessa pagina). A schermo due etichette distinte in admin, nell'elenco pubblico e in testata al documento, più i chip di filtro. In coda: **due difetti visti solo a schermo** — la pastiglia più un quarto tasto spingeva il cestino oltre il bordo, e nella cella ICAO andava a capo alzando la riga di 40px; la forma buona è **la pastiglia che È il comando** — e la misura che assolve il terzo: lo sforo residuo (17px a 1600, 171px a 1280) è **identico** su righe militari e civili, quindi è il §H3 già aperto. |
| [feature/2026-08-25-biblioteca-allegati.md](feature/2026-08-25-biblioteca-allegati.md) | ✅ **TUTTE E NOVE LE SLICE FATTE, FUSA in `main` il 30 agosto 2026** (merge `cdeeb163`) **e R8 CHIUSO**: l'embed di Drive provato dal vivo con due PDF veri — il documento si vede davvero nel riquadro. ⚠️ **E ha trovato che in `frame-src` mancava `'self'`**: l'iframe punta alla NOSTRA rotta `/vsop/files/{slug}`, non a Drive — è tutto il senso del disegno — e `frame-src` guarda l'indirizzo a cui il riquadro **naviga**, che al primo passo è il nostro. Oggi non si vedeva perché l'intestazione è **Report-Only**; alla CSP vera il riquadro sarebbe rimasto **vuoto**. ⚠️ **E il presidio asseriva la direttiva incompleta**, quindi passava. ⚠️ Secondo difetto, già in produzione: la colonna versione stampava **`v@r.VersionNumber`** alla lettera — la regola di Razor per gli **indirizzi email**, unico caso in tutta la UI. ⚠️ **Lo slug proposto non si cambia col TRIPLO CLIC** (lascia il vecchio attaccato al nuovo); con Ctrl+A o Backspace sì — è il comportamento dei campi «controllati», **19 file**, e non è della biblioteca. **Biblioteca allegati**: i PDF si caricano in un posto solo e si citano dai documenti (blocco «Allegato» + link inline nella prosa). ⚠️ **I byte non stanno da noi**: il piano di hosting non ammette il formato PDF (vincolo **contrattuale**, non aggirabile mettendoli in MariaDB) e IVAO HQ indica il **Drive di divisione** — quindi tutto ciò che entra in biblioteca è **pubblico**, niente allegati riservati. Modello a **due livelli** (`Attachment` slug stabile → `AttachmentVersion` → file sul deposito): i documenti citano lo **slug**, così sostituire un PDF è spostare un puntatore e non riaprire dodici documenti. ⚠️ L'id del file sta **solo sulla versione** — la carta lo metteva anche sulla voce, e due posti che dicono la stessa cosa un giorno dicono cose diverse; di conseguenza una voce **nasce con la v1**. L'identità del link è **nostra** (`/vsop/files/{slug}` → **302** + ⚠️ **`no-cache`**, non `immutable` come le immagini: lì l'URL È il contenuto, qui l'URL è stabile e il contenuto cambia sotto). Il registro «chi cita cosa» si **ricava** dalle quattro fonti, non da una tabella di join che si desincronizza — ⚠️ e una **release non porta un `DocumentId`**: si attribuisce per *(tipo, chiave)*, o restano senza nome proprio le citazioni pubblicate. Il link segue **sempre** la versione corrente. Blocco in due modi, **Link** e **Incorporato** (iframe sulla nostra rotta, ⚠️ **`frame-src` in CSP** o funziona oggi e muore al passaggio a CSP vera, perché l'intestazione è Report-Only). ⚠️ `MarkdownLite` conosce **un solo** link, lo schema `allegato:`, con lo **slug vincolato alla sua forma**. Sostituzione ed eliminazione con **conferma informata** → riga nella **casella degli impatti** (⚠️ **non** in «Cambiamenti», che è pubblica e si ricava dal ciclo AIRAC). ⚠️ **Enum in coda**: nel payload di release sono ordinali. ⚠️ **Resta R8**: l'embed di Drive **mai provato dal vivo**. |
| [feature/2026-08-26-eliminare-con-le-protezioni.md](feature/2026-08-26-eliminare-con-le-protezioni.md) | ✅ **ESEGUITA il 26 agosto sul ramo `statistiche-atc`, provata sui dati veri** (copia del `vipi.db`: un settore e un aeroporto eliminati DAVVERO dall’interfaccia, 321→320 e 93‒92, audit col nome dentro). **Eliminare, con le protezioni.** Prima: un settore non aveva **nessun** tasto (`DeleteSectorAsync` esisteva in tre strati senza chiamanti nell’UI) e un aeroporto con settori non si cancellava — l’unica via passava dagli orfani, una pagina alla volta. Ora un motore solo, `IDeletionService`, con **anteprima**: dice cosa sparisce, cosa si sposta, cosa resta da ripubblicare e chi blocca, e l’esecuzione **ricalcola** il piano prima di agire. Le politiche stanno in `DeletionRules`, pure e senza IO: figli al **nonno** (radice → figli radici); documento/parte vLOA/blocco → se resta ancorato altrove si sgancia e si marca **da rivedere**, se è l’ultimo aggancio si **blocca**; i blocchi hanno **tre vie** (estremo di un da→a = falso, muore; solo ambito = resta sganciato); gli accordi **bloccano sempre** dicendo quali; la **torre** cade solo con lo scalo. ⚠️ **Si elimina solo ciò che la sorgente non manda da due giri**, e per renderlo calcolabile sono serviti tre pezzi: `ImportState.PrevSuccessUtc`, il timbro che mancava sugli aeroporti (`LastSeenAtUtc`) e i giri **a mano** che ora timbrano. ⚠️ La ACC **non** cascada: «svuotala prima». In più la pagina **/admin/pending**, che assorbe la casella degli impatti e dice chi ha già maturato il diritto a essere eliminato e chi no. Difetto trovato solo a schermo: con sedici accordi in elenco i tasti finivano fuori dalla finestra — ora sono `sticky`. ⚠️ **E rileggendo sono usciti due difetti più grossi del tasto che mancava**: il riaggancio dei figli si scriveva solo nella proiezione e durava **una notte** (la proiezione lo ricalcola dal catalogo a ogni sync), e **nessun documento vero si cancellava** — due vincoli del corpo sono `RESTRICT` e ogni vIPI ha nove sezioni figlie su dieci, quindi la pagina Documenti rispondeva `FOREIGN KEY constraint failed`, difetto **preesistente** che si nascondeva nei test perché le righe appena create sono tracciate. In coda: retention delle **sessioni ATC** a 12 mesi con `AtcMonthRollup` (21 275 → 5 848 righe), eliminazione di **candidati confinanti** e **aree regolamentate**, l’avviso sugli **incarichi appesi**, e §§17-18 con lo **stato dell’archivio** da cui ripartire dopo la pulizia del database. |

| [feature/2026-08-26-chiedere-alla-sorgente.md](feature/2026-08-26-chiedere-alla-sorgente.md) | ✅ **ESEGUITA il 26 agosto sera sul ramo `statistiche-atc`, provata contro IVAO vero.** **Chiedere alla sorgente, invece di aspettarla.** La regola D8 dell’eliminazione fa aspettare **due giri d’import** prima di poter togliere qualcosa: è un modo di *dedurre* l’assenza dal silenzio. Ora la finestra offre di **chiederlo in puntuale**, e la constatazione batte la deduzione. ⚠️ **Non è «chiama l’API e guarda se torna null»**: `IvaoHttp` riduce *ogni* risposta non-2xx a `null`, quindi un 401 e un 404 arrivano identici — letto come «sparito», un’ora storta di IVAO diventerebbe il permesso di svuotare il catalogo. Serve una porta nuova (`ISourcePresenceProbe`) che guardi lo **status**, con verdetto a **tre** valori (assente / presente / **non si sa**) e **due chiamate**: la puntuale più una **controprova** sull’elenco del contenitore, che deve rispondere **e nominare altro** — un elenco vuoto è la stessa ambiguità per cui i due giri esistono. Scioglie **solo D8**: accordi, ultimo aggancio, torre e ACC piena reggono. La prova si **rifà al `DELETE`** (il flag è un *ordine di chiedere*, non un verdetto) e le sue tracce finiscono nell’audit. ⚠️ Urgente adesso: quando il committente ripulisce il DB sparisce anche `ImportState`, e senza questo **per due giri non si potrebbe eliminare niente**. Provata a schermo sui nove orfani veri: **otto IVAO li manda ancora** (sono nascosti nel *nostro* catalogo, non spariti dalla sorgente — distinzione prima illeggibile), uno solo, `LIED_G_APP`, è sparito davvero e il tasto si è acceso. |
| [feature/2026-08-26-da-fare-una-lista-sola.md](feature/2026-08-26-da-fare-una-lista-sola.md) | ✅ **ESEGUITA il 26 agosto sera sul ramo `statistiche-atc`, provata a schermo.** **«Da fare»: una lista sola.** L'elenco «Documenti da rivedere» era una tabella di **sola lettura** — niente link, niente azioni — e gli incarichi vivevano in un'altra pagina: chi apriva un editor vedeva metà del lavoro che lo riguardava. Ora `/services/vsop/tasks` è **«Da fare»**: le segnalazioni che il sistema ha aperto sui documenti che puoi modificare E i tuoi incarichi, in una lista sola ordinata per **urgenza**, ciascuno col collegamento all'editor giusto. ⚠️ **Nessun terzo meccanismo**: `DocumentImpact` ed `EditorTask` restano quello che sono e la lista è un **read-model** (`WorkItem`) che li legge — la regola §1 del FEATURE-PROCESS dice estendi o sostituisci, mai affiancare. ⚠️ **Le righe calcolate non si spuntano**: su «la copia pubblicata è indietro» il tasto è **Ripubblica**, che rende il fatto falso; un ✓ sarebbe un ping-pong col giro notturno. Lo **stesso componente** rende la riga in tre posti (elenco, banner in cima all'editor, «Da sistemare»). L'unica riga di schema è il ponte `EditorTask.FromImpactId` (una migrazione ×2 → **quattordici** in coda al cutover), che evita di mostrare due volte un lavoro **preso in carico**. ⚠️ **L'esempio del committente funzionava già**: `ImpactDriftUseCase` confronta il RISULTATO e non la causa, quindi un coordinamento cambiato apre da sé due righe «ripubblica» su due documenti — verificato dal vivo su `vLOA LIBB↔LGGG` e `vLOA LIBB↔LYBA`. Trovato per strada un rosso **non** di questo giro: un test affermava «10 giorni» contro un fixture fisso mentre il codice conta su `UtcNow`. |
| [feature/2026-08-26-identita-dei-settori.md](feature/2026-08-26-identita-dei-settori.md) | ✅ **ESEGUITA il 26 agosto sul ramo `identita-settori`, verifica live sul `vipi.db` reale.** **L'identità di un settore non è il suo nome.** I cataloghi erano chiavati sul **callsign** e l'import è additivo: quando IVAO rinominava una posizione il catalogo **aggiungeva** una riga, la proiezione creava un `Sector` **nuovo e vuoto**, e il vecchio restava **attivo** col documento, l'AoR, gli accordi e i figli addosso — due settori dove ce n'è uno. ⚠️ **L'id c'era già e lo scartavamo**: `/v2/centers/{ACC}/subcenters` e `/v2/airports/{ICAO}/ATCPositions` portano un `id` numerico **già nella lista**, quindi senza una chiamata in più. Misurato contro l'API vera: non è indirizzabile (`/v2/subcenters/1171` → `400 "Callsign is in the wrong format"`), vive **per tipo** (subcenter 1171–3916, posizioni 3398–24707), e IVAO **aggiorna le righe in posto** (145 su 192 hanno `updatedAt ≠ createdAt`; `LIRQ_TWR` è lo stesso id dal 2020 al 2026). Ora `AccSector.IvaoId`/`AirportSector.IvaoId` reggono l'upsert e **il callsign è un attributo**: una rinomina è un `UPDATE`. ⚠️ **E l'euristica muore, perché era sbagliata**: il caso vero del 22 agosto — `LIRR_NE1_CTR` nato accanto a `LIRR_NE_CTR` con la **stessa frequenza** (124.2) e lo stesso nome IVAO — è uno **sdoppiamento**, e `FindRenameCandidateAsync` l'avrebbe chiamato rinomina spostandoci sopra il documento. Via lei, `RenameCandidate`, i campi `Position`/`Scope` di `StaleCatalogRow` e la chiave `Orph_MaybeRenamed`. Un motore solo (`ICallsignRenameService`, sullo stampo di `IDeletionService`) chiamato **in cima** ai due upsert, così l'upsert per callsign ritrova le righe al loro posto. ⚠️ **L'inventario dei posti da riscrivere non è a occhio**: viene da una spazzata su **ogni** colonna testuale del `vipi.db` vero, e ha trovato quel che nessuno cercava — **35 righe di `ContentBlock.BodyJson`** con puntatori per nominativo (`Callsigns`, `MemberCallsigns`, `OpenCallsigns`: le configurazioni AoR e i gruppi APP). Si riscrive camminando il JSON, mai cerca-e-sostituisci: `LIRR_NE_CTR` è **prefisso** di `LIRR_NE1_CTR`. Restano intatte le colonne che sono **storia** (21 267 `AtcSessions.Callsign`, il registro), ed è per quelle che c'è `CallsignAlias` + `CallsignHistory`, che traduce **in lettura** — con il taglio ai primi N che esce dal database, perché due metà che si fondono entrano fra le prime solo **dopo** essere state sommate. Un nominativo già occupato **non si forza**: si riferisce e decide una persona. **Verifica live** (payload veri, copia del db di produzione): backfill **37/37** e **192/192**, **zero** alias inventati; rinomina di `LIRR_NE_CTR` → `sectorId=22` invariato, 6 figli · 1 accordo · 6 padri · 5 blocchi al loro posto, 582 sessioni col nome di allora, 13 righe di catalogo (erano 13), e in statistica una riga sola. ✅ **In `main`** (ramo fuso e cancellato). |
| [feature/2026-08-26-lassenza-non-cancella.md](feature/2026-08-26-lassenza-non-cancella.md) | ✅ **ESEGUITA il 26 agosto sul ramo `identita-settori`, misurata sul `vipi.db` reale.** **L'assenza non cancella la presenza.** La sorgente risponde `regionMapPolygon: []` — **tutte e 229** le righe italiane, verificato contro l'API vera — e i tre upsert di catalogo lo prendevano per una forma, scrivendolo sopra quella che avevamo. ⚠️ **Le shape delle TWR non sono mai state di IVAO, ed è voluto** (correzione del committente): sono nostre — GitHub + cerchio da 5 NM — ed è per questo che quella catena esiste; la correzione serve a **proteggerle**. ⚠️ Quel che invece è cambiato davvero **non riguarda le torri**: fra il 17 e il 26 agosto **59 APP** e **27 CTR/FSS** sono passati da poligono vero a vuoto, e **per loro non esiste nessun ripiego** — da chiarire se sia anch'esso voluto. Misurato applicando l'import vero a una copia della produzione: **83 poligoni su 83 azzerati in un solo giro** (66 reali presi da **GitHub**, non da IVAO, e 17 cerchi di ripiego), 142 righe lasciate con `"[]"`. ⚠️ **Non se n'era accorto nessuno perché il giro notturno lo ripara da sé** — l'ordine è import → GitHub → cerchi — ma **gli altri tre chiamanti di `ImportAsync` i ripieghi non li rieseguono**: premere «importa dalla sorgente» nell'editor di un aeroporto toglieva l'area alla sua TWR fino al giorno dopo, e un GitHub irraggiungibile durante il giro avrebbe degradato i 66 poligoni reali a cerchi. ⓘ La regola c'era già, scritta due volte (aree regolamentate, confinanti), ma come `is not null` — e un array vuoto quel controllo lo passa. Ora `PolygonGeometry.IsEmptyShape` risponde a **una domanda sola: «la sorgente mi ha dato qualcosa?»**. ⚠️ **Chiede se è vuoto, non se è valido**, e la differenza è voluta: la prima stesura usava `ParsePoints(...).Count >= 3`, cioè un **validatore**, che il giorno in cui la sorgente manda un formato che il parser non conosce lo butterebbe via **in silenzio** tenendosi il vecchio — giudicare se una shape si disegna è di chi la disegna, e quelli hanno già il loro piano B (un test presidia la scelta: un poligono di due punti non si proietta ma non è un'assenza). `IsShapeSynthetic` si azzera **solo** con una shape vera, così un cerchio resta rimpiazzabile da GitHub. In coda la migrazione **`ShapeVuoteANull`** (×2) per le **207** righe già sporche — 148 ACC + 59 aeroporto — che non erano innocue: `HasPolygon` guarda `!= null`, quindi a schermo dicevano «ha un poligono». Provata su copia della produzione: 207 vuoti → 0, 88 poligoni veri intatti, nessuna riga persa. ⚠️ Un test esistente asseriva `Equal("[]", …)`: presidiava il **modo in cui l'assenza era scritta**, non l'intenzione; ora dice `Null`. **Resta aperto** il perché IVAO non le mandi più — se tornassero, l'upsert le riprenderebbe da sé. |
| [feature/2026-08-26-shape-dal-sectorfile.md](feature/2026-08-26-shape-dal-sectorfile.md) | ✅ **ESEGUITA il 26 agosto sul ramo `identita-settori`**, provata sui file veri e su copia della produzione. **Le shape dal sectorfile, e il ciclo che non è ancora uscito.** L'anagrafica IVAO non dà più i poligoni, e il sectorfile Aurora li ha tutti — ma è il file che scriviamo **noi, in anticipo sul ciclo AIRAC**. **Tre sorgenti, in ordine scritto una volta sola**: anagrafica IVAO (nessun gate), sectorfile come **ripiego** e solo quando la prima tace (**gate AIRAC**), cerchio sintetico per le sole TWR. ⓘ Il tracker resta fuori ma se un giorno entra non ha bisogno del gate. **(1) Il parser** regge i file di settore: intestazioni con **più callsign** (una shape serve più enti) e vertici che sono **nomi di punto**; il catalogo navaid cresce di due campi — le coordinate — come il suo stesso commento diceva di fare. ⚠️ **Un punto che non si risolve invalida l'anello INTERO**: saltarlo darebbe un poligono sbagliato che si disegna benissimo e mente. ⚠️ **Il secondo separatore l'ha trovato solo la prova sui file veri** — 16 intestazioni usano lo spazio, 3 i **due punti** — e leggendo solo lo spazio quattro settori di Milano restavano senza area, senza un errore da nessuna parte. Misure: **128 callsign** con anello, zero degeneri. ⚠️ I 2 blocchi scartati (`GODRA`/`GIGUS`) erano **un difetto nostro**, non punti mancanti — vedi il §9 della carta. **(2) Il ripiego** (`SectorShapeFallbackService`, gemello di quello TWR): quali file leggere lo dice **`ITALY.isc`**, l'indice che carica Aurora stessa, non un elenco nostro. Non sovrascrive mai l'anagrafica; ⚠️ ma **aggiorna quel che ha scritto lui** — la prima stesura riempiva solo i vuoti e faceva del sectorfile una sorgente *write-once*. Su copia della produzione: da **5 a 115** aree, **110 applicate**, secondo giro 0. **(3) Il gate AIRAC**: la sezione `aor` è già `Frozen` su tutte e 26 le occorrenze, quindi il buco non era la lettura ma la **pubblicazione**. La domanda giusta la pone la release, che il suo ciclo lo sa già: «qual è la shape in vigore **al ciclo di QUESTA release**?». Quattro colonne per catalogo (corrente · in vigore · ciclo d'entrata · provenienza); ⚠️ **la prima shape non si differisce mai** (nessuna area per 28 giorni è peggio di una in anticipo). Provato: editor → la ridisegnata, release per il **2608** → quella di prima, release per il **2609** → la ridisegnata. ⚠️ **Trappola presa al volo**: lo scaffolding emetteva `defaultValue: ""` per l'enum, che avrebbe reso illeggibile ogni riga già in tabella. ✅ **Chiuso il 26 a notte anche l'avviso a schermo** (§7): il **pannello release** elenca le aree che resterebbero indietro e offre «Pubblica comunque le aree nuove» — stessa domanda del congelamento (`ShapeAiracGate.IsDeferredAt`), due cicli guardati perché i tasti sono due, e forzare **non** scrive «in vigore» (la promozione notturna spegne la forzatura da sé). ✅ **E i ripieghi valgono solo per gli enti della divisione** (§8, decisione del committente): le aree degli ATC esteri le dà IVAO, se ce le dà. ⚠️ **Il catalogo dei punti leggeva TRE file su OTTO** (§9): `GODRA`/`GIGUS` stavano in `NAVAIDS/ESTERNI.fix`, che non scaricavamo perché la configurazione elencava tre file **scritti a mano** mentre `ITALY.isc` ne cita otto. Ora l'elenco lo dà l'indice, come già per i file di settore: **1385 → 3732** nomi in catalogo e i blocchi di Milano chiudono l'anello — gli irrisolti erano **tre**, non due (c'era anche `GEMLA`): il difetto era la lista, non i nomi. ✅ **In `main`** (ramo fuso e cancellato). |
| [feature/2026-08-24-tessere-mappa-ritenti.md](feature/2026-08-24-tessere-mappa-ritenti.md) | ✅ **CHIUSA il 24 agosto.** **Mappe a scacchi alla prima apertura**: segnalato dalla produzione «a volte la mappa si carica male, refresho ed è a posto». Il documento LIBB porta **77 mappe Leaflet** (AoR, minime, e le decine di mappine delle aree regolamentate) che chiedono le tessere tutte insieme: quelle che il server rifiuta non venivano mai richieste di nuovo, e restava il fondo a scacchi. ⚠️ **Aggiornamento 27 agosto**: le **77 mappine** citate qui non esistono più — le aree regolamentate hanno una mappa sola (vedi la carta del 27). ✅ **Aggiornamento 27 agosto sera**: il fondo aveva iniziato a chiedere una **API key** — le tessere arrivavano stampigliate «API KEY REQUIRED» — e ora non è più CARTO: vedi la carta *Il fondo delle mappe non è più CARTO*. ⚠️ La lezione da tenere è qui: quel guasto **le reti di questa carta non potevano vederlo**, perché la tessera arrivava — con la scritta sopra — ed era un'immagine valida. |
| [feature/2026-08-25-documenti-da-rivedere.md](feature/2026-08-25-documenti-da-rivedere.md) | ✅ **ESEGUITA il 25 agosto sul ramo `statistiche-atc`**, provata sui dati veri. **La casella degli impatti**: una riga per **fatto a monte** (non per documento), banner multi-riga nell'editor e sezione **Orfani**. Nasce dall'analisi «cosa succede se un dato importato sparisce» e ne assorbe tre dei sette fix. ⚠️ La sentinella «aperta» è l'**epoca Unix** perché il `DATETIME` di MariaDB parte dal 1000, e l'ancora è il **DocumentId**: la chiave di release è instabile. |
| [feature/2026-08-26-ordine-sezioni-personalizzato.md](feature/2026-08-26-ordine-sezioni-personalizzato.md) | ✅ **ESEGUITA il 26 agosto sul ramo `identita-settori`**, verifica live sulle tre famiglie. **L'ordine delle sezioni è una scelta editoriale**: le sezioni di **catalogo** si spostano su e giù dentro il loro gruppo — restano non rinominabili e non eliminabili, **è l'ordine a non essere del catalogo, non l'identità**. Il motore c'era già (`MoveSectionAsync` scambia l'`Order` fra fratelli, versionato e catturato nello snapshot): mancava il tasto, perché l'editor condiviso legava «obbligatoria» a **tre divieti insieme**. Accanto al titolo, di quanti posti si è allontanata dallo standard (`SectionOrdering`, funzione pura): `↑2`, `↓1`. ⚠️ Si contano solo le sezioni **fisse** e solo quelle **presenti**, o una sezione libera in testa farebbe apparire uno scostamento su tutte le altre. ⚠️ **Difetto trovato strada facendo** (trappola del doc 11 §8): il viewer vLOA rendeva le due **direzioni** dei coordinamenti in una sequenza **scritta nel codice** — spostarle nell'editor avrebbe cambiato l'editor e **non** il pubblicato. In coda: si riordinano anche i **blocchi** della vIPI ACC, ma i settori di **Aerovia restano in testa** (regola in `MoveGroupAsync`, che li riconosce dal blockmeta e non dal posto). ⚠️ L'editor **aeroporto non partecipava**: non montava `DocumentSectionsEditor` e non aveva catalogo sezioni — **superato la notte stessa**, vedi la carta *l'aeroporto entra nel catalogo*. ✅ **In `main`** (ramo fuso e cancellato). |
| [feature/2026-08-26-riordino-sezioni-trascinando.md](feature/2026-08-26-riordino-sezioni-trascinando.md) | ✅ **ESEGUITA il 26 agosto sul ramo `identita-settori`**, verifica live con browser vero sulle tre famiglie (vIPI ACC, vIPI APP, vLOA). **Le sezioni si riordinano trascinandole nel menu Navigazione**: le frecce del giro precedente stanno sulla card, in una pagina alta migliaia di pixel — portare una sezione in cima sono otto pressioni, e a ogni pressione la sezione **esce dallo schermo**. Il menu è l'unico posto dove l'ordine si vede **tutto insieme**; le frecce restano (tastiera, tocco). **La regola a schermo è una sola**: *la sezione lasciata prende il posto di quella su cui la si lascia* — i due riferimenti diversi secondo il verso li calcola `SectionOrdering.TryDropOnto` (funzione pura), e su fratelli **adiacenti** l'esito è identico a quello della freccia. Motore nuovo perché lo scambio ±1 non salta N posti: `MoveSectionBeforeAsync(sezione, prima-di?)` reinserisce e **rinumera il gruppo**. ⚠️ Il vincolo «solo dentro il suo gruppo» sta nel **motore**, non nella UI: il riferimento dev'essere un **fratello**, altrimenti la mossa non avviene — è ciò che impedisce a un riordino di diventare una **riparentazione silenziosa**, che cambierebbe il *significato* di una sezione e non la sua posizione. ⚠️ `draggable="false"` va scritto **esplicitamente**: un `<a href>` nasce trascinabile per conto suo, e senza quell'attributo la voce del pannello Release si lascia prendere per poi non fare niente. ⚠️ Il trascinamento è **opt-in dell'host** (`EditorToc.OnReorder`): non passarlo lascia il pannello identico a prima, **senza gestori registrati sul circuito** — è così che l'editor aeroporto resta fuori senza una condizione dedicata, e fuori dalla modifica l'indice resta un indice. 15 test nuovi (5 puri, 2 sul repository, 8 bUnit). **Non fa, deciso**: i **blocchi** ACC non si trascinano (nel menu sono intestazioni, e hanno la regola dell'Aerovia in testa) e le **sotto-sezioni** nemmeno. 🔴 **§8, 27 agosto: col MOUSE non ha mai funzionato**, su nessuna delle tre famiglie. `@ondragover:preventDefault` sulla voce era **lettera morta** — Blazor installa il proprio listener globale per un evento solo quando un componente vi registra un **gestore**, e per `dragover` non ce n'era; senza quel `preventDefault` il bersaglio non accetta e il browser annulla il gesto **senza errori e senza segni** (`dragstart` e `dragenter` arrivavano, la destinazione si illuminava, il `drop` mai). Cura: `wireTocDrop` in `vipi-ui.js`, un listener in cattura installato una volta — scartato il gestore `@ondragover` finto, perché `dragover` scatta a ogni movimento del mouse. ⚠️ **E la lezione vale oltre il difetto**: né gli otto test bUnit né la verifica live guardavano il pezzo rotto, perché **fabbricavano** gli eventi (`DragStart()` + `Drop()` sui gestori; `new DragEvent(...)` nello script) — dispacciando da sé proprio il `drop` che nella realtà non arrivava. **Un gesto del browser si prova col browser che lo fa**: CDP `Input.setInterceptDrags`, headful. |
| [feature/2026-08-27-chip-morte-pagina-statica.md](feature/2026-08-27-chip-morte-pagina-statica.md) | ✅ **ESEGUITA il 27 agosto sera**, su segnalazione del committente. **Le chip che non facevano niente**: METAR/TAF e il selettore di pista delle SID, sulla vIPI d'aeroporto. Due sintomi, **una radice** — la pagina è diventata **SSR statica** (doc 14 §3g) e lo stato dei suoi comandi è rimasto dov'era. ⚠️ **Il meteo**: `AirportWeather` non è mai stato promosso a isola, quindi i suoi `@onclick` uscivano in HTML come bottoni **senza un solo attributo Blazor** — il clic non partiva nemmeno, e non lasciava traccia da nessuna parte. ⚠️ **Le SID**: lì l'isola c'era e il clic **arrivava al server** (visto sul WebSocket, con tanto di `RenderBatch` di ritorno), ma lo stato della scelta viveva nel **genitore statico** — il gestore aggiornava il campo di una pagina che non si ridisegna più, e l'isola si riprendeva il valore congelato nel descrittore. La regola che ne resta: *uno stato che cambia deve vivere dentro l'isola che lo cambia; un genitore statico può solo **seminarlo***. ⚠️ **Perché i test non l'hanno visto**: le cinque prove sulle SID montavano il componente con la pista **già scelta** — chip premute: zero — e **bUnit ignora i render mode**, quindi la prova del meteo sarebbe passata anche col difetto. Il render mode è un fatto dell'**hosting**: l'unica rete che lo vede è il browser. Aggiunti 4 test (652). Costo del meteo interattivo: **+2 806 byte** di descrittore, e **nessun circuito in più** — `<LiveBadge>` ne apre già uno su ogni pagina. Propagazione: chiusura transitiva sulle undici pagine pubbliche statiche, **nessun altro comando muto** (i quattro candidati hanno i gestori dentro il ramo `Editing`). ✅ **In `main` dal 28 agosto** (`1c15f81`, ramo cancellato). |
| [feature/2026-08-27-basemap-esri.md](feature/2026-08-27-basemap-esri.md) | ✅ **ESEGUITA il 27 agosto sera.** **Il fondo delle mappe non è più CARTO.** Le tessere di `basemaps.cartocdn.com` arrivavano stampigliate **«API KEY REQUIRED»**, in produzione, su **tutte** le mappe. ⚠️ **Il ritentatore e lo spazzino non potevano accorgersene**: guardano la tessera che *non arriva*, e questa arrivava — `200`, immagine valida, con la scritta sopra. *Un fornitore che chiude il rubinetto non si vede dal codice della risposta.* Ora il fondo è **Esri «Light Gray Canvas»**, su `server.arcgisonline.com`, che **non è un host nuovo**: è già quello del rilievo delle minime. ⚠️ **Due fogli, non uno** — in Esri il fondo è **muto** e i nomi stanno in `…Reference`; col solo primo si otteneva un'Italia senza il nome di una città, e nessun test l'avrebbe detto. ⚠️ **Indirizzo `{z}/{y}/{x}`**, riga prima della colonna: invertirlo non dà errore, dà **un altro pezzo di mondo** sotto i settori. ⚠️ `maxNativeZoom: 16` e non `maxZoom`, o il fondo sparisce appena si ingrandisce. ℹ️ Sparisce l'`@2x` (Esri non ce l'ha): nel 2D il fondo è appena più morbido sugli schermi fini, nel 3D **non cambia quasi nulla** — la texture era già 256 px per casella, quindi la tessera doppia veniva rimpicciolita: era supersampling, non risoluzione. Nel 3D le etichette si dipingono **dopo** il fondo della stessa casella, o un `Reference` veloce finirebbe coperto. ⚠️ **Trovato per strada**: la CSP `img-src` nominava **solo CARTO** mentre le minime chiedono tessere ad altri due host — invisibile perché l'intestazione è **Report-Only**. 🔵 Resta aperta la **categoria**: Esri è gratuito e senza contratto come CARTO fino a ieri; l'unica strada che non si ripresenta sono le **tessere nostre**. ✅ **In `main` dal 28 agosto** (`95e4227`, ramo cancellato). |
| [feature/2026-08-27-aree-regolamentate-una-mappa.md](feature/2026-08-27-aree-regolamentate-una-mappa.md) | ✅ **ESEGUITA il 27 agosto sera sul ramo `riordino-e-aree`**, verifica live su LIBB (due sezioni, 69 e 4 aree), LIRR (**105 aree**) e un APP seminato dall'editor vero. **Aree regolamentate: una mappa sola, con le chip.** C'era un `<details>` per area **con la propria mappina Leaflet** — 105 contenitori mappa su LIRR, 69 su LIBB, cioè centinaia di tessere per disegnare cento volte lo stesso pezzo di Mediterraneo (è il caso che il commento sull'accensione a scaglioni di `vipi-aor.js` già citava) — e il difetto peggiore non era il costo: con una mappina per riquadro **due aree non si possono confrontare**, che è la domanda vera di chi apre quella sezione. Ora una mappa (2D/3D), una chip per area colorata per **tipo**, i preset **R/D/P/TSA/TRA**, e sotto le descrizioni delle **sole aree accese**, chiuse, col conteggio. **Nessun motore di mappa nuovo**: si riusa `AccAor`/`AccAor3d` e la traduzione area→«settore» è `RegulatedAreasMap` (pura). ⚠️ **La chiave è l'id IVAO, non il nome**: i nomi hanno spazi e punti (`LI R301B SMarco in Lamis Bis`) e finiscono dentro `[data-sec="…"]`; il nome sta in `AccSectorAor.Label`, campo nuovo e opzionale. ⚠️ **Le quote delle aree sono in PIEDI** (29 000, 1 500) e il 3D estrude su una banda FL → `AorFlBand.Normalize`, o un'area a 29 000 ft diventa un prisma alto FL 29 000. ⚠️ **I preset per tipo devono stare DENTRO `AccAorView.Configs`**, perché la fila di tasti la disegna `AccAor` leggendo quello: costruirli accanto è come non averli, **ed è successo** — visto solo alla prima prova dal vivo, con 105 chip e nessun filtro. Il legame chip→descrizione sta **dentro `setSec`** in `vipi-aor.js`, così ogni strada (chip, Tutti/Nessuno, preset, 2D o 3D) porta con sé la riga; scope per `data-aor`, col suffisso `-3d` tolto. **Colori**: R rosso, D giallo, P viola, TSA blu, TRA verde — ⚠️ per **TSA e TRA un colore ufficiale non esiste** (non sono aree ICAO Annex 4, sono costrutti FUA): blu e verde sono la convenzione europea, e si cambiano in un posto solo. ⚠️ **La vLOA non c'entra**: lì `regulated` è **Editorial** nel catalogo, un paragrafo di prosa sul coordinamento delle aree militari transfrontaliere. **Niente migrazioni e niente ripubblicazione**: le aree sono sempre **live** dai cataloghi, gli id li porta il documento. Tolto per propagazione `.area-map`/`.area-noshape`, `PRINT_AREA_MAP_H` e il ramo `isArea` di `resizeMaps`; ⚠️ trovato per strada un **doppione** — `.area-wrap`/`.area-svg`/`.area-alt` definite **due volte** nel foglio, a novecento righe di distanza, e vinceva la seconda. In stampa **le chip spente non si stampano**: sulla carta la chip è la **legenda** della mappa, non un comando. 15 test nuovi. ✅ **Fuso in `main`** il 27 agosto (dentro `8e5f640`), ramo cancellato. |
| [feature/2026-08-26-aeroporto-a-sezioni.md](feature/2026-08-26-aeroporto-a-sezioni.md) | ✅ **ESEGUITA il 26 agosto sul ramo `aeroporto-a-sezioni`** (il **terzo** in fila, da `identita-settori`), verifica live sui quattro documenti veri. **La vIPI d'aeroporto entra nel catalogo delle sezioni.** Era l'ultima famiglia fuori dall'asse 08→13, e non le mancava un tasto: le mancava **il documento**. `RebuildDocumentAsync` **cuoceva** le sezioni dalle tabelle del profilo, le riconosceva **per titolo** e le cancellava e ricreava a ogni rigenerazione — con chiavi **casuali**, perché `BlockSection.Airport` non ha una chiave di catalogo e il builder ricadeva su `SectionKeys.NewCustom()` (è il motivo per cui il viewer cercava `Title.Contains("transi")`). Ordine, «nascondi», sotto-sezioni e Live/Frozen stanno **sulla sezione**, e la sezione veniva distrutta. Ora `SectionProfile.Airport` con otto chiavi (⚠️ **fuori** `aor`/`coordination`/`regulated`: l'aeroporto descrive un **luogo**, quelle appartengono a torre e avvicinamento), le sezioni fisse sono **ancore senza corpo** e il contenuto si deriva a view-time e si **congela** alla release come per l'APP. ⚠️ **Nessuna migrazione**: non cambia lo schema, cambia chi scrive. Porta nuova `IsAlwaysLive` — il **meteo** è derivato ma non congelabile (un METAR di un ciclo fa è meteo scaduto). Due conseguenze volute: l'editor adotta **bozza + lock** (obbligato: ogni mutazione del motore condiviso passa da `IEditingService`) e la pagina pubblica **smette** di mostrare piste e frequenze prese dal profilo live. **La prova che conta**: pubblicato e poi cambiato il TORA di una pista, il pubblico resta a 3000 e la bozza dice il valore nuovo. ⚠️ **Difetto creato e chiuso subito**: il METAR spariva dal pubblico, perché la pagina legge lo **snapshot** e quello — per ogni scalo non ripubblicato — è anteriore e non conosce la sezione; con lo stesso difetto `transition` e `runways` uscivano come tabelle generiche. Chiuso con `AirportLegacySections` (**una** mappa titolo→chiave, **due** lettori: la riconciliazione d'avvio e il viewer) e la regola generale *una sezione **sempre live** non è mai parte della verità di uno snapshot*. In coda tre rifiniture: i pannelli che non sono sezioni (**Settori ATC**, **Release**) erano larghi quanto la **pagina** in tre editor su quattro → parametro `AfterSections`; e «**Validità e revisione**» porta ora tre campi che nessuno ricopia — ciclo AIRAC, data e **chi ha premuto Pubblica** (nome, posizione staff, VID) — con la scheda **sopra** e il testo scritto a mano **sotto** (`SectionBodySource.HostAndBlocks`, l'unica sezione con due corpi). ⚠️ Eredità unica: `AirportExtraSection` si droppa **un rilascio dopo**, perché le migrazioni girano all'avvio *prima* delle riconciliazioni. ✅ **In `main`** (ramo fuso e cancellato). |
| [feature/2026-08-27-vsop-militari.md](feature/2026-08-27-vsop-militari.md) | ✅ **ESEGUITA il 28 agosto — tutte e dieci le slice, in `main` (`6644b5e`).** Il primo SOP vero, **LIPI Rivolto**, è caricato in bozza con `tools/Vipi.MilSopLoader` (§9), e la lettura del documento vero ha trovato **tre difetti latenti** che la suite verde non vedeva (§8c-e): le sotto-sezioni di una sezione con scheda uscivano **due volte**, «nascosta» non valeva per le **sotto-sezioni**, e la macchina traduceva **«MARTE» → *MARS*** e **«CHI» → *WHO*** — nomi di punti in un piano di volo, cioè un dato falso. ⚠️ Il filo comune: nessuno si vedeva sul corpus di prova perché quello era fatto di **prosa**, e un documento tecnico è per metà **celle**. **vSOP militari**: i 15 SOP reali hanno *lo stesso indice*, quindi sono un **profilo di catalogo**, non contenuto libero — e non e' quello dell'aeroporto (due chiavi in comune su venti). Documento **separato** legato allo stesso scalo (`Airport.MilDocumentId`, `Sector.MilDocumentId`) con `Document.Edition`, cosi' LIRP pubblica il SOP militare e l'IPI civile su cicli AIRAC diversi — verificato: l'identita' di una release e' gia' `(TargetType, TargetKey)`, zero lavoro. ⚠️ **Dal 28-ago il documento militare nasce in ITALIANO** (§1d): la lingua sorgente e' quella in cui si REDIGE, non quella dei PDF di partenza -- e i titoli delle sezioni cambiano di conseguenza; resta **aperto** portare anche la vLOA allo stesso criterio. **26 sezioni**, sei riusate (`weather`, `frequencies`, `runways`, `transition`, `regulated`, `validity`, `operationaltechnique`), e due riusano anche il **motore**: la mappa AoR con le chip *e' gia'* quello che il PDF disegna a mano. Piu' il **flag audience** pilota/ATC per-sezione su **tutti** i documenti (default `Both` = nulla cambia), con filtro in query string perche' il link filtrato sia condivisibile. ⚠️ `AirportReleaseTarget` e' il **catch-all** dei Document vIPI: senza il controllo su `Edition` ogni documento militare ci finisce dentro in silenzio. ⚠️ `qra` e' contenuto **nuovo** (nei PDF QRA e' solo una colonna, e solo sulle 4 basi di difesa aerea). |
| [feature/2026-08-27-documenti-bilingue.md](feature/2026-08-27-documenti-bilingue.md) | ✅ **ESEGUITA il 28 agosto — tutte e dieci le slice, in `main` (`0c81e61`).** Motore **Azure** primario e **DeepL** di riserva. Provato sul corpus vero e poi sul primo SOP militare, che ha aggiunto tre correzioni (§«Che cosa ha insegnato il primo documento VERO»): il **segmento che è solo un identificatore** non parte più (28 su 218), il **grassetto rotto** si ripara, e le **intestazioni delle tabelle** hanno un glossario umano. **Documenti bilingue**: si scrive in italiano, si legge in italiano **o** in inglese. La traduzione non e' un campo del documento (sarebbero **19 colonne gemelle**) ma una **memoria indicizzata sull'hash del testo sorgente** — da cui vengono gratis l'incrementale (cambio una frase, manca solo quella), il **dedup** (stessa frase in 50 documenti = una chiamata) e il fatto che una correzione umana valga **ovunque e per sempre** (`Origin = Human` non si sovrascrive mai). ✅ **Invariante del committente**: le due lingue dicono **la stessa cosa** — la divergenza non e' un rischio da sorvegliare, e' irrappresentabile. Misurato: corpus attuale **23.344 caratteri**, i 15 SOP militari **179.864** (~85k al netto di coordinate e identificatori) → la prima traduzione di tutto sta in un mese di piano gratuito. ⚠️ **VID e nomi utente non escono mai** (decisione del committente): il timbro `ValidityStamp` e' salvo perche' e' un *componente*, ma il **firmatario vLOA** e' scritto a mano in un blocco editoriale — tre difese (derivare, proteggere col roster, **fail closed**) e un test sul `vipi.db` reale. ⚠️ Il pericolo non e' il lessico ma la **fraseologia**: *plausibile ma non standard* e' peggio di *assente*. ⚠️ Le sezioni derivate interpolano ~15 campi di prosa umana: senza quelli l'inglese esce **a chiazze**. |
| [feature/2026-08-28-archivio-atc-mondiale.md](feature/2026-08-28-archivio-atc-mondiale.md) | ✅ **ESEGUITA il 28 agosto**, verifica live contro IVAO vero; ✅ **fuso in `main`** la notte del 28 agosto (`f120d5c`), ⚠️ **consegna attesa entro il 1° settembre 2026**. **Il poller smette di buttare le postazioni fuori divisione.** La fotografia della rete (`/v2/tracker/whazzup`, una chiamata al minuto) porta **tutte** le postazioni ATC del mondo e l'adattatore ne teneva solo i prefissi italiani: le altre erano già pagate e diventavano **irrecuperabili**, perché il whazzup non ricorda il passato e lo storico si interroga per prefisso di callsign. Ora si archiviano tutte, marcate con `AtcSession.IsOutsideDivision`. ⚠️ **La colonna è in NEGATIVO, e non per gusto**: un `bool NOT NULL` nuovo nasce `false` su tutti e tre i percorsi che creano schema (migrazione, `EnsureCreated`, `PostgresSchemaReconciler`) e le 21 133 righe in archivio sono tutte di divisione (verificato: zero callsign non-`LI`) — in positivo quel default avrebbe dichiarato straniero l'intero storico italiano. **Il filtro è sceso di un piano**: l'adattatore restituisce tutto e filtra chi sa a cosa serve la lista (cache live, attribuzione del traffico, piste da ATIS, statistiche, copertura scali, ricalcolo turni) attraverso un metodo solo, `AtcSessionScope.DiDivisione()` — e **non** un filtro globale, che si dimenticherebbe al contrario. ⚠️ L'unica lettura che NON filtra è quella con cui il poller chiude le sessioni sparite: escluderci il mondo lascerebbe le connessioni straniere **aperte per sempre**. Ritenzione **dodici mesi per tutto**, scelta sui numeri: 240 byte/riga misurati sul `vipi.db` reale, e il **D1 dell'archiviatore del validatore** (13,35 MB dal 2 giugno = 0,153 MB/giorno) che dà **575-805 sessioni/giorno** nel mondo, cioè **10×-14×** le italiane → **~93 MB l'anno** su MariaDB. §3-ter misura il database **intero** tabella per tabella (**10,05 MiB** oggi, di cui metà sono già le sessioni) e lo proietta a regime: **~230 MB** su MariaDB, di cui ~85 sono le righe fuori divisione — e il pezzo più grosso non è il mondo, è il **dettaglio del traffico** (~72 MiB), che nasce solo dalla divisione, ma il riassunto mensile resta **solo italiano** — riassumerci il mondo vorrebbe dire metterlo in classifica. Nuovi: pagina staff `/services/stats/world` (esclusa dalla cache delle letture anonime: vive sotto `/services/stats` e non porta `admin` nell'indirizzo) e `GET /vsop/api/v1/atc/sessions`, anonimo e limitato come `/vsop/live/atc`, che nasce perché il validatore dei tour teneva un **archiviatore proprio** sullo stesso whazzup. ⚠️ **Si ricomincia da capo**: lo storico del Worker NON si travasa, la raccolta parte il **1° settembre 2026** e il cutover dal servizio vecchio è nel **2027** — fino ad allora i due archiviatori girano in parallelo. Nessun cancello di data nel codice, ed è voluto: prima del 1° settembre toglierebbe giorni gratis, dopo non ne recupererebbe nessuno (§S2 dei lavori aperti). |
| [feature/2026-08-28-autorizzazioni-a-livelli.md](feature/2026-08-28-autorizzazioni-a-livelli.md) | ✅ **ESEGUITA il 28-29 agosto, otto slice, fusa in `main`** (`8d14b499`). **L'autorizzazione smette di essere un booleano.** `IsAdmin` era **un interruttore solo, 160 usi su 46 file**: o vedi tutto — sorgenti, permessi, statistiche personali di chiunque — o vedi le pagine pubbliche. Ora è un **enum ordinato a cinque livelli cumulativi** (`User` → `IvaoStaff` → `DivisionStaff` → `Editor` → `Admin`) e ogni cancello è un `>=`. ⚠️ **I livelli cumulativi non sono una comodità**: risolvono da soli il caso «un chief d'ACC è anche membro della divisione», che con un insieme di flag sarebbe stata una regola in più da ricordarsi. **L'Editor edita TUTTO** (decisione del committente: «il CH di Roma può dare una mano a quello di Milano»), e da lì **le concessioni per ACC muoiono** — entità, tabella, e le cinque domande che le interrogavano: **219 riferimenti** diventati `IsEditor`, **sincroni e senza parametro**. Chiedere «puoi editare QUESTO documento?» non ha senso se la risposta non dipende dal documento. ⚠️ Con loro sparisce `HasAnyGrantAsync` dal layout — **la prima query di ogni pagina per un utente loggato**, e la causa prima delle corse sul `DbContext` di circuito: il difetto del 24 agosto è chiuso **alla radice**, non mitigato (è sparito anche il `try/catch` che ne ingoiava l'errore, l'unica eccezione motivata del progetto). **Il pavimento non è un controllo, è un `max`**: `Effettivo = max(daStaff, promozione)`, quindi «declassare» sotto la posizione staff è **inerte** — e siccome un no-op silenzioso è una bugia, la pagina disabilita quei livelli invece di accettarli. Le promozioni a mano stanno in `RoleOverrides` (chiave = il VID: «una riga per persona» la garantisce la tabella) e si tengono **intere in memoria**, o si rimetterebbe nel layout la query appena tolta. ⚠️ **La verifica live su cinque identità ha trovato tre difetti che la suite non vedeva**: un **500 sulla pagina Struttura per un Editor** — `OrphanSectorService` chiedeva ancora l'admin, ed è **esattamente** il caso che la carta annunciava (il cancello sta in **due sedi**, la pagina e il servizio) — due pagine senza cancello di pagina, e due falsi allarmi nella sonda invece che nel prodotto. Per poterla fare è nato `DevIdentityOptions`: prima l'identità di sviluppo era una **costante nel codice**, quindi cinque livelli = cinque ricompilazioni, quindi in pratica non si verificava. **86 test nuovi**, tre migrazioni (coda MariaDB a **ventitré**). ⚠️ **Al deploy gli `IT-` fuori dagli otto codici di direzione smettono di editare**: la risposta è una promozione a mano, trenta secondi — ma va annunciata. |
| [feature/2026-08-29-convertitore-coordinate.md](feature/2026-08-29-convertitore-coordinate.md) | ✅ **ESEGUITA il 29 agosto, dieci slice, e FUSA in `main`** (il ramo `convertitore-coordinate` non esiste più). ⚠️ Dal 30 agosto prende anche **un'area dal catalogo degli spazi aerei**. **Un servizio nuovo in `/services`: le coordinate, da qualunque formato ai due che ci servono.** `/services/coordinates`, riservato a **DivisionStaff** e superiori. In ingresso **tredici forme** riconosciute da sole (DMS Aurora puntato e compatto, DMS coi simboli, coi due punti e a spazi, gradi e primi decimali, ARINC a larghezza fissa, decimali con segno o emisfero, `lat:lon` del DB, CSV/Google Maps, sectorfile a punti e a segmenti, JSON/GeoJSON, **KML/KMZ**); in uscita i **due** chiesti, col sectorfile in **due forme** — l'**elenco dei punti** (`N042.00.28.000;E011.58.06.000;`, il default) e i **segmenti** (`latA;lonA;latB;lonB;TIPO;NOME;`). ⚠️ **La differenza che decide tutto**: il DB e l'elenco punti elencano **VERTICI**, la forma a segmenti elenca **LATI** — cinque vertici fanno cinque righe solo perché l'ultima **chiude l'anello**, e quel lato va **generato**; al contrario si prende il primo punto di ogni riga e il vertice di chiusura non si riscrive. Tipo e nome esistono **solo** per i segmenti, e spariscono dalla pagina quando non servono. **Nessun modello nuovo e nessun motore nuovo**: il DMS e l'ordine lat/lon del JSON **traslocano** in `Vipi.Application/Coordinates` (`DmsCoordinate`, `AuroraRingJson`) e `AuroraSectorfileParser` **delega** in una riga — due verità sullo stesso formato divergono sempre, e di solito in una sola; la mappa è quella dell'**AoR** (`AccAor` + `vipi-aor.js`, 2D e 3D, +1 riga di JS per il tratteggio), e **una coordinata sola** si vede come un cerchietto da 0,3 NM costruito con `CircleShapeBuilder`, che c'era già per le TWR. Il motore è **puro**: nessuna tabella, **nessuna migrazione**, nessuno stato fra una visita e l'altra. ⚠️ **I test hanno trovato tre difetti prima dello schermo**, tutti dove vertici e segmenti si incontrano — il peggiore faceva uscire l'area R14A con **un** punto su cinque. ⚠️ **E la verifica dal vivo ne ha trovati altri CINQUE che la suite non vedeva**, con nove progetti verdi: il righello diceva «linea aperta» sotto un poligono **chiuso** (e perimetro e area erano in disaccordo fra loro), le chip delle aree comparivano **due volte** — proprio ciò che la carta vietava — «1 points read from 1 lines» col plurale sgrammaticato e un conto tautologico, gesti **inerti** con una coordinata sola, e il confronto acceso che «non faceva niente» perché a conversione esatta le due forme si sovrappongono **al pixel**. **143 test nuovi** (suite a **3 984** su nove progetti), Release verde su net8 e net10 con 0 avvisi. Rinviate dal committente al «ragionamento più ampio»: salvare l'area fra le aree regolamentate e incollare un `italy.restrict` intero (lavori aperti §W3). |
| [lavori-aperti.md §Q16](lavori-aperti.md) *(non ha una carta sua)* | ✅ **CHIUSA il 30 agosto 2026 — la spesa di traduzione si CONTA, non si deduce.** Fino a ieri si ricavava dai testi rimasti in memoria, ed è una cosa diversa: un segmento tornato **rotto** non si salva — giustamente, una frase senza il callsign sembra giusta e non lo è — quindi i suoi caratteri, **pagati**, erano invisibili al tetto. Ora c'è **`TranslationSpends`**: una riga per invio coi caratteri **spediti**, rotti compresi, perché è quello che il fornitore fattura. ⚠️ **La fotografia iniziale**: passare al contatore avrebbe azzerato la spesa e il tetto avrebbe creduto di avere tutta la franchigia davanti — per DeepL **non si rinnova** — quindi al primo avvio si scrive una riga `Baseline` per motore con la vecchia stima. ⚠️ E «una volta sola» **si chiede al database**, non a un campo: il processo si riavvia, e un flag scriverebbe una fotografia in più, cioè **gonfiando** la spesa. ⚠️ **Il difetto gemello**: una frase di partenza di una vLOA tornava **rotta a ogni giro** — due segnaposti attaccati (`LIBB/LGGG`), che un motore tende a fondere — **155 caratteri ogni quindici minuti**, ~446 000 al mese, per una traduzione che sapevamo già. Le sette frasi di `VloaSections` hanno ora l'italiano **accanto all'inglese** e si **seminano** (`FrasiVloa`), come i titoli ufficiali. ⚠️ **Un avviso che non dice QUALE è inservibile**: «1 segmento rotto» non si può cercare, e il testo ce l'avevamo in mano dove lo buttavamo. **12 test nuovi, due migrazioni.** |
| [feature/2026-08-29-spazi-aerei-dal-kmz.md](feature/2026-08-29-spazi-aerei-dal-kmz.md) | ✅ **TUTTE E SETTE LE SLICE FATTE, verifica dal vivo compresa, e FUSA in `main` il 30 agosto 2026** (merge `86576ecc`). **Gli spazi aerei dell'AIP, da un KMZ caricato a mano**: certi avvicinamenti controllano esattamente il **CTR** — Amendola sono due zone, **Catania sette** — e da IVAO arriva un poligono solo, generoso. ⚠️ **Il file non contiene contorni, contiene SCATOLE 3D**: tetto, pavimento e una parete per lato — **26 989 poligoni per 1 536 volumi**, e `TMA MILANO Z1` da sola ne ha 147. La regola (poligoni a **una sola quota**, dedup dell'anello 2D) dà **un anello per volume, sempre**, verificata su tutti e 1 536. ⚠️ **Le quote non stanno nelle coordinate** (lì `GND` è la quota del *terreno*) ma in due campi di testo, e **sopra l'UNL convenzionale non c'è un livello ma l'illimitato**, scritto in tre modi (`FL999`, `FL980`, `FL2000`). ⚠️ **Sulle classi decide il NOME, non la categoria**: la classe D contiene 104 CTA **e** 49 fra ATZ e MATZ, e l'unico volume di classe C è `PISA CTR Z3`. ⚠️ **§6-bis, la correzione alla carta**: l'aggancio **non si scrive** in `RegionMapPolygon` come diceva la prima stesura — quella colonna tiene **UN anello** e `ParsePoints` scende su `items[0]`, quindi le sette zone di Catania sarebbero diventate **una**, disegnata benissimo. Vive in una tabella sua e si legge dove l'AoR si costruisce, dove i poligoni sono già una **lista**; **confinanti, attribuzione del traffico e vLOA restano sulla forma di IVAO**, e la pagina lo scrive. ⚠️ L'aggancio cita la **chiave naturale**, non l'id: un file nuovo rifà tutte le righe. Un volume sparito lascia l'aggancio **scoperto** — il settore torna a IVAO — senza cancellarlo. Le **aree regolamentate (R, P, D) e le TRA** si leggono ma **non si usano**: vengono dal catalogo IVAO (decisione del committente), e la lista bianca delle famiglie sta **in un posto solo**. **S4**: l'ATZ al posto del cerchio da 5 NM, fonte **secondaria** (dopo il sectorfile) — **13 torri su 84** hanno un confine vero, e ⚠️ un ICAO con **più di un ATZ si salta** (Guidonia due, Torino Aeritalia tre). **S5**: `/services/vsop/airspace` (nata pubblica a `/services/airspace`, dal 1 settembre 2026 riservata allo **staff di divisione**, vecchio indirizzo tolto), una famiglia per volta (362 volumi tutti insieme sono ~620 KB), riusa `AccAor` per intero. **S7**: il rapporto radioassistenze che **segnala e basta** — ⚠️ e la verifica dal vivo ha trovato che dava **54 «canale diverso»** mentre **49 erano lacune** e solo **5** discordanze vere: ora l'**ordine delle voci è la gravità**. ⚠️ **Il ciclo AIRAC lo dichiara chi carica** (il file non lo porta), e ⚠️ dove una release **congela** la sezione AoR l'aggancio si vede in pubblico solo dopo aver **ripubblicato**. **104 test nuovi, quattro migrazioni.** |
| [feature/2026-08-29-quote-transizione-colonna-destra.md](feature/2026-08-29-quote-transizione-colonna-destra.md) | ✅ **ESEGUITA il 29 agosto**, ✅ **fusa in `main`** (avanzamento diretto, ramo cancellato). **Metà della sezione «Quote di transizione» era vuota**: la tabella dei livelli ha un tetto di 420px — giusto, sono due colonne di numeri — e su una sezione da 822px lasciava **402px** liberi (misurato a schermo su LIBD). Fra sei proposte il committente ha scelto le due che usano dati **già in casa**: **«Transition Level adesso»**, il verdetto sul QNH del METAR scritto grande con l'ora del bollettino — ⚠️ **`noprint`**, come il meteo, perché su carta sarebbe già scaduto — e **«Dati del campo»** (elevazione in piedi e metri, variazione magnetica **con l'emisfero**, IATA, coordinate). ⚠️ I quattro dati del campo il database **ce li aveva già** e nessuna pagina pubblica li mostrava: arrivano dall'anagrafica in cache (`AirportStation`, cinque colonne in più su una lettura che il layout fa comunque, **zero query nuove**) e **non** dallo snapshot di release — non sono dati di release, li riscrive il giro notturno, e nello snapshot sarebbero **trattini** su ogni documento già pubblicato finché qualcuno non ripubblica; il vSOP **militare** li ha allo stesso prezzo, mentre passando dal profilo dell'aeroporto avrebbe dovuto fare sei query che oggi non fa. La scheda «adesso» non compare **senza TA** (lì la tabella scrive «N/A» e un livello grande accanto sarebbe inventato) e, con un QNH fuori da ogni fascia, **resta e lo dice**. Va a capo con un **flex**, senza `@media` e senza `@container`: la soglia la calcola il layout, che lo `zoom` dell'applicazione lo vede. **7 test nuovi, nessuna migrazione**; verifica dal vivo su LIBD e LIML nei due temi: vuoto a destra **402px → 0**. ⚠️ **Correzione del committente subito dopo**: le schede erano **appese in alto**, ora stanno al **centro** dell'altezza della tabella — con due schede sposta di 10px, ma il caso che contava è **una scheda sola** (campo senza METAR: niente «TL adesso»), dove il vuoto sotto era lungo quanto quello che la colonna era nata per chiudere; misurato su LIML, **67px sopra e 67 sotto** contro 0 e 134. Quando la riga va a capo il centraggio è un **no-op**. |
| [feature/2026-09-01-fraseologia-e-traduzioni.md](feature/2026-09-01-fraseologia-e-traduzioni.md) | 🟡 **In `ricaduta-verticale-e-cicli`, verificata dal vivo, NON in produzione.** **Il glossario di fraseologia e il registro delle traduzioni diventano UNA pagina** (`/services/vsop/admin/glossary`; il vecchio indirizzo del registro è una **seconda rotta** della stessa pagina, e nella barra admin la voce è una sola: 17 → 16, per l'Editor 12 → 11). Non erano due lavori: si scrive una voce di glossario ed è nel registro che si va a vedere se è servita, e passare da un indirizzo all'altro ricaricava tutto, **coppia di lingue compresa**. Due sezioni richiudibili, **una** tendina di direzione — ⚠️ prezzo dichiarato: le direzioni non pesano uguale (**22 formule `it→en` contro 1**, **176 frasi `en→it` contro 98**), quindi una sezione è sempre la più magra. ⚠️ **La ricerca sta sul DATABASE**: il registro mostrava **cento righe su 176 e non lo diceva**, e un filtro sulle righe già caricate direbbe «non c'è» di una frase che sta alla riga 101 — cioè mentirebbe **esattamente** quando serve. Il **M** di «N di M» esce dalla **stessa** query dell'elenco, e l'ordinamento è **totale**, o «carica altre» salta e ripete righe. **«Dove si usa»**, a due livelli (una formula vive nelle frasi, una frase vive nei documenti): pastiglia col numero in elenco, pannello al clic coi documenti e il **dove** (prosa · tabella · titolo). ⚠️ Il **corpus editoriale si legge UNA volta per LOTTO** — 499 campi per 23 344 caratteri: `DocumentiToccatiAsync` lo leggeva intero per **una** frase, e cento righe sarebbero state cento letture; ora quel conteggio **poggia** sulla stessa passata, o pastiglia ed elenco divergono. ⚠️ **Gli id di sezione sono PER VERSIONE**, e il collegamento «vedi» preso dalla versione sbagliata apre la pagina giusta su un'ancora che lì **non esiste** (la stessa «Remarks» di LIBC è la 611 nella pubblicata e la 651 nella bozza): vince l'occorrenza della versione **corrente**, e se la frase sta solo in una vecchia l'ancora **non si offre** — il titolo sì. ⚠️ **Il filtro per origine è UN comando a tre stati, non due interruttori**: misurato, 192 righe umane **tutte** riviste e 82 automatiche **tutte** mai riviste, **zero** miste, perché `SaveHumanAsync` scrive `ReviewedUtc` e ribalta `Origin` nello stesso gesto — così si guadagna lo stato che prima non si poteva chiedere: **solo le corrette da una persona**. Più «N di M» + «carica altre», tastiera (Invio nel glossario, **Ctrl+Invio** nel registro — là è un'`<textarea>` e una resa può avere un a-capo, Esc ovunque), testo cercato **marcato** col componente `<Marca>` (⚠️ `AddContent`, **mai** `MarkupString`: quelle frasi le scrivono persone) e l'ordine del glossario (recenti / A→Z per `SourceKey`, o «Riporta» finirebbe prima di «attendi»). **24 test in `RicercaEDoveSiUsaTests`, nessuna migrazione.** |
| [feature/2026-08-31-ricaduta-verticale-e-cicli.md](feature/2026-08-31-ricaduta-verticale-e-cicli.md) | 🟡 **In `ricaduta-verticale-e-cicli`, verificata dal vivo, NON ancora in produzione.** Due cose che sembrano diverse e sono l'**albero di copertura**. **(1) Il ciclo, visto in produzione**: su `atc.it.ivao.aero` `LIMF_WW0_APP` era **nipote di sé stesso**. L'aeroporto pendeva da `LIMF_WN0_APP`, che aveva come padre scritto `LIMF_WW0_APP`, che padre scritto non ne aveva — e `AirportPositionLadder.ParentOf`, per un APP (nessun gradino sopra), usciva diritta su `airportParent`, cioè su una **propria discendente**. ⚠️ Nessuna guardia poteva vederlo: `EnsureNoCycle` riceveva i soli padri **scritti**, mentre tutti leggono `EffectiveParentCallsign`; e la validazione stava dentro `if (parentCallsign is not null)`, quindi scegliere «eredita» **non passava da nessun controllo** — ed è il gesto che l'ha armato. E non esplodeva: ogni lettore ha una guardia sui nodi già visti, per cui la catena si **tronca in silenzio**. Quattro mosse: la derivazione non punta più a un proprio discendente (**null**, meglio orfana e visibile che ciclica e muta); `EffectiveHierarchy` è la porta unica dell'albero effettivo e la guardia valida una **simulazione** della modifica, rifiutando gli anelli che essa **crea** e non quelli che trova (in produzione uno c'è, e la pagina è l'unico posto da cui si scioglie); la pagina sceglie le **radici** col padre effettivo come già faceva coi figli (era la differenza fra i due criteri a disegnare il nodo due volte); e un rilievo «Gerarchia ciclica» nel report, rete per le porte che scrivono padri senza chiedere niente. Misura: **0 anelli attivi** in sviluppo, ma **19 aeroporti** pendono da una PROPRIA APP — erano 19 inneschi a un clic. **(2) La ricaduta verticale**: l'albero a un padre solo non può dire «questi due si sostituiscono a vicenda». Misurato: Milano è divisa a **FL325** e i due alti stanno **uno sotto l'altro** (`ES5 → WS5 → WS2`), quindi con **WS5** chiuso il traffico alto d'ovest finiva su WS2, che sopra FL325 non ha niente, mentre lo teneva ES5. `SectorFallbacks` porta righe con **fascia di quota** (piede incluso, tetto **escluso**), consultate **prima** del padre — che resta la **coda implicita**, e per questo la tabella nasce **vuota** e non c'è nessun travaso (né un `Sql` in migrazione, vietato nella finestra cieca). Risoluzione in **ampiezza**. La usano la vista live (quota del **punto**) e `TransferMatcher` (quota di **crociera**); ⚠️ le **statistiche restano fuori di proposito** (lì le pretese andrebbero rese per pezzo di forma). **(3) La geometria propone** accoppiando per **banda**, non per pianta: ES5 e WS5 in pianta non si toccano mai, è lo *strato* a renderli sostituti. ⚠️ **La verifica live ha trovato quattro difetti che i test non vedevano**: un titolo italiano dentro la pagina inglese, **155 proposte** (Algeri, Vienna, Zurigo — mancava del tutto un criterio geografico: ora la proposta si ferma all'**ACC**), il bersaglio che non si rivedeva nella riga (`value` su `<select>` senza `selected`, e una riga che *sembra* vuota alla riscrittura **viene scartata**: il difetto si mangiava il dato), e DEL/GND proposti a FL325 perché senza poligono la banda risulta «tutta aperta» — **«non ho una forma» non è «prendo tutto il cielo»**. **56 test nuovi, una migrazione additiva** (due provider). |
| [feature/2026-08-31-riconnessione-circuito.md](feature/2026-08-31-riconnessione-circuito.md) | ✅ **IN PRODUZIONE dal 31 agosto 2026** (pacchetto 1.1.0, verificato dal vivo: `/vsop/ping` 204, la Ricerca risponde, console pulita — lavori aperti §A15). **«Attempting to reconnect to the server…» — quattro mosse.** Quel riquadro non vuol dire che il sito sia giù: vuol dire che è morto il **circuito**. Le cause sono due e chiedono rimedi **opposti** — un buco di rete (il circuito di là c'è ancora: **riprovare**) e un processo morto e rinato, che su Plesk+Passenger succede **da solo** per inattività (il circuito non esiste più da nessuna parte: **ricaricare**). Il difetto era che il secondo caso finiva nel comportamento del primo: si riprovava, si falliva, e restava un messaggio inglese con un tasto. **(1) Prima si conta**: `avvio-diagnostica.txt` è **riscritto** a ogni avvio, quindi tre riavvii al giorno e quaranta producevano lo stesso file; ora `diagnostica/avvii.txt` scrive **in coda** una riga per avvio e una per arresto — e ⚠️ **un AVVIO che segue un altro AVVIO è un processo morto male**, perché lo spegnimento per inattività fa in tempo a scrivere la sua riga e un crash no. **(2)** `blazor.web.js` parte con `autostart="false"` e ad avviarlo è `vipi-riconnessione.js`, che è l'unico modo di scrivere i tempi: 55 tentativi ogni 5 s (≈ la finestra di retention), riquadro **nostro** tradotto e dentro il tema, e sullo stato `rejected` — che è il server a dire «quel circuito non lo conosco», non una deduzione — **si ricarica da soli**, con un tetto di 3 ricariche al minuto; sullo stato `failed` **no**, perché quasi sempre è la rete dell'utente e una pagina ricaricata senza rete è una pagina d'errore del browser. ⚠️ **`autostart="false"` e quel file sono una cosa sola**: un pacchetto col primo e senza il secondo dà un sito che si vede **intero e non risponde a niente**, senza errori in pagina — presidiato da `RiconnessioneTests`. **(3)** `/vsop/ping`, `204` e basta, chiamato ogni 2,5 min dalle schede **visibili**: a Passenger per non spegnere serve *una richiesta qualsiasi*, e ⚠️ **non deve diventare una sonda** (`/vsop/health/ready` fa due query: sarebbe carico continuo per scheda aperta). **(4)** I tempi dai due capi: retention **2→5 min**, `ClientTimeoutInterval` **30→60 s**, `KeepAliveInterval` 15 s scritto, `HandshakeTimeout` **15→30 s**. ⚠️ La retention non serve a niente quando muore il processo — i circuiti trattenuti muoiono con lui. **18 test nuovi, nessuna migrazione.** Restano fuori: il **pinger esterno** (decisione di Ivao.It) e la revisione di quali schermate admin abbiano davvero bisogno di `InteractiveServer`, da fare **dopo** aver letto `avvii.txt`. |
| [feature/2026-08-31-corse-dbcontext-e-diagnostica.md](feature/2026-08-31-corse-dbcontext-e-diagnostica.md) | 🔴 **FATTA il 31 agosto 2026, ramo `corse-e-perdita-diagnostica` da `consegna-20260831` — NON fusa, NON consegnata.** ⚠️ **Quattro difetti letti nella diagnostica del server, e il primo era la diagnostica stessa.** Il committente segnala «This page did not open» e «A second operation was started on this context instance» su un documento su cui lavora **da solo**, e consegna la cartella `diagnostica/`. La direzione la da' **`avvii.txt`**, il file nato il giorno prima per un altro motivo (§AE): due AVVII con «il processo precedente NON si e' spento in modo ordinato», a due-tre ore l'uno dall'altro — una cadenza cosi' regolare non e' un difetto che scatta su un gesto, e' **qualcosa che cresce**. **(A)** `CollisioniDbContext` — lo strumento scritto il 24 agosto *per capire* le corse sul `DbContext` — teneva accanto alla tabella debole un elenco delle liste vive e ci aggiungeva un riferimento **a ogni comando SQL**, potandolo solo quando scattava una fotografia (quasi mai): con quattordici `HostedService` piu' il traffico sono milioni di oggetti in poche ore. La prova a occhio nudo sta nel file — la stessa lista di operazioni stampata **34, 38 e 44 volte** dentro una sola fotografia. Ora la tabella debole si enumera da se', tetto di 64 per contesto, interruttore `VIPI_DIAGNOSTICA_COLLISIONI=0`, e via `DataReaderClosing` dall'intercettore (EF chiama *sia* Closing *sia* Disposing: la seconda chiusura toglieva la riga di un'**altra** operazione con lo stesso SQL). ⚠️ **La regola: uno strumento di diagnosi non puo' tenere stato che cresce con il traffico.** **(B)** Il registro allegava **venti** fotografie a ogni errore: **634 kB per tre voci** su un tetto di rotazione di 512 kB (la storia utile ruotava via dopo tre errori), e la voce delle 11:40 portava uno scatto delle **11:37**, di un'altra richiesta — una scena altrui allegata alla tua la prima volta la si legge come se fosse la tua. Ora **una** fotografia, e solo se scattata **entro dieci secondi**. **(C)** `TranslationReviewPanel` correva contro l'editor che lo contiene: non isolato, lettura **non condizionata al cambio dei parametri** (`OnParametersSetAsync` scatta a **ogni ridisegno** del genitore) e documento caricato **due volte** — la seconda con l'altra lingua, solo per dedurre se il vuoto significasse «stessa lingua» — per un blocco **chiuso di suo**. Ora scope proprio in **lettura**, guardia su `(DocumentId, lingua)` e `RevisioneAsync` che **dice** la lingua sorgente; ⚠️ la **scrittura** resta sul contesto del circuito e non va «uniformata» (passa da `IEditAuthorizationService`, che in uno scope nuovo risponderebbe «anonimo» e rifiuterebbe il salvataggio a tutti). **(D)** `SopHome.OnInitialized()` era `=> Stations.Prewarm();` nudo: la barra quella lettura la protegge dal 24 agosto, ma **ingoiare lascia la cache vuota** e la pagina sotto ritenta sullo stesso contesto rotto — il presidio proteggeva la barra e non cio' che le sta dentro. **(E)** E il **rimedio alla radice**, nello stesso giro: il catalogo delle stazioni si legge **una volta per processo** (`CatalogoStazioni`, singleton) invece che una per circuito — così la lettura da cui nascono C e D non c'è più. ⚠️ Ha richiesto di chiudere prima un buco che nessuno vedeva: `Bump()` c'era in **4** punti contro **11** posti che scrivono `Acc`/`Airport`, e con la cache scoped il dato vecchio durava un istante mentre con quella di processo durerebbe fino al riavvio — la spinta è passata dai servizi a un `SaveChangesInterceptor`. **23 test nuovi, nessuna migrazione** (consegnabile nella finestra cieca). Restano §AM2–AM4: la **verifica dal vivo** — queste corse si aprono solo con la latenza di un database remoto —, rileggere `avvii.txt`, cancellare `errori-richieste.txt` sul server. |

## Decisioni architetturali — `adr/` 🟢
| File | Scopo |
|---|---|
| [adr/adr-0001-scelte-architetturali-fondanti.md](adr/adr-0001-scelte-architetturali-fondanti.md) | Fondamenti (stack, layer, persistenza, AoR, polling). Emendato da ADR-0002. |
| [adr/adr-0002-integrazione-e-autenticazione-portabile.md](adr/adr-0002-integrazione-e-autenticazione-portabile.md) | Integrazione come RCL + identità portabile (`ICurrentUserProvider`, scenari A/B/C). |
| [adr/adr-0003-trasporto-live-sse.md](adr/adr-0003-trasporto-live-sse.md) | Trasporto push live = SSE. |
| [adr/adr-0004-configurazione-divisione-e-admin.md](adr/adr-0004-configurazione-divisione-e-admin.md) | Configurazione divisione + derivazione codici admin. |
| [adr/adr-0005-superficie-modulo-e-isolamento.md](adr/adr-0005-superficie-modulo-e-isolamento.md) | Superficie del modulo + isolamento CSS/JS. |
| [adr/adr-0006-indipendenza-sorgente-dati-e-policy-import.md](adr/adr-0006-indipendenza-sorgente-dati-e-policy-import.md) | Indipendenza dalla sorgente + policy di import (+ nota Round 20: fonte unica). |
| [adr/adr-0007-produzione-persistenza-e-scala.md](adr/adr-0007-produzione-persistenza-e-scala.md) | Produzione: tampone WAL SQLite ora + cutover Postgres pianificato + scala Blazor + guardia identità dev. Aggiornamenti: 30-lug (reconciler + drift probe D1-bis), **1-ago (D4: MySQL solo su net8)**. |

## Refactor — `refactor/` 🟢
Asse di revisione strutturale post round ~23-34 (doc di area 01→10, **tutti eseguiti**). Ordine di studio bottom-up.
| File | Scopo |
|---|---|
| [refactor/00-overview.md](refactor/00-overview.md) | Piano: DAG dipendenze, principi, indice dei doc di area, ordine di studio. |
| [refactor/REFACTOR-PROCESS.md](refactor/REFACTOR-PROCESS.md) | Runbook anti-vibecoding per i refactor (ciclo Fase 0→4, gate «carta prima di codice»). |
| [refactor/01-import-infra-condivisa.md](refactor/01-import-infra-condivisa.md) | Infrastruttura di import condivisa. |
| [refactor/02-import-acc-e-settori.md](refactor/02-import-acc-e-settori.md) | Import ACC e settori. |
| [refactor/03-import-aeroporti-e-settori.md](refactor/03-import-aeroporti-e-settori.md) | Import aeroporti e settori. |
| [refactor/04-import-github.md](refactor/04-import-github.md) | Import da GitHub (sectorfile Aurora: SID, ecc.). |
| [refactor/05-import-confinanti.md](refactor/05-import-confinanti.md) | Import ACC confinanti/esteri. |
| [refactor/06-gerarchia.md](refactor/06-gerarchia.md) | Gerarchia di copertura (padri per callsign, cross-ACC). |
| [refactor/07-trasferimenti.md](refactor/07-trasferimenti.md) | Coordinamenti/trasferimenti (sorvoli, vLOA in stile ACC+EN). |
| [refactor/08-modello-documento-ed-editing.md](refactor/08-modello-documento-ed-editing.md) | Modello `Document`+`DocumentVersion` unificato per tutti e 4 i tipi + editing. |
| [refactor/09-flusso-pubblicazione.md](refactor/09-flusso-pubblicazione.md) | Flusso di pubblicazione generico (registry `IReleaseTarget`/`IDocKindRoutes`). |
| [refactor/10-snapshot-totale-e-rendermode.md](refactor/10-snapshot-totale-e-rendermode.md) | Snapshot totale al publish + `RenderMode` per sezione; visibilità pubblica = release effettiva. **Merged.** |
| [refactor/11-uniformita-tre-documenti.md](refactor/11-uniformita-tre-documenti.md) | Uniformità vIPI ACC / vIPI APP / vLOA fra editor, bozza e pubblica (audit 2026-07-30, P1→P9): chiave di sezione univoca, resa editoriale condivisa, `DocumentSection.IsHidden` e `BeforeParentBody` versionati, fallback a pubblica frozen, superficie APP = non remotizzati, stato iniziale di apertura delle sezioni. Include **§3bis: non-problemi verificati**, da non «aggiustare». |
| [refactor/12-vista-live-unificata.md](refactor/12-vista-live-unificata.md) | **Vista live unificata per callsign** (2026-07-31): una pagina sola `/services/vsop/live[/{callsign}]` al posto di `AccLivePage`/`AppLivePage`, descrittori per tipo di ente (`ILiveStationKind`) con **TWR/GND/DEL** finalmente coperti, postazione dalla connessione IVAO senza selettore, trasferimenti per mittente effettivo. Rimosse anche le `Ridotta*` morte. |
| [refactor/14-quattro-documenti.md](refactor/14-quattro-documenti.md) | **I quattro documenti, un motore solo** (audit di supervisione 2026-08-27): la vIPI d'aeroporto entra nel confronto e le famiglie diventano quattro. Guardia release↔documento nel servizio, la vLOA smette di scrivere il ciclo AIRAC a mano, snapshot letto una volta per pagina, un guscio solo per i quattro editor, un ciclo solo per le sezioni dei viewer, le sezioni alla nascita dal catalogo, un enum solo. **Prove di parità sui cinque profili.** L'aeroporto rientra nel modello: editor a componenti (**2180 → 1001** righe), pagina SSR statica con due isole, nascita del documento unica. **Chiuso**: suite 5714, zero voci aperte. |

## Storia — `history/` ⚪
> ⚠️ Le rotte citate qui dentro sono quelle **del giorno in cui il documento è stato scritto**: i resoconti non
> si riscrivono. Dal 22 agosto 2026 il prefisso è `/services/vsop` e dieci segmenti sono in inglese — la
> tabella di conversione sta in [feature/2026-08-22-servizi-atc-e-profile-swapper.md](feature/2026-08-22-servizi-atc-e-profile-swapper.md) §2.
| File | Scopo |
|---|---|
| [history/rounds.md](history/rounds.md) | **Changelog cronologico** dei round (R5→R34) + asse refactor 01→10, retention/fix pubblicazione e feature 2026-07 (confinanti, QoL editor trasferimenti, condizione operativa pista/area). |
| [history/handoff-round5.md](history/handoff-round5.md) | Handoff di chiusura del Round 5 (fusione Settore/Posizione). |
| [history/handoff-round22.md](history/handoff-round22.md) | Handoff di sessione Round 22 (shape tonda TWR + coord aeroporto + rifiniture trasferimenti/AOR). |
| [history/handoff-coordinamenti-fasi-3-4.md](history/handoff-coordinamenti-fasi-3-4.md) | Handoff coordinamenti/trasferimenti (fasi 3-4, refactor 07). |
| [history/piano-round20.md](history/piano-round20.md) | Piano esecutivo del Round 20 (fonte unica cataloghi). |
| [history/review-flusso-gap.md](history/review-flusso-gap.md) | Analisi flusso vs documenti (decisioni round 4). |
| [history/audit-2026-07-14-correttezza-fonti-dati.md](history/audit-2026-07-14-correttezza-fonti-dati.md) | Audit senior correttezza + fonti-dati multiple: findings A1–A4/B1–B5, falsi positivi, fix. |
| [history/audit-2026-07-22-criticita-full-stack.md](history/audit-2026-07-22-criticita-full-stack.md) | Audit full-stack (back/front/DB): 15 criticità con severità + piano a fasi + Fase 1 (health-check migrazioni, osservabilità import, rete test bUnit/E2E). |
| [history/audit-2026-07-30-concorrenza-e-ridondanze.md](history/audit-2026-07-30-concorrenza-e-ridondanze.md) | Audit concorrenza/codice morto/ridondanze: 8 fix di race condition, import SID rotto in silenzio, 450 righe morte, 4 estrazioni + bug Razor `v@r.…` trovato in verifica live. |
| [history/audit-2026-08-11-crepe-full-stack.md](history/audit-2026-08-11-crepe-full-stack.md) | Audit full-stack **con esito**: 34 voci (2 bloccanti, 5 alte, 17 medie, 11 di debito), 23 chiuse, 3 ribaltate dalla misura, 5 rimandate con la ragione. Carta scritta prima di toccare il codice, esito in fondo. La CI era rossa sul ramo del doc 13 e su net8 girava 1 progetto di test su 7. |
| [history/audit-2026-08-23-frontend-ui.md](history/audit-2026-08-23-frontend-ui.md) | Audit **frontend/UI con esito**: 15 voci (4 alte), tutte chiuse in giornata su `audit-frontend-ui`. Il filo che le lega: **tre difetti su tredici nascono da regole che il progetto aveva gia' scritto, applicate a meta'** (`Chip.razor` sui `<span>` cliccabili, `App.razor` sui 592 KB di three.js, il foglio sul fuoco visibile) — sfuggite perche' quel punto era arrivato da un'altra strada. Dentro: l'ACC che dal rename del 22 agosto **non era piu' evidenziato in topbar**; i comandi AoR che esistevano **solo per il mouse** sulle pagine pubbliche; **nessuna pagina con un `<h1>`**; e la terza ricaduta di «una `@media` misura la finestra, non il layout» — il documento a **161px a zoom 1.8**, curato con una `@container`. ⚠️ La prova che ritaggare 20 pagine non cambia il disegno e' MISURATA (216 titoli, prima/dopo): il primo giro ha trovato **sei regressioni vere** che sarebbero passate. In coda due correzioni all'analisi: in tutti e due i casi mentiva la grep, non il codice. |
| [history/audit-2026-08-25-versioni-release.md](history/audit-2026-08-25-versioni-release.md) | Audit **versioni & release con esito**: 8 difetti, tutti chiusi in giornata su `audit-versioni-release` (8 commit, +6 test, migrazione doppia). Il filo: **sei su otto sono regole gia' scritte, applicate a meta'** — il gate `IsHidden` c'era in tre tipi su quattro ma la vIPI ACC nascosta restava servita all'URL diretto; il lock di editing lo pretendeva il publish-versione ma non «Pubblica ora» della stessa pagina (che promuoveva la bozza ALTRUI a meta'); «una release per ciclo» era imposto e annullato nella stessa funzione (due «Programmata» gemelle); il diff della release in vigore diceva «nessuna release in vigore» con tutto «Aggiunta»; la retention potava per stato mentre gli stati invecchiano da soli; le foto citate solo da release rimosse non si liberavano mai. Piu' l'indice unico sul progressivo di release (con la SELECT di controllo pre-deploy) e tre frasi UI raddrizzate. In coda le osservazioni SENZA esecuzione: la chiave ACC legata al catalogo (release orfane mai potate), la schedulata che fotografa la bozza di quel momento, il payload corrotto che ripiega in silenzio. |
| [history/audit-2026-08-27-prestazioni.md](history/audit-2026-08-27-prestazioni.md) | Audit **prestazioni con esito**, misurato e non letto: applicazione compilata in Release, avviata su copia del `vipi.db` reale e cronometrata. Dieci voci, otto eseguite. **Prima visita 336 192 -> 113 052 byte (-66%), avvio 465 -> 153 query.** Il filo: **quattro difetti su otto sono default del framework mai scritti** — il livello di compressione (`Fastest` = qualita' 1 per Brotli, che faceva scaricare il 24% IN PIU' che non avere Brotli affatto), il livello di log (EF scriveva il testo di ogni query su disco: 1 MB ogni 210 pagine), un `@rendermode` sull'ingresso del sito che non ha un solo comando, e un `DateTime.UtcNow` dove serviva il timbro della sorgente (312 UPDATE a ogni avvio, e la regola D8 che rispondeva «la sorgente lo manda ancora? si', perche' abbiamo riavviato»). Dentro anche: minificazione e precompressione al publish (il **44% dei byte di CSS/JS erano commenti**), i moduli JS pesanti caricati solo dove servono (verificato in Edge), e la cache delle letture anonime. ⚠️ **DUE interventi pianificati sono stati SCARTATI SU MISURA** e la misura e' nel codice: ReadyToRun (+29 MB per un 2% nel rumore — l'avvio e' database, non compilazione) e la deduplicazione dei poligoni AoR (guardava i byte GREZZI: compressi, toglierli fa uscire PIU' byte). In coda il metodo: **quattro test verdi che non dimostravano niente**, e un `grep` di verifica che diceva «0 falliti» mentre un progetto non compilava. |
| [history/audit-2026-08-29-vsop-militari-relazione.md](history/audit-2026-08-29-vsop-militari-relazione.md) | Audit **della RELAZIONE fra il vSOP militare e la vIPI civile**, letto contro la carta del 27 agosto: dieci voci, tutte chiuse. Il filo: il documento militare era agganciato al motore di **lettura** (viewer, editor, catalogo, traduzione, descrittori) e **non** a quello di **governo** — elenco unificato, impatti, drift, eliminazione, congelamento. Il segno misurabile: **`MilDocumentId` aveva quattro lettori in tutto il repository**, `DocumentId` decine. Le quattro grosse: (A) **nessun provider di congelamento** per `AirportMil` e la vista che leggeva lo snapshot della release **civile** — su Pisa mostrava la fotografia dell'altro documento, su Rivolto il congelamento era un no-op; (B) i tre elenchi generici senza `.Include(d => d.MilAirport)`: il documento **spariva** da ricerca, «Novità», VersioniPage, incarichi, eliminazione e drift, in silenzio; (C) gli impatti che non avvisavano mai il gemello militare, cioè la promessa del §2 mantenuta dal motore e non da chi doveva dirlo all'editore; (D) l'eliminazione di un aeroporto **solo militare** che lasciava il documento orfano. Più: il badge pilota/ATC **mai reso da nessuna pagina** e il filtro su 2 viewer su 5, `AppMil` con due rotte verso pagine mai scritte, e `DescribeOrder` in **pareggio** invece che «più basso di tutti». ⚠️ Perché la suite non lo vedeva: l'aiutante `TestReleaseTargets` conosceva **quattro** descrittori su sei. ⚠️ E TRE voci le ha trovate solo lo SCHERMO, pubblicando i due documenti di un campo MISTO (Linate): **un vSOP militare pubblicato diceva «nessun vSOP militare pubblicato»** (il bersaglio di release non si risolveva sul legame militare — il viewer pubblico non aveva mai funzionato su un documento pubblicato, perché l'unico guardato era in BOZZA), **tre tabelle su tre erano titoli vuoti** (il corpo derivato lo disegnavano solo le sezioni radice) e **il catalogo non trovava le sezioni annidate** (venti su ventisei date per libere anche nell'editor). La misura finale, stesso scalo e stesso istante: civile `LIML_TWR 118.999` (ripubblicata), militare `118.100` (non ripubblicata) — snapshot indipendenti. Suite **3 841**, nove progetti su nove. |

---
**Nota:** i commenti nel codice sorgente (`.cs`/`.razor`) citano i documenti per **nome e sezione** in forma informale (es. «modello-dati §9.12», «ADR-0001 D5»), non come link a percorso.

<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <!-- La prova del motore sull'albero INTERO del sector (carta F2 §5): round-trip byte per byte di ogni
         file e conto delle righe che il motore conserva senza capirle. Si lancia a mano, puntando a una
         copia del sector; in CI girano i campioni dei test. È nella soluzione perché compili sempre. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Vipi.Sectorfile\Vipi.Sectorfile.csproj" />
    <!-- Solo per la concordanza: il DMS di vIPI (DmsCoordinate) contro quello del motore (carta F2 §2.4). -->
    <ProjectReference Include="..\..\src\Vipi.Application\Vipi.Application.csproj" />
  </ItemGroup>

</Project>

<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <!-- Tool one-shot: non parte del deploy. Ripreso a mano quando serve rifare il seed del DB hostato. -->
  </PropertyGroup>

  <ItemGroup>
    <!-- Eredita VipiDbContext + entità + i provider EF Sqlite/Npgsql (referenziati non-private in Infrastructure). -->
    <ProjectReference Include="..\..\src\Vipi.Infrastructure\Vipi.Infrastructure.csproj" />
  </ItemGroup>

</Project>

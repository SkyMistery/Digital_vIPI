@page "/"
@inject NavigationManager Nav

<PageTitle>vIPI · IVAO Italia</PageTitle>

@code {
    // Host di sviluppo: la home reindirizza al portale vIPI montato dalla RCL.
    protected override void OnInitialized() => Nav.NavigateTo("/vsop", replace: true);
}

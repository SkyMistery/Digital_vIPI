<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- net8.0 serve SOLO per l'embedding nel sito Ivao.It (host net8). Il ramo net10.0 resta quello
         consumato da Vipi.Host e dai test. Vedi docs/guide/integration.md §TFM. -->
    <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>

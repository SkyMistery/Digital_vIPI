@* L'AVVISO DI SIMULAZIONE — la riga che dice che questa documentazione non porta nessuno da nessuna parte.

   Sta su OGNI documento pubblico (le cinque famiglie), sulla vista live e sulla mappa degli spazi aerei,
   sotto il titolo; e a piè di OGNI foglio stampato di OGNI pagina del sito (SopLayout).

   ⚠️ IL TESTO STA QUI E SOLO QUI. Ha SETTE sedi a schermo, più l'intestazione di stampa e il piè di
   pagina: scritto a mano in nove posti diventerebbe nove testi leggermente diversi al primo ritocco.
   Chi deve cambiarlo cambia `Testo`, e cambia ovunque.

   ⚠️ NON SI TRADUCE, ed è una decisione: è l'inglese in una pagina italiana, come le briciole di pane
   (regole-lingua R3) e per la stessa ragione — non è prosa del sito, è un cartello. Chi lo trovasse
   "dimenticato" fuori dai .resx legga R8 prima di spostarlo. *@

<span class="sim-disc">@Testo</span>

@code {
    /// <summary>
    /// L'avviso, alla lettera. ⚠️ Inglese in entrambe le lingue del sito (regole-lingua R8): non passa dai
    /// `.resx` e non passa dal motore di traduzione.
    /// </summary>
    public const string Testo = "ONLY FOR SIMULATION: DO NOT USE FOR REAL LIFE NAVIGATION";
}

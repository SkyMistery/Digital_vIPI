@* Sezione Minime di vettoramento. NON è più un placeholder in attesa dell'import: quello è stato scartato
   il 9 agosto 2026 (nel sectorfile la struttura dei file MVA non dice a quale settore appartiene un'area, e
   indovinarlo su un dato operativo è peggio che non averlo — vedi docs/lavori-aperti.md §E2).
   Il riquadro dice quindi come stanno le cose, non una promessa: se le minime servono, si scrivono qui come
   contenuto editoriale. Testo cambiato insieme alla decisione, per la regola di propagazione del runbook. *@
@inject IStringLocalizer<SharedResource> L
<div class="callout" style="margin:0">
    <span class="cic">ℹ️</span>
    <div>
        <p class="ct">@L["AppMinima_Title"]</p>
        @L["AppMinima_Body"]
    </div>
</div>

@code { }

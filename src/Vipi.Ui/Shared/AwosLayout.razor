@* Il layout del quadro vAWOS: nient'altro che la pagina.

   ⚠️ Niente topbar, niente breadcrumb, niente menù — e non è minimalismo. Questa pagina si apre in una
   scheda a parte e ci resta per tutto il turno, su un secondo monitor: ogni pixel di cornice del sito è
   spazio tolto ai numeri che si guardano da lontano, e un menù che non si userà mai è un invito a
   cliccare via dal quadro mentre si controlla.

   ⚠️ Non eredita `.vipi-root`: il quadro ha una tavolozza sua (vipi-awos.css) e non segue il tema
   chiaro/scuro del sito. Il perché sta in testa a quel foglio.
*@
@inherits LayoutComponentBase

@Body
